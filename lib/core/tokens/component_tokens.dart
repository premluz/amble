/// Tier 3 — component tokens: rare, per-component overrides for cases
/// where a component genuinely deviates from the Tier 2 semantic default.
///
/// Intentionally empty. Components should consume [AmbleTheme] (Tier 2)
/// directly until a specific component has a real, documented reason to
/// deviate — at that point, add a token here (e.g. `TaskPillTokens`), not
/// a hardcoded value in the widget.
library;

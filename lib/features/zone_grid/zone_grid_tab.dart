/// The two tabs shown at the top of the Weekly Zone Authoring Grid, per
/// the reviewed mockup. Only [zones] is built this session — [events] is a
/// separate, comparably-sized future feature (a parallel grid for
/// Tasks/calendar events), rendered in the tab chrome but non-interactive.
enum ZoneGridTab { events, zones }

name: amble
description: "A local-first task and timeline planner."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.13.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.4.2
  riverpod_annotation: ^4.0.6
  hive_ce: ^2.16.0
  hive_ce_flutter: ^2.3.4
  uuid: ^4.6.0
  flutter_svg: ^2.3.0
  flutter_local_notifications: ^22.3.0
  timezone: ^0.11.1
  flutter_timezone: ^5.1.0
  share_plus: ^13.3.0
  file_picker: ^12.0.0
  path_provider: ^2.1.6
  package_info_plus: ^10.2.1
  lottie: ^3.5.1
  speech_to_text: ^7.4.0
  workmanager: ^0.10.9
  http: ^1.6.0
  device_calendar: ^4.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  riverpod_generator: ^4.0.8
  build_runner: ^2.16.0
  hive_ce_generator: 1.11.2

dependency_overrides:
  analyzer: ^13.0.0
  # device_calendar 3.9.0 (this project's original pin) turned out to be
  # fundamentally incompatible with this Flutter version — not just its
  # Gradle config (jcenter(), old compileSdkVersion), but its actual
  # Android Kotlin source still targets the pre-V2 plugin embedding API
  # (PluginRegistry.Registrar), which no longer exists at all. A local
  # Gradle-file-only patch got past the build-config errors but hit real
  # Kotlin compile errors — confirmed by attempting it directly, not
  # guessed — so the package was upgraded to 4.3.3 instead (modern
  # embedding, actively fixed as recently as 2024) rather than maintaining
  # what would effectively be a source-level fork.
  #
  # 4.3.3 itself declares timezone ^0.9.0, which conflicts with this
  # project's existing timezone ^0.11.1 (flutter_local_notifications
  # requires ^0.11.0, used for Task/Zone alerts). Forcing timezone to stay
  # on our existing, already-tested ^0.11.1 here — timezone is pure Dart
  # with no native/platform surface, so overriding a package's *declared*
  # constraint (not its actual runtime compatibility) is safe in a way the
  # device_calendar situation above was not. Confirmed with the user
  # before making either change, per CLAUDE.md's dependency-change rule.
  timezone: ^0.11.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Animated Noto emoji (Lottie JSON), bundled locally rather than fetched
  # from Google's CDN at runtime — see completion_checkbox.dart and
  # docs/DECISIONS.md.
  assets:
    - assets/celebrations/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
  #
  # App-wide font — JetBrains Mono (OFL-1.1, bundled locally in
  # assets/fonts/, see assets/fonts/OFL.txt). Referenced by name via
  # TypePrimitives.fontFamily (lib/core/tokens/type_primitives.dart) rather
  # than hardcoded at call sites, so a future font swap is one constant.
  fonts:
    - family: JetBrains Mono
      fonts:
        - asset: assets/fonts/JetBrainsMono-Thin.ttf
          weight: 100
        - asset: assets/fonts/JetBrainsMono-ExtraLight.ttf
          weight: 200
        - asset: assets/fonts/JetBrainsMono-Light.ttf
          weight: 300
        - asset: assets/fonts/JetBrainsMono-Regular.ttf
          weight: 400
        - asset: assets/fonts/JetBrainsMono-Italic.ttf
          weight: 400
          style: italic
        - asset: assets/fonts/JetBrainsMono-Medium.ttf
          weight: 500
        - asset: assets/fonts/JetBrainsMono-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/JetBrainsMono-Bold.ttf
          weight: 700
        - asset: assets/fonts/JetBrainsMono-ExtraBold.ttf
          weight: 800

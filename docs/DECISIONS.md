# Amble — Decisions Log

Running log of choices made mid-build that aren't big enough for the Constitution but are still worth not re-litigating. Append-only, dated.

---

**[Locked pre-build] Task colors are semantic categories, not free-pick.**
Users choose from a fixed set of category colors (health, work, personal, admin, etc.) mapped through the Tier 2 token layer — not an open color picker. Keeps the timeline visually coherent and keeps colors swappable via re-theme without touching feature code.

**[Locked pre-build] State management is Riverpod, not Provider/Bloc.**
Chosen for closest fit to existing Zustand-style mental model (atomic, composable, testable without BuildContext threading) and good code-gen ergonomics with Claude Code sessions.

**[Locked pre-build] Editor/tooling: VS Code + Flutter/Dart extensions, Claude Code for implementation sessions.**
This chat (or equivalent) stays the architecture/planning layer; Claude Code in the actual project directory does file-by-file implementation.

**[Locked pre-build] Xcode 15.4 vs. 16+ — non-blocking.**
Works today, upgrade deferred to a "spare hour" task rather than a build blocker.

**[Phase 1] Riverpod providers use `keepAlive: true`, not `autoDispose`.**
Task CRUD state is session-scoped app state, not screen-scoped — `autoDispose` caused a real bug where a notifier's own write could be torn down mid-flight if no listener was active. Applies to `taskListProvider` and `taskByIdProvider`; default to `keepAlive` for any future app-level state provider unless there's a specific reason a provider should be screen-scoped.

**[Phase 1] `shared/providers/` added as a real folder, not just `features/*/providers`.**
Task CRUD state isn't feature-specific — it sits next to `shared/models/` and `shared/repositories/`, which it directly wraps. Feature-specific providers (if any arise) still belong under their own `features/*/` folder; this only applies to state that multiple features would otherwise duplicate.

**[Pre-Phase 3] Timeline scaffolding: custom-build the capsule blocks, allow a lightweight package for the date strip.**
The vertical day-timeline with capsule task blocks is the product's visual signature (per the Structured teardown and ARCHITECTURE.md's note on this being a design-taste problem as much as a code one) — building it on an existing calendar package (e.g. Syncfusion) would mean fighting the package's rendering/theming defaults to get the rounded, icon-overlapping capsule look, likely costing more effort than building custom on `CustomPainter`/`Stack` over the existing token system. The week/date strip at the top is a well-solved, non-differentiating pattern — a lightweight package there is fine if one saves real time, still subject to the "flag new dependencies" rule before adding.

**[Pre-Phase 3] Color primitives switched to OKLCH.**
Prompted by the clay/ochre category-color distinguishability concern flagged during Phase 2 review — hex/RGB doesn't guarantee two colors that read as "close" are actually perceptually close, OKLCH's perceptual uniformity does. Retrofit happens before Phase 3's capsule block work rather than after, since Phase 3 is about to make the current token values load-bearing across every timeline block. Implemented as a hand-written OKLCH→sRGB conversion (no new dependency — simple enough not to need one), applied only to color primitives; see ARCHITECTURE.md for the resulting `const` → `static final` deviation for that one token category. Category taxonomy (health/work/personal/admin) and its four accent hues are re-derived from OKLCH with deliberate hue/lightness/chroma spacing, not just re-hexed from the Phase 2 approximations.

**[Phase 3, Step 0] New category OKLCH values — needs review, not final.**
All four category accents fixed at equal L=0.70, C=0.13; hues spread 30°/120°/250°/340° (health/work/personal/admin) rather than naive 90° steps, specifically to avoid landing any pair on the red-green complementary axis (the classic protanopia/deuteranopia confusion line) — minimum pairwise hue gap is 50° (up from Phase 2's clay/ochre gap of ~35°), most pairs are 90°+ apart. The Phase 2 `moss500` (admin, dark yellow-green) is renamed `berry500` (magenta-pink) since re-deriving with proper spacing moved its hue far enough that the old name no longer describes the color — flagging the rename since it's a public token name, not just a value change. Confirmed visually via a debug build on iPhone 15 Pro Max simulator (`lib/features/timeline/timeline_capsule_preview_main.dart`, a scaffolding-only entry point) — all four colors read as clearly distinct at a glance.

**[Phase 3, Step 1] Capsule block badge-overlap padding bug, caught via simulator screenshot.**
The first capsule-block implementation positioned the icon badge correctly but under-computed the text content's left padding (used `badgeSize/2 + spacingSm` instead of `badgeSize + spacingSm`), so task titles rendered partially hidden behind the badge — invisible in `flutter analyze`/widget-test-without-golden, only caught by actually looking at a rendered screenshot. Flutter's headless `flutter test` golden rendering also doesn't load real fonts by default (renders text/icons as blank boxes), so the golden alone wasn't trustworthy for this review — confirmed the fix against a real debug build on the iOS Simulator instead, then regenerated the golden. If font-accurate goldens become worth the setup cost later, that's a `flutter_test_config.dart` + bundled font-loading addition, not yet done here.

**[Phase 3] Current-time indicator refreshes via a 1-minute `Timer.periodic`, not lifecycle events.**
The work order left refresh strategy as a judgment call (live vs. on-focus/resume). A periodic timer is simpler than wiring `AppLifecycleState` listeners and correct for the actual requirement — the line only needs to move once a minute, matching clock-face granularity. Scoped to `CurrentTimeIndicator`'s own `State` (started in `initState`, cancelled in `dispose`), so it only runs while the widget is mounted — no background timer leak risk. Revisit only if a real battery-cost concern surfaces; not expected at 1 update/minute.

**[Phase 3] Timeline day view opens scrolled to roughly the current time, not the top of the day.**
Added after reviewing the populated-day screenshot and finding the current-time indicator (correctly rendered) sat below the fold on load, at 5pm on a 6am–10pm view. A day view whose signature live element isn't visible without scrolling defeats the point of building it. Implemented via `ScrollController.jumpTo` in a post-frame callback in `_DayTimeline`'s `initState` — not requested explicitly by the work order but treated as directly in service of the current-time-indicator requirement, not scope creep.

**[Phase 3] `TaskCapsuleBlock`'s badge size is duplicated (not imported) in `timeline_screen.dart` for position math.**
The screen needs the badge's rendered size to convert "top of badge" positioning into "center of badge on the hour row" — but the badge size is an internal layout constant inside `TaskCapsuleBlock`, not part of its public API, and the component was explicitly kept unchanged this session. Mirrored the `spacingXl * 1.5` expression at the call site with a comment pointing back to the source of truth, rather than exporting a constant from the component or changing its interface. Worth promoting to a real shared constant (or a token) if a third call site ever needs it — per the "value used twice becomes a token" rule, this is the second use, so it's already a candidate.

**[Phase 3] Timeline golden test left stale through 3 rounds of visual iteration, per explicit instruction — now doubly stale.**
`test/features/timeline/timeline_capsule_preview_test.dart`'s golden was never regenerated across the two-column restructure, the badge/connector resize, or this session's alignment fix, since the work order said to hold until final sign-off on each round. It now fails `flutter test` with a large, expected pixel diff. Regenerating it is explicitly the first thing due whenever that file is next touched — noting here so a future session doesn't mistake the failure for a new regression.

**[Phase 4] `main.dart` replaced wholesale, not incrementally — Explore/Saved/Profile scaffold removed entirely.**
The scaffold mismatch (flagged repeatedly since Phase 0) had no real content worth preserving — placeholder pages with no data, no navigation logic tied to SCOPE.md's actual Inbox/Timeline/Settings structure. Replaced `AmbleHome`'s whole `IndexedStack`/`NavigationBar` shell with `Scaffold(body: TimelineScreen())` directly, no bottom nav yet. Bottom nav is deferred until Inbox (Phase 5) and Settings actually exist — building nav chrome for screens that don't exist yet would be exactly the kind of premature scaffolding CLAUDE.md warns against. `AmbleTheme.light` is now registered on `ThemeData.extensions` in `main.dart`, which the old scaffold never needed since it didn't consume Tier 2 tokens.

**[Phase 4] Completion/skip/reschedule status colors reuse the pre-existing, previously-unused `colorTaskCompleted`/`colorTaskSkipped` tokens — not the category color, and not a new "success green."**
Phase 2 defined these tokens speculatively; this session is their first real consumer. `completed` swaps the badge to `colorTaskCompleted` (sage, not the task's category color) plus a small checkmark overlay and a dimmed/strikethrough title — deliberately not the category color, so "done" reads as a distinct, calm state rather than "still that category, just also done." `skipped` swaps to `colorTaskSkipped` (neutral gray) rather than the previous alpha-blended category color, which is more correct token usage (a real semantic color, not a derived tint) and reads as more clearly "did not happen" without being punitive. `rescheduled` intentionally gets **no color change** — just a small neutral "moved" icon next to the time — per design principle 1 (no shame-coding); a rescheduled task is not a failed one, so recoloring it would misrepresent what the status enum is for.

**[Phase 4] Drag-to-reschedule snaps to 5-minute increments, tracked as a local `_dragOffset`, committed only on release.**
Continuous free-form dragging (no snap) would make it easy to schedule a task at :07 or :23, which doesn't match how the hour-marker grid or any real calendar UX reads. 5 minutes is fine-grained enough to feel responsive but coarse enough to always land visually aligned. The drag is purely a local widget-state visual (`_DraggableTaskBlockState._dragOffset`, reset to 0 on release) — nothing is written to the provider/repository until `onVerticalDragEnd`, so an aborted or accidental drag never touches persisted data. Reschedule logic itself (setting `originalScheduledAt` only once, moving `status` to `rescheduled`) lives in `TaskList.rescheduleTask` in the provider, not the UI — the widget only computes a `DateTime` and hands it off, per the "all writes go through taskListProvider" rule.

**[Phase 4] Real bug: `AppSheet`'s Cupertino branch had no `Material` ancestor — crashed the moment sheet content used a `TextField`.**
Caught while trying to screenshot the new task detail sheet, which was the first real (non-trivial) sheet content built since Phase 2 — `AppSheet`'s original test coverage only ever put a bare `Text` inside the sheet, on Material platform only, so this gap was invisible until now. `showCupertinoModalPopup` doesn't provide a `Material` ancestor the way `showModalBottomSheet` does implicitly; any Material-family widget (a `TextField`, in this case) inside Cupertino-branch sheet content throws "No Material widget found." Fixed by wrapping the Cupertino branch's content in `Material(type: MaterialType.transparency, ...)` inside `AppSheet` itself, so any future sheet content works on both platform branches without every caller needing to know about this. Also strengthened `design_system_scaffold_test.dart`'s sheet-content probe from a bare `Text` to a real `TextField`, and added a Cupertino-platform run of that same test — the Material-only, `Text`-only coverage is exactly why this didn't surface at Phase 2.

**[Phase 4] Task detail screen redesigned as a near-full-screen modal, not `AppSheet`.**
Per direct visual-reference feedback: a colored, edge-to-edge category header with an inline-editable title, a wheel-style time scroller, and a duration slider, presented almost full-screen (small gray gap at the very top, large corner radius) rather than `AppSheet`'s partial-height bottom sheet. Built as its own `PageRouteBuilder` (slide-up transition) inside `showTaskDetailSheet`, not through `AppSheet` — the colored, padding-free header doesn't fit `AppSheet`'s fixed white-background-plus-uniform-padding contract, and other, simpler future sheets should still get to use `AppSheet` as-is rather than inheriting this screen's specific visual needs. One form now serves both create and edit (previously discussed, confirmed with the user), pre-filled from the existing task when editing.

**[Phase 4] Two new adaptive `core/widgets/` components: `AppSlider`.**
The duration control needed a slider, and per design principle 4 (no raw `Material*`/`Cupertino*` in feature code), that meant a proper adaptive wrapper (`Slider`/`CupertinoSlider`), not an inline one-off in the feature file — added `AppSlider` alongside the existing `AppButton`/`AppSheet`/`AppIconButton`. `ListWheelScrollView` for the time picker was used directly (not wrapped) since it's a core Flutter widgets-library type, not Material- or Cupertino-specific, same class as `GestureDetector`/`Stack`.

**[Phase 4] Time scroller steps in 15-minute increments; duration slider steps in 5-minute increments, 5–120 min range — flagged, not derived from the reference.**
The reference screenshot's scroller showed 15-minute increments (17:30/17:45/18:00/...), kept as-is. The duration slider in the reference used discrete named steps (1/15/30/45m/1h/1.5h); per direct instruction this was intentionally *not* followed — replaced with a continuous 5-minute-step slider from 5 to 120 minutes instead, since a labelled multi-stop segmented control doesn't translate cleanly to a slider and 5-minute granularity is finer/more flexible.

**[Phase 4] Surface tokenization for the new gray-background/white-panel layout — flagged, not resolved.**
The redesigned detail screen introduces a "gray page background, white content panel" pattern (`colorSurfaceTimeline` reused for the gray, `colorSurfacePrimary` for the white panels) that doesn't yet have its own semantic name in `AmbleTheme` — it's borrowing two tokens whose original semantic intent was "timeline canvas" and "primary surface," not "elevated panel over a dimmed page." Works correctly today; flagged per the user's own call to defer a dedicated token (e.g. `colorSurfaceElevated`) rather than block this session on a token-system change.

**[Phase 5] `Task.scheduledAt`/`durationMinutes` made nullable to represent unscheduled (Inbox) tasks.**
The model had no way to represent "captured but not yet scheduled" — both fields were required and non-nullable, and the Timeline's day-filter provider unconditionally dereferenced `scheduledAt`. Considered a sentinel-date hack (rejected — violates "no magic values" and creates real collision/ambiguity risk) versus nullable fields plus a derived `isScheduled` getter. Chose the nullable-field approach (confirmed with the user via AskUserQuestion rather than guessing, per CLAUDE.md). Added `Task.captured({title, notes})` as a second factory alongside `Task.create(...)`, title-only per design principle 2, defaulting `category` to `TaskCategory.personal` (changeable later when scheduled). `isScheduled` checks both fields are non-null (they're always set together — see `TaskList.scheduleTask`). Call sites that only ever see scheduled tasks (`TaskCapsuleBlock`, `TimelineScreen`'s positioning/drag math) use `!` rather than re-checking, since their callers (`tasksForSelectedDayProvider`) already guarantee it — documented in each with a doc comment rather than re-validated defensively.

**[Phase 5] Bottom nav built now — Inbox + Timeline, Settings still deferred.**
Phase 4's decision deferred all bottom nav chrome until Inbox existed (see above entries), on the grounds that scaffolding for screens that don't exist yet is exactly the premature-scaffolding CLAUDE.md warns against. Inbox now exists; Settings (Phase 8) still doesn't, so the nav ships with two real destinations rather than a three-tab shell with a Settings stub. `AmbleHome` (`main.dart`) wraps both screens in an `IndexedStack` behind a Material `NavigationBar`, defaulting to the Timeline tab on launch (index 1) to preserve prior default behavior. Add the Settings destination when Phase 8 gives it real content, not before.

**[Phase 5] Move-to-timeline reuses the existing task detail screen — no new scheduling UI.**
`TaskDetailForm`'s existing edit path (`existing.scheduledAt = _scheduledAt; existing.durationMinutes = _durationMinutes; ... updateTask(existing)`) already handles turning an unscheduled task into a scheduled one correctly — it just assigns real values to fields that were previously null, then persists via the normal update path. No changes were needed to `task_detail_sheet.dart` for this. `TaskList.scheduleTask(task, {scheduledAt, durationMinutes})` was still added to the provider as a named, intent-revealing entry point for any future non-UI caller, even though the sheet itself doesn't need it.

**[Phase 6] Exit-confirmation "worth asking" threshold: non-empty title AND at least one field (including the title itself) changed from its starting value.**
The work order asked for the threshold to be flagged, not guessed. Landed on: closing via the × button prompts only if the title is non-empty *and* something differs from where the form started (the task's saved values on edit, the pre-filled defaults on create) — checked against a snapshot taken in `initState` (`_initialTitle`/`_initialScheduledAt`/`_initialDurationMinutes`/`_initialCategory`/`_initialNotes`). Title counts as one of the tracked fields, not just a gate — typing a title from empty is itself "a field touched from its default," so a bare "typed a title, hit close" (the most common real case, e.g. from Quick Capture) does trigger the prompt. An empty title short-circuits to a silent close regardless of other fields, since no save is possible either way. Caught via the widget test in `test/features/task_detail/exit_confirmation_test.dart`: an earlier version only diffed the non-title fields, so typing a title alone never prompted — a real bug, not a spec ambiguity, fixed before considering the feature done.

**[Phase 6] Edit-flow close uses "Discard changes" / create-flow close uses "Delete draft" — same dialog, different secondary-button label and semantics.**
Confirmed with the user rather than guessing: an edit-flow close isn't discarding a draft, it's declining to save changes to an already-persisted task, so the destructive-styled button reads "Discard changes" and — critically — its handler is just `Navigator.pop()` (no delete call), leaving the previously-saved task exactly as it was. Create-flow close (nothing persisted yet) keeps "Delete draft," semantically a true discard, though it's the same no-op `Navigator.pop()` under the hood — the two paths only differ in what's already on disk, not in the code executed. Distinguished via `widget.task != null` (`isEditing`) inside `_handleClose`.

**[Phase 6] New adaptive component: `AppAlertDialog`.**
No existing two-choice confirmation dialog existed in `core/widgets/` — `showDialog`/`showCupertinoDialog` had never been used anywhere in the app. Added `AppAlertDialog.show(...)` (Cupertino `CupertinoAlertDialog` / Material `AlertDialog`, mirroring `AppSheet`'s platform-branching pattern) rather than reaching for a raw platform dialog inline, per design principle 4. Takes a primary and secondary `AppAlertDialogAction` (label + `isDestructive`), returns `true`/`false`/`null` for primary/secondary/dismissed.

**[Phase 6] Real bug found via testing, not screenshots: `flutter test` hangs on real Hive I/O triggered from a tapped widget.**
Full root-cause and fix in `docs/ERROR_LOG.md` ("`flutter test` hangs indefinitely on real Hive disk I/O triggered from a widget interaction"). Notable because it was caught by writing a widget test *before* trusting an on-device screenshot of the "Discard changes" outcome — the screenshot alone (an empty Inbox after discard) was ambiguous between "discard worked correctly" and "discard accidentally deleted the task," and only the widget test's explicit `expect(box.values.single.title, 'Existing task')` assertion actually proved which one was true. The on-device scaffolds (`exit_confirm_schedule_main.dart`/`exit_confirm_discard_main.dart`) were then updated to seed a second, untouched Inbox item so the screenshots themselves became unambiguous too, but the widget test is what caught and confirmed correctness first.

**[Phase 7] Two new dependencies added for notification scheduling: `timezone` and `flutter_timezone`.**
`flutter_local_notifications`' `zonedSchedule` — the only API that survives device timezone/DST changes correctly — requires `timezone`'s `TZDateTime`. `timezone` itself has no way to detect the device's actual IANA timezone name, and `flutter_local_notifications`' own README names `flutter_timezone` as the standard companion for exactly this gap. Both confirmed with the user before adding, per CLAUDE.md's "no new dependency without flagging first" rule; a hardcode-UTC alternative was explicitly rejected (silently wrong times for every non-UTC user is not acceptable). `NotificationService.initialize()` calls `FlutterTimezone.getLocalTimezone()` once, then `tz.setLocalLocation(...)`, before the plugin is otherwise usable.

**[Phase 7] Notification scheduling lives in `shared/services/notification_service.dart`, called from `TaskList` (the provider layer) — never from feature/UI code.**
Matches the existing repository-pattern precedent (`TaskRepository`/`HiveTaskRepository`): `NotificationService` only knows "schedule a task's alert" / "cancel by id" / "sync" (cancel-then-reschedule-if-appropriate); it has no opinion on *when* those should happen. Every `TaskList` mutator (`createTask`, `scheduleTask`, `updateTask`, `rescheduleTask`, `toggleComplete`, `deleteTask`) calls into it right after the repository write, so a screen never has to remember to sync a notification itself — the same reasoning that already keeps all persistence behind `taskListProvider`. `captureTask` (Inbox capture) is the one mutator that doesn't call it: an unscheduled task has no start time to alert on, so there is nothing to sync.

**[Phase 7] `syncForTask` always cancels first, then re-schedules only if still appropriate — one function, correct for every write path.**
Considered separate `schedule`/`reschedule`/`cancel` call sites per mutator (mirroring each mutator's specific semantic), but a single idempotent "cancel whatever alert this task might have, then schedule a fresh one if it should have one" is simpler and can't drift out of sync with itself — there's no way to leave a stale alert behind, since every path (including toggling a task back from `completed` to `pending`) runs through the same function. `syncForTask` skips scheduling entirely if `status == TaskStatus.completed` (no reason to alert for a task already marked done) — this is the one piece of scheduling *policy* that lives in the service rather than being pushed back to `TaskList`, since it's a rule about the service's own responsibility ("don't alert for finished things"), not a task-writing decision.

**[Phase 7] Notification permission requested lazily on first schedule attempt, not at app launch.**
The work order left this as a judgment call. `NotificationService.initialize()` (called once from `main.dart` before `runApp`) sets all `DarwinInitializationSettings.request*Permission` flags to `false`, so iOS shows no prompt on cold start. The actual `requestPermissions`/`requestNotificationsPermission` call happens inside `scheduleForTask`, the first time a task is created or scheduled with a future `scheduledAt` — i.e. the first moment permission is actually needed, not before. If denied, `scheduleForTask` returns early and every other write path (`saveTask`, `_refresh`) proceeds normally; the app has no notification-permission-gated features, so there's nothing else to degrade.

**[Phase 7] Notification id derived deterministically from the task's UUID string via `hashCode & 0x7fffffff`, not a separate persisted counter.**
`flutter_local_notifications` requires an `int` id per notification; task ids are UUID strings. Hashing gives the same task the same notification id every time without persisting a second id anywhere, so cancel-then-reschedule (`syncForTask`) always targets the exact alert a given task previously had. The `& 0x7fffffff` mask keeps the result a non-negative 32-bit int (`String.hashCode` can be negative/64-bit on some platforms), which is what the plugin's Android channel expects. A theoretical hash collision between two different task UUIDs would mean one task's alert silently overwrites another's — accepted as negligible given UUID v4's collision space and no evidence any Dart `hashCode` weakness make this a realistic risk at this app's scale.

**[Phase 7] Tap-to-open routes to the Timeline day containing the tapped task, via a new `notificationTapProvider` — done this session, not deferred.**
The work order flagged this as "if reasonable this session." Implemented via `getNotificationAppLaunchDetails()` (cold start) and `onDidReceiveNotificationResponse` (foreground/background tap) in `NotificationService`, both funneling the tapped task's id into a small keepAlive `NotificationTap` notifier. `AmbleHome` (`main.dart`) is the single place that reacts to it (`ref.listen`): looks up the task via the existing `taskByIdProvider`, calls `selectedDateProvider.notifier.goTo(scheduledAt)`, switches to the Timeline tab, then consumes the pending tap so it can't re-fire on a later rebuild. No new navigation infrastructure — reuses the existing bottom-nav `_selectedIndex` and the existing day-navigation provider.

**[Phase 7] Android manifest additions: two `flutter_local_notifications` receivers plus `RECEIVE_BOOT_COMPLETED`/`SCHEDULE_EXACT_ALARM`/`POST_NOTIFICATIONS` permissions.**
Taken directly from the plugin's own README (see `~/.pub-cache/.../flutter_local_notifications-22.3.0/README.md`), not guessed: `ScheduledNotificationReceiver` and `ScheduledNotificationBootReceiver` are required for scheduled alarms to actually fire and to survive a device reboot; `SCHEDULE_EXACT_ALARM` is the Android 12+ permission `requestExactAlarmsPermission()` requests at runtime, needed for `AndroidScheduleMode.exactAllowWhileIdle` (chosen over `inexact*` modes since a task-start alert that can silently arrive minutes late defeats the point). Notification icon reuses the existing `@mipmap/ic_launcher` rather than adding a dedicated drawable resource — a documented, valid option, just not Android's "official" preference; flagged in code as a candidate to revisit if a dedicated icon is ever designed. No R8/ProGuard `keep.xml` added, since the project has no release-build minification configured yet (`minifyEnabled` absent from `android/app/build.gradle.kts`) — worth adding before a real release build per the plugin's own warning that R8 can silently strip the icon resource otherwise.

**[Phase 7] iOS: `AppDelegate.swift` gained `FlutterLocalNotificationsPlugin.setPluginRegistrantCallback` inside the existing `didInitializeImplicitFlutterEngine`.**
Required per the plugin's README so plugin channels stay available if a notification action ever runs on iOS's background isolate — not currently used this session (no notification actions, only a plain tap-to-open), but it's the documented setup step and costs nothing to have in place now rather than being a silent gap discovered later. No other iOS-side native code was needed; permission requests and scheduling are handled entirely through the Dart-side plugin API.

**[Phase 6 closure] Android: `android/app/build.gradle.kts` gained `isCoreLibraryDesugaringEnabled = true` plus a `coreLibraryDesugaring` dependency.**
A genuine gap left by Phase 6, not an intentional deferral — `flutter_local_notifications` requires core library desugaring on any `minSdk` below 26 (this project's is 24), and Android had never actually been built with the notifications plugin present until this session, so the build failure only surfaced now. Fixed exactly per the plugin's own documented Kotlin/`build.gradle.kts` recipe (`desugar_jdk_libs:2.1.4`), not improvised. `multiDexEnabled` (also mentioned in the same README section) was deliberately *not* added — nothing in this build indicated the 64K method-count limit was actually being hit, and adding it speculatively would be exactly the kind of "design for a hypothetical future requirement" CLAUDE.md warns against; add it if and when a real build actually needs it.

**[Phase 6 closure] Android verification used a fresh local emulator (API 34, `google_apis/arm64-v8a`), not a physical device — no physical Android device was available.**
Created via `avdmanager` (`Amble_Test_API34`, Pixel 6 profile) since no AVD existed on this machine yet. API 34 (Android 14) chosen specifically because it's comfortably past API 33, where `POST_NOTIFICATIONS` became a runtime-requested permission — the exact behavior this session needed to confirm. Plain `google_apis` (not `google_apis_playstore`) to avoid Play Store account/approval friction that isn't relevant to this verification. `adb shell input tap`/`dumpsys notification`/`dumpsys alarm` gave direct, scriptable evidence of real permission-prompt delivery, notification content, and exact-alarm registration — a materially stronger verification path than was available on iOS Simulator last session (which had no equivalent to `adb`, hence the `DeliveredNotifications.plist` workaround documented in `docs/ERROR_LOG.md`).

**[Phase 7 (export/import)] Export/import serialization is hand-written `toJson`/`fromJson` on `Task`, no `json_serializable`.**
The model is small (10 fields) and stable; codegen for this one model would be a new dev-dependency (`json_serializable` + `build_runner` codegen step, on top of the `riverpod_generator`/`hive_ce_generator` already in the build) for marginal benefit. Both directions validate explicitly rather than trusting types: `fromJson` throws `FormatException` for any missing/wrong-typed/unrecognized field (including enum values that don't match `TaskCategory`/`TaskStatus`, and malformed ISO-8601 date strings), so a corrupt or hand-edited backup file fails loudly and specifically rather than constructing a partially-wrong `Task`.

**[Phase 7 (export/import)] Import conflict handling: skip + report, never silently overwrite. Confirmed with the user rather than assumed.**
An imported task whose id matches a local task is compared field-by-field (`Task.hasSameFieldsAs`, deliberately not an `==` override — see its doc comment on why overriding equality on a `HiveObject` subclass is risky). Identical → counted as `alreadyPresent`, not rewritten (avoids a needless Hive write). Different → counted as a `conflict` and left completely untouched; the work order's own instruction was explicit that import must never silently corrupt or overwrite existing data, and building a real merge-resolution UI (which of two conflicting versions to keep, field by field) is a meaningfully larger feature than this session's scope — flagged as a genuine follow-up, not built speculatively. `TaskList.importTasks` returns an `ImportResult` (imported/alreadyPresent/conflicts counts) so the UI can surface this without the caller needing to inspect individual tasks.

**[Phase 7 (export/import)] Import does not trigger notification scheduling, even for imported tasks with a future `scheduledAt`.**
Confirmed with the user rather than assumed (the alternative — routing every imported task through `NotificationService.syncForTask`, same as a normal create — was presented as the explicit alternative). A bulk/backup restore is often historical data being brought onto a fresh install; auto-scheduling a wave of notifications (and triggering the permission prompt) as a side effect of import is a separate, real design decision that deserves its own deliberate session rather than inheriting the "every write syncs notifications" pattern by default. `TaskList.importTasks` is the only mutator that doesn't call `NotificationService`.

**[Phase 7 (export/import)] Entry point: a temporary third bottom-nav tab ("Backup"), not folded into a stub Settings screen.**
Settings (Phase 8) doesn't exist yet. Confirmed with the user rather than assumed (the alternative — a small icon button on the Timeline app bar — was presented as the explicit alternative) — a real nav tab keeps the feature fully discoverable via the same pattern as Inbox/Timeline, at the cost of a nav structure that will need collapsing back into Settings once Phase 8 builds it. Placed *after* Timeline (not between Inbox and Timeline) so the existing notification-tap-to-open logic in `main.dart` (which hardcodes `_selectedIndex = 1` for "switch to Timeline") didn't need to change.

**[Phase 7 (export/import)] `share_plus`/`file_picker`/`path_provider` needed no new platform config — already-installed, general-purpose plugins.**
Unlike `flutter_local_notifications` (Phase 6/7's notification work, which needed real Android manifest additions and Gradle desugaring), these three plugins' basic file-share/file-pick/temp-directory functionality works with each plugin's own bundled manifest merge and no project-side setup. Confirmed by a clean real build and on-device run on iOS Simulator this session (see `docs/PROGRESS_LOG.md`) — Android on-device verification of export/import specifically was not done this session (see the same entry for why), so this is confirmed for iOS only; flag if an Android build surfaces something these three plugins need that wasn't required for notifications.

**[Phase 8 (settings)] Settings is a persistent third bottom-nav tab (Inbox/Timeline/Settings), not reached another way.**
This was flagged as an open UX question in the Phase 8 work order, but SCOPE.md already explicitly documents "Bottom nav: Inbox / Timeline / Settings" as the intended final structure — so this wasn't actually an open decision this session, just a confirmation against existing docs rather than a fresh call.

**[Phase 8 (settings)] "Open OS notification settings" uses `flutter_local_notifications`' own `openAppNotificationSettings()` — no new dependency.**
Confirmed by reading the plugin's source directly (`flutter_local_notifications-22.3.0`); the method is implemented on iOS, Android, and macOS platform channels already. `NotificationService` gained two new thin wrapper methods (`hasPermission()`, `openNotificationSettings()`) rather than a new package for permission-settings deep-linking.

**[Phase 8 (settings)] `package_info_plus` (10.2.1) added as a new direct dependency for app-version display.**
Confirmed with the user before adding (per CLAUDE.md's "no new dependency without flagging first" rule). `PackageInfo.fromPlatform()` is the standard, actively maintained way to read the real installed version/build number at runtime in Flutter; no existing dependency already exposed this.

**[Phase 8 (settings)] Export/import relocated from the temporary Backup tab with zero functional changes — same `BackupService`, same `TaskList.importTasks`, same dev-scaffold pattern.**
Per the work order's explicit instruction ("relocating a working feature to its permanent home, not rebuilding it"). `lib/features/backup/` was deleted outright rather than kept as a redirect/shim, since Phase 7's tab was explicitly documented as temporary scaffolding to be collapsed into Settings once it existed.

**[Phase 9 (device verification + store prep)] Bundle ID/package name left as `com.example.amble` for now — confirmed with the user, not changed.**
The Flutter scaffold default can't be used for real App Store/Play Store submission, but changing it is consequential (re-signing, and on Android a changed `applicationId` post-publish is effectively a new app listing). Since this session is store-*prep*, not submission, the user chose to defer the real identifier decision to the actual submission session rather than lock in a guessed value now.

**[Phase 9 (device verification + store prep)] App icon and splash screen are explicit placeholders — a procedurally generated sage-brand mark, not final branding.**
Generated via a one-off Python/Pillow script (not a new Flutter dependency — `flutter_launcher_icons`/`flutter_native_splash` were considered but not added, since a plain PNG-at-each-resolution approach needed no new package). Uses the app's real `sage500`/`sand50` OKLCH-derived brand colors (ported the existing `oklch()` conversion to Python to get exact values, not eyeballed) so the placeholder is at least on-brand. Covers both platforms: iOS `AppIcon.appiconset` (all 15 required sizes) + `LaunchImage.imageset` + `LaunchScreen.storyboard` background color; Android all 5 `mipmap-*` launcher icon densities + a new `launch_background`/`launch_image` drawable pair + `colors.xml`. Flagged explicitly per the work order's own instruction — swap for real branding before actual store submission.

**[Phase 9 (device verification + store prep)] App display name fixed on both platforms: Android `android:label` moved from a hardcoded lowercase `"amble"` to a proper `@string/app_name` resource ("Amble"); iOS `CFBundleName` capitalized to match the already-correct `CFBundleDisplayName`.**
Both were leftover Flutter-scaffold-default-adjacent values, not previously corrected. Added `android/app/src/main/res/values/strings.xml` (didn't exist before) rather than continuing to hardcode the label inline, since that's the conventional Android pattern and makes any future localization possible without touching the manifest.

**[Phase 9 (device verification + store prep)] Android on-device verification used the existing `Amble_Test_API34` emulator, not physical hardware — no physical iPhone or Android device was connected this session.**
`flutter devices` / `system_profiler SPUSBDataType` confirmed no physical device present at session start; only iOS Simulator and the Phase 6 Android emulator were available. All "real device" walkthrough items in this session's work order (create/edit/complete task, notification delivery under real OS conditions, real keyboard/haptics behavior) are consequently unverified on genuine hardware — flagged plainly rather than presented as equivalent to a physical-device pass. `adb shell input tap`/`input text` on the emulator did allow a live, non-scaffold walkthrough of create → complete-toggle on the real release APK, which is a stronger check than a dev-scaffold alone, but is still emulator behavior, not hardware behavior.

**[Phase 9 (device verification + store prep)] Import file picker's native OS UI itself remains unverified by a real human tap on both platforms — flagged, not worked around.**
Confirmed with the user directly (AskUserQuestion) rather than building another scaffold-based workaround: no tap-injection tool is available for driving a native document/file picker (a system-level UI, not something `adb shell input tap` coordinates can reliably target without knowing the picker's exact on-screen layout ahead of time, unlike the app's own known widget positions). The user chose to leave this flagged as still-open for a future session rather than do a manual pass this session.

**[Capsule block, post-Phase 9] Task block badge changed from a fixed circle + thin connector line to a single tall rounded pill spanning the task's full duration — confirmed against a reference image, not guessed.**
User flagged that duration wasn't visually legible with the old circle-plus-hairline-connector shape and provided a reference screenshot (Structured) where the badge itself stretches for the task's duration. Confirmed via AskUserQuestion to fully replace the connector with one pill (not just thicken the connector while keeping a separate circle badge) — the more faithful match to the reference. `TaskCapsuleBlock`'s `pillHeight` reuses the same `math.max(duration * pixelsPerMinute, badgeSize)` floor the old `connectorHeight` used, so short tasks still get a legible minimum-size shape rather than collapsing to nothing. `radiusTaskPill` (already existed, previously unused for this) provides the fully-rounded corners. The completed-checkmark overlay moved from the bottom-right of the small circle to near the top of the pill (roughly where the circle used to sit), so it stays visually anchored to the icon rather than sliding to the bottom of a now-tall shape.

`TimelineScreen`'s positioning math changed to match: the pill's top edge now *is* the scheduled-time row (previously the badge was vertically centered on it via a `-badgeSize/2` offset, appropriate when the badge was a small circle floating independent of the time it represented). Removed the `_badgeSize()` mirror-constant + offset entirely rather than updating its value, since duration-based positioning no longer needs a badge-size lookup at all.

**[Bug fix, post-Phase 9] Task writes are now resilient to notification-sync failure — `TaskList._syncNotificationSafely` wraps every `syncForTask`/`cancelForTask` call.**
User-reported bug: "tap Continue while editing, nothing happens." Root cause was architectural, not a one-off — none of `TaskList`'s five notification-syncing mutators guarded against `syncForTask` throwing, so a real native platform-channel failure (from `flutter_local_notifications`' `zonedSchedule`) could silently abort the whole save before the UI's `Navigator.pop()` ran. Confirmed with the user before fixing (AskUserQuestion: fix the architecture vs. investigate the exact native exception first) — chose "save must always succeed even if notification scheduling fails," matching the principle that a side-effect (scheduling an alert) shouldn't be able to block the primary action (saving a task) it's attached to. Failures are logged via `debugPrint`, not silently swallowed, per CLAUDE.md's "fail loud" rule — the difference between this and a genuinely silent catch is that the *caller* (the save) always succeeds, while the *failure itself* is still visible in logs for debugging. New `edit_save_repro_main.dart` dev-scaffold kept as a lasting reproduction/regression tool (mirrors `debugAutoTriggerClose`'s existing pattern with a new `debugAutoTriggerSave` flag), plus three new regression tests using a `ThrowingNotificationService` test double.

**[Tooling, post-Phase 9] Dev-scaffold entry points no longer suppress the DEBUG banner; `lib/main.dart` still does.**
All 16 `*_main.dart` scaffolds previously set `debugShowCheckedModeBanner: false`, which made a scaffold build visually indistinguishable from the real app once installed — and since every scaffold installs over the same bundle ID, a leftover scaffold silently *is* "the app" on that device until the real one is reinstalled. This directly caused a false "the nav disappeared" bug report (see `docs/ERROR_LOG.md`). Removing the suppression from scaffolds only (never from `lib/main.dart`, which should stay clean) means any scaffold build is now obvious at a glance. Cost: the DEBUG banner appears in the corner of scaffold screenshots used for visual verification — accepted deliberately, since a corner ribbon is far cheaper than repeatedly re-diagnosing which build is on screen.

**[Timeline, post-Phase 9] Drag shows live start/end times; overlapping tasks render side by side rather than pushing each other — cascade replanning stays out of scope.**
Two related asks from the same request. (1) **Drag time labels**: while a task is dragged, `TaskCapsuleBlock` shows the would-be start time above the pill and the end time (start + duration) below it, via a new `dragPreviewStartsAt` parameter. Both the label and the eventual commit read from the same `_previewStartsAt`/`_snappedMinutesDelta` getters, so the time shown mid-drag can never disagree with the time actually saved — the previous code computed the snapped delta only at drop time.

(2) **Drop onto an occupied slot**: the request was to "push another one" out of the way, but SCOPE.md line 26 explicitly defers "cascade replanning logic (automatic reflow when a task runs over)" out of the MVP, and push-on-drop *is* that feature — so per CLAUDE.md this was a stop-and-confirm, not a judgment call. Presented three options (allow-and-show, block-the-drop, full cascade with a SCOPE.md amendment); user chose **allow the overlap and show it visually**. Rationale that made this the recommendation: a partial "push one task" behaviour is arguably worse than none, because A pushing B can leave B overlapping C — either you chain-resolve (full cascade, deferred) or you silently create a different clash while appearing to have handled it. Showing the clash also matches design principle 1 (the plan is provisional, not a verdict) and keeps replanning an explicit user act rather than something the app does behind their back.

Implemented as a new pure function, `layoutOverlappingTasks` (`features/timeline/task_overlap_layout.dart`), kept widget-free so it's unit-testable: it groups tasks into overlap clusters, then packs each cluster into the fewest columns that keep genuinely-overlapping pairs apart — a task reuses the first column whose previous occupant has already finished, so 9:00-10:00 / 9:30-10:30 / 10:00-11:00 needs two columns, not three. Column count is per-group, so one busy hour doesn't narrow the whole day. 10 unit tests cover the edge cases (touching-but-not-overlapping boundaries, fully-contained tasks, column reuse, input-order independence).

Two rendering details worth recording: overlapping pills are offset by a **fixed pill-width per column**, not a fractional width — an initial `FractionallySizedBox` attempt rendered nothing differently, because the pill is a fixed-width element inside the block's `Row`, so narrowing the *available* width leaves it exactly where it was (caught on-device, not by analyze/tests). And a task in any column but the last caps its text width to one pill-width (`maxTextWidth`), so titles ellipsize instead of running underneath the neighbouring column's pill.

**[Timeline, post-Phase 9] Current time labelled in bold in the hour gutter; the hour label it would collide with is suppressed.**
Requested directly: show the current time in bold, to the left of the red now-line, with space between the label and the line's dot. Implemented in `CurrentTimeIndicator` by putting the label in a `SizedBox` matching the timeline's `_hourGutterWidth` (passed in as `gutterWidth` rather than duplicated, so it can't drift out of alignment with `HourMarkers`' labels), followed by the existing dot and line. The whole row is shifted up half its height (`FractionalTranslation`) because the row is now taller than the line itself — without that, adding the label would have pushed the line off the current minute, which is the one thing this component must get exactly right.

Screenshotting it surfaced a problem the request didn't anticipate: the bold "now" label rendered directly on top of the muted hour label whenever the current time falls near an hour boundary (14:54 sitting on 15:00). Rather than nudge one of them, `HourMarkers` gained an optional `hideLabelNear` — the hour label closest to the current time is simply not rendered, so the gutter reads as one column with "now" as its emphasised member. The collision threshold is the caption style's own rendered line height (`fontSize * height`, read from the theme), not a hardcoded pixel gap, so it scales with the type scale. `_DayTimeline` owns a one-minute ticker to keep that rule current; `CurrentTimeIndicator` keeps its own timer for its own position, since merging the two would mean gutting a component with documented history for no real gain.

**Pre-existing bug noticed while doing this, not fixed here:** `CurrentTimeIndicator` is rendered unconditionally by `_DayTimeline`, so the red now-line (and now the bold now-label) appears on past and future days too, where "now" has no meaning on that day's timeline. Flagged rather than silently fixed — it's outside what was asked for, and the right behaviour (hide entirely on non-today days vs. something else) is a real UX call.

**[Phase 10 (TrackedBehavior data layer)] `TrackedBehavior` field shape as actually implemented — `timesPerWeek` as a plain int, and a constructor-enforced target invariant.**
Built to CONSTITUTION.md's shape with two implementation choices worth recording. **Frequency** is a bare `int timesPerWeek`, not a recurrence rule — the work order and SCOPE.md both explicitly rule out a general recurrence system, and naming the field for its unit (rather than a generic `frequency`) makes the limitation self-documenting at every call site instead of inviting someone to later overload it with a richer meaning. **The "targetAmount is null only when targetType is binary" rule from CONSTITUTION.md is enforced by an assert in the constructor**, not left as prose. The alternative (trusting callers) would let an invalid object — a `duration` behavior with no target — persist to Hive and surface much later as a confusing null dereference in UI that doesn't exist yet. An assert fails loudly at the moment of construction and costs nothing in release builds; it's covered by a test that asserts the rejection, so the invariant can't silently rot. `BehaviorTargetType` is its own file/typeId (4) mirroring how `TaskStatus`/`TaskCategory` are already split out, rather than nested in the model file.

**[Phase 10] Feature flag mechanism: a plain `const` in `lib/core/feature_flags.dart`, default off.**
The work order left the mechanism to my judgment and explicitly said a bool in `core/` is enough for MVP. Chose an `abstract final class FeatureFlags` with `static const bool trackedBehaviorEnabled = false` over the alternatives (a Riverpod provider, a Hive-persisted setting, remote config): nothing about this flag needs to change at runtime or per-user, a `const` is trivially greppable to find every gated entry point later, and it dead-code-eliminates cleanly. Deliberately **nothing reads it today** — no UI exists to gate, which is the intended state until Phase 11+. Documented in the file itself that the flag gates *UI entry points only*; the data layer is never "turned off," only dormant, per CONSTITUTION.md.

**[Phase 10] `Task.behaviorId`/`actualAmount` are included in `toJson` but treated as optional in `fromJson` — backward compatibility with pre-Phase-10 backup files.**
The two new fields are written on export (otherwise a backup would silently drop a task's behavior link), but read with a plain nullable cast rather than the explicit presence/type validation the required fields get. This is deliberate: every backup file exported before this session simply has no `behaviorId`/`actualAmount` keys, and those files must still import cleanly — validating them as required would have broken import for every existing user backup. A *wrong-typed* value still fails loudly via the cast, matching how `notes`/`durationMinutes` and the other optional fields already behave. Covered by a test that strips both keys from an exported map and asserts the import still succeeds.

**[Phase 10] Two Hive field numbers (10, 11) appended to `Task`; no existing field numbers touched.**
Hive adapters key on field number, so appending is the only safe way to extend a model that already has persisted data on real devices — renumbering or reusing a retired number would silently misread existing records. Both new fields are nullable with a null default and are declared *after* every existing parameter, so `Task.create` and `Task.captured` are untouched at every one of their call sites. Verified by running the full pre-existing suite (59 tests, unmodified) immediately after regenerating the adapter, before writing any new tests — it passed identically to the pre-change baseline, which is the specific check the work order called the real risk of the session.

**[Phase 11 (recurring tasks)] Generation runs at app launch only, not on Timeline load — confirmed with the user, not guessed.**
CONSTITUTION.md says instances are materialized "whenever the app opens or a relevant screen loads," which leaves the actual trigger open; the work order explicitly called this a real design call. Presented launch-only vs. launch+Timeline via AskUserQuestion; user chose **launch-only**. Rationale: generation writes real Hive rows, and the Timeline is currently a pure reader — adding a write path there would mean every day-swipe could trigger writes. With an 8-week window, a session would have to stay open for weeks before the window ran dry, which does not happen in practice. `main.dart` calls `taskListProvider.notifier.materializeDueRecurrences()` once after the boxes open and notifications initialize. Creating a recurring task *also* materializes its series immediately (inside `createTask`), so a new series appears on the Timeline right away rather than only after the next launch — that's a separate, deliberate call, not the launch trigger firing early.

**[Phase 11] `recurrenceWindowWeeks = 8`, a named constant in the generator, not a magic number.**
Per the work order. Eight weeks is far enough that swiping forward never hits the window edge in normal use, and small enough that an open-ended daily series costs ~56 rows rather than the unbounded set generating-to-`endDate` would produce for a rule with no end. The generator takes `windowWeeks` as a parameter defaulting to this constant, which is what lets the tests exercise small deterministic windows without touching production behaviour.

**[Phase 11] Duplicate prevention keys on the occurrence slot — `originalScheduledAt ?? scheduledAt` — not on current position. Corrected after seeing the wrong behaviour on-device.**
`generateRecurrenceInstances` collects the claimed slot of every already-persisted instance in the series and skips any computed occurrence matching one. Instant-based rather than date-based, since a weekly rule with several `daysOfWeek` can legitimately put two instances on one day.

The first implementation keyed on `scheduledAt` alone, and I documented the consequence as an accepted trade-off: moving an instance frees its original slot, which the next generation run refills. **On-device verification showed that reasoning was wrong.** The edit-mode screenshot showed two "Morning run" entries on the same day — the user's rescheduled 08:30 one *and* a regenerated 07:00 duplicate. A user who reschedules one occurrence has not asked for a replacement to appear at the old time; that reads as the app undoing their edit. Fixed by preferring `originalScheduledAt`, which `TaskList.rescheduleTask` already preserves for exactly this "where did this originally belong" purpose (CONSTITUTION.md), falling back to `scheduledAt` for instances never moved. The regression test now asserts the moved-instance case generates **nothing**. Worth recording that the unit tests passed against the wrong behaviour too — they encoded my incorrect assumption, and only a real screenshot exposed it.

**[Phase 11] The series id is a fresh UUID, and only the template carries the rule.**
`createTask` mints a new UUID for `recurrenceId` when a rule is supplied. Every materialized instance carries that same `recurrenceId` with a **null** `recurrenceRule`, per CONSTITUTION.md — so "which task owns the rule" has exactly one answer (`isRecurrenceTemplate`), and `materializeDueRecurrences` finds series by scanning for templates rather than needing a separate index. Generation itself never persists: `generateRecurrenceInstances` is a pure function returning new `Task`s, and `TaskList` writes them through `TaskRepository` like every other mutation, so the "all writes go through the provider" rule holds.

**[Phase 11] Date arithmetic rebuilds from calendar parts rather than adding `Duration(days:)`.**
`DateTime.add(Duration(days: 1))` adds 24 hours of absolute time, which shifts the wall-clock hour by one across a DST boundary — a 09:00 daily task would silently become 08:00 or 10:00 for half the year. `_addDaysPreservingTime` constructs `DateTime(y, m, d + n, hour, minute, second)` instead, so the local time of day is preserved. Not hypothetical for a task app whose entire value is showing the right time.

**[Phase 11] Recurrence UI: an inline "Repeats" panel in the create form, collapsed by default and shown only when creating.**
Confirmed via AskUserQuestion (inline vs. a separate sheet); user chose inline. Off by default means an ordinary task sees one unobtrusive row and gains no friction (design principle 2); switching it on reveals Daily/Weekly chips and an interval stepper in place. Deliberately **not rendered in the edit flow** (`if (widget.task == null)`): MVP edits affect a single instance only, so there is no series-level rule to edit from there, and showing a control that silently wouldn't apply would be worse than omitting it. `daysOfWeek` and `endDate` exist in the model and are fully supported by the generator and its tests, but are not yet exposed in the UI — flagged as the obvious next increment rather than built speculatively.

**[Phase 11] New adaptive `AppSwitch` in `core/widgets/`.**
The Repeats toggle needed a switch, and design principle 4 forbids raw `Switch`/`CupertinoSwitch` in feature code. Unlike `AppSlider` (custom-painted because the design needed a track neither platform widget could produce), a switch has no such requirement, so `AppSwitch` wraps the real platform controls and keeps their native feel, theming only the active colour from Tier 2 tokens.

**[Phase 11] Recurrence indicator: a muted `repeat` icon beside the time, matching the existing "moved" indicator exactly.**
Kept minimal per the work order, and given the same `colorTextSecondary` treatment as the rescheduled indicator rather than a colour of its own — being part of a series is a neutral fact about a task, not a status worth colouring (design principle 1). Shown for every instance including the template, since from the user's point of view they're all just occurrences. Uses only existing Tier 2 tokens; no new tokens were needed.

**[Phase 11] Editing/deleting one instance needed no special-casing — confirmed rather than assumed.**
The work order asked me to confirm this was naturally true. It is: a materialized instance is an ordinary `Task` row, and the detail form's edit path mutates only its own `existing` object before calling `updateTask`, while `deleteTask` removes a single id. No sibling is reachable from those paths, so "this one only" falls out of the existing design rather than needing a guard. Verified on-device by editing one instance and screenshotting the surrounding days.

**[Phase 12 (cross-platform verification)] The import file picker IS drivable on Android via `uiautomator dump` — the "no tap tool" assumption was only ever true for iOS.**
Every prior session recorded the native file picker as unverifiable by an agent on *both* platforms (see the Phase 9 entry above). That was half wrong, and this session proved it by doing the thing. `adb shell uiautomator dump` returns the full on-screen view hierarchy **including other apps' UI** — so the picker's file rows can be located by their real `bounds` and tapped by computed coordinates, rather than guessed at. The full flow was driven end to end: tap the real "Import backup" button in the real app → `dumpsys activity` confirms the foreground activity becomes `com.google.android.documentsui/...PickActivity` (a genuinely separate process, not Amble's widget tree) → navigate the drawer to Downloads → tap the target file → control returns to `com.example.amble/.MainActivity` → the app reports "Imported 1 task(s)" and the persisted Hive box contains the exact title from the selected file.

The correction worth recording is methodological: the earlier conclusion generalised an iOS limitation to Android without testing Android separately. "No tap-injection tool" was true of iOS Simulator and assumed of the Android picker because the picker is out-of-process — but out-of-process only defeats *coordinate guessing*, not *coordinate discovery*. Prefer checking whether the platform exposes a query mechanism before recording something as unverifiable.

**iOS remains genuinely blocked**, and for a stronger reason than "the picker is a separate app": iOS Simulator has **no tap capability at all**. `xcrun simctl` has no tap/touch/input subcommand (`simctl ui` only gets/sets appearance), and `idb`/`idb_companion` are not installed. Reaching the Import button at all — never mind the picker — is impossible without either a human or installing `idb`, which would be a new system-level dependency and is therefore flagged rather than added.

**[Phase 12] Verification scaffold times are now anchored to the current hour rather than hardcoded to 07:00.**
`recurrence_verification_main.dart` seeded its tasks at a fixed 07:00/11:00. The Timeline auto-scrolls to "now" on open, so on any run after ~08:00 those tasks sat above the fold — invisible in a screenshot, and un-scrollable on iOS Simulator, which has no scroll gesture. Anchoring the seeds to `now.hour + 1` / `now.hour + 2` keeps both tasks in view on any platform at any time of day. This is a verification-tooling fix, not a product change: it makes the same scaffold usable on iOS, where the Android workaround (`adb shell input swipe`) has no equivalent.

**[Phase 12 (TrackedBehavior first UI slice)] The feature flag became a build-time override rather than being flipped to `true` — confirmed with the user.**
The work order left the mechanism open. Presented "build-time override, default stays off" vs. "flip the const to true"; user chose the override. `trackedBehaviorEnabled` is now `bool.fromEnvironment('trackedBehavior')`, which defaults to `false` when the define is absent — so an ordinary build (including any release build made from this commit) behaves exactly as it did before this UI existed, and SCOPE.md's "default off" stays honest without needing a code edit to keep it that way. Testing uses `--dart-define=trackedBehavior=true`. Because `bool.fromEnvironment` is const-evaluated, every `if (FeatureFlags.trackedBehaviorEnabled)` subtree still dead-code-eliminates in builds that don't opt in — the gate costs nothing at runtime.

**[Phase 12] The create-behavior form uses `AppSheet`, not Phase 4's near-full-screen modal.**
Phase 4 built the full-screen `PageRouteBuilder` pattern specifically for the task detail screen's colored edge-to-edge header, wheel time picker, and duration slider — none of which this form has. It's five short fields (title, type, target, minimum, times-per-week), so a full-screen takeover would be disproportionate and would imply more ceremony than the interaction deserves. `AppSheet` already handles the platform branching and the Material-ancestor fix from Phase 4, so this reuses a solved component rather than a solved *layout*.

**[Phase 12] Linking is available on both create and edit; recurrence remains create-only.**
These look similar but aren't. A recurrence rule defines a *series*, so editing it from one materialized instance raises the "this one or all future?" question that SCOPE.md explicitly defers — hence create-only. A behavior link is a property of *this single task* and nothing else, so changing it later is unambiguous and affects no sibling. The picker is therefore shown in both flows. Unlinking clears `actualAmount` in the same write: an amount measured against a behavior the task no longer belongs to would be orphaned data.

**[Phase 12] The outcome prompt is skipped for binary behaviors, on un-completion, and for dangling links — ordinary tasks never reach it at all.**
`_completeTask` in `timeline_screen.dart` guards in order: flag off → un-completing → no `behaviorId` → all take the original single-argument `toggleComplete(task)` path unchanged. Only a task that is being *completed*, is linked, and whose behavior is non-binary sees a prompt. Binary is excluded deliberately: "did it happen" is already answered by the completion itself, so asking would be friction with nothing to gain (design principle 2). A dangling link (behavior since deleted) also completes normally rather than blocking on a lookup that can't succeed. Dismissing the prompt still completes the task with `actualAmount` left null — declining to quantify is not declining to finish.

**[Phase 12] `toggleComplete` gained an optional `actualAmount`; un-completing clears it.**
The parameter is optional and omitted entirely by ordinary call sites, so ordinary completion is the same operation it always was — proven by the full pre-existing suite passing unmodified. Un-completing sets `actualAmount = null` because the amount described a completion that no longer stands; leaving it would misreport history the moment the task is completed again with a different result.

**[Phase 12] Recurring instances inherit `behaviorId` but never `actualAmount`.**
CONSTITUTION.md says the two systems compose without special-casing, so a series linked to a behavior means every occurrence is an instance of that behavior — the generator now copies the link onto each materialized instance. `actualAmount` is deliberately *not* copied: each occurrence records its own outcome, and inheriting one would fabricate evidence for days that haven't happened yet.

**[Phase 12] Indicator: a muted `track_changes` icon beside the time, matching the recurring indicator exactly.**
Same `colorTextSecondary` treatment as the recurring and "moved" indicators — being tracked is a neutral fact about a task, not a status worth colouring (design principle 1). A task can show both this and the repeat icon simultaneously, which is correct: the two systems are independent. Uses existing Tier 2 tokens only; no new tokens were needed.

**[Phase 13a (dark mode)] Preferences are a generic key-value store, not a theme-mode box.**
The work order called for this explicitly and it's the right shape: theme mode is the *first* preference to need persisting, not a special one. `PreferencesRepository` mirrors `TaskRepository`/`TrackedBehaviorRepository`'s interface style over a single untyped `preferences` box, with keys owned by `PreferenceKeys` so a read and a write can't diverge on a string literal. Later settings (default duration, week start, notification lead time) land here rather than each growing a box, adapter, and repository. `getValue<T>` returns null when the stored value isn't a `T` rather than throwing — a preference whose type changes between releases must degrade to its default, not make the app unlaunchable over a cosmetic setting.

**[Phase 13a] Theme mode is persisted as our own `AppThemeMode` enum (typeId 7), not Flutter's `ThemeMode`.**
Persisting a framework enum would tie the stored schema to Flutter's declaration order — a reordering upstream would silently reinterpret every stored value. `toFlutterThemeMode` in `preferences_providers.dart` is the single translation point. Provider is `keepAlive: true` per the Phase 1 precedent, and more strongly here than usual: the root `MaterialApp` is the only listener, so a screen-scoped provider would be wrong by construction. Defaults to `system` — a fresh install follows the OS rather than picking a side.

**[Phase 13a] Dark surfaces are a new `ink` OKLCH ramp at hue 90, not an inversion of the light palette.**
Derived the same way every other ramp here was (deliberate OKLCH triples — see the Pre-Phase 3 entry), not auto-darkened. Hue stays at 90, matching the `sand` neutral, so dark mode reads as the same product in low light rather than a generic grey theme. Two details that are deliberate rather than arbitrary: chroma is tiny (0.008–0.012) and *rises* with lightness, because at very low L a neutral needs almost no chroma to read warm and more makes surfaces look muddy; and the lightness steps are uneven (0.185 → 0.225 → 0.285 → 0.360) because perceived separation compresses near black, so equal steps would make the two darkest surfaces indistinguishable on a real screen. Two new `sand` entries (`sand200`/`sand400`) carry dark-mode text at a measured 15.6:1 and 7.5:1 against `ink900` — `sand50` blooms at that contrast and `sand500` doesn't clear comfortable secondary text.

**[Phase 13a] Category colors are unchanged for dark mode — measured, not assumed.**
Checked before deciding: at their current L=0.62/C=0.10 they land 4.9–5.2:1 against `ink900`, and the four hues stay as separable on dark as on light. Adding a dark-specific category ramp would have meant maintaining two sets of accent values for no measured gain, and would have edged toward the light-mode changes this session's non-goals rule out. Revisit in 13b only if they read as too saturated *in situ* rather than on paper.

**[Phase 13a] Material `ThemeData` seed and scaffold colors now derive from the palette instead of hardcoded hex.**
`main.dart` previously carried literal `0xFF5B6F52` and `0xFFF7F5F0` values — a no-magic-values violation, and a real hazard for dark mode specifically: the Material scaffold underneath our own tokens could disagree with them, leaving a light background peeking through a dark theme. `_themeDataFor(palette, brightness)` now reads `colorAccent`/`colorSurfacePrimary` off the palette, so a palette change propagates automatically and the two can't drift.

**[Phase 13a] System bar styling needs an explicit root `AnnotatedRegion` — no platform-specific code required.**
Worth recording because it's a non-obvious dependency: the app has no `AppBar` anywhere, and an `AppBar` is what normally manages status-bar icon brightness. Without this, dark mode renders dark status-bar icons on a near-black surface — invisible. Resolved against the *effective* brightness (`MediaQuery.platformBrightnessOf` for `system` mode), not the stored setting, so system-following works. One declaration covers both platforms: `statusBarBrightness` is iOS-only, `statusBarIconBrightness`/`systemNavigationBar*` are Android-only, and Flutter ignores the irrelevant ones per platform rather than erroring. Note the naming trap — `statusBarIconBrightness` is the *icon* colour while `statusBarBrightness` is the *background* it sits on, so a dark surface wants `Brightness.light` for one and `Brightness.dark` for the other.

**[Timeline, post-Phase 13a] Drag "lift" state: large shadow + slight scale + full opacity, replacing the previous opacity fade.**
Requested directly ("lift state on drag and drop, visually large drop shadow"). Confirmed the treatment via AskUserQuestion rather than guessing, since it replaces existing behaviour: the block previously faded to 0.75 opacity while dragging, which read as *receding* — the opposite of being picked up. It now stays fully opaque, scales to 1.04, and casts `shadowLift`.

The scale is deliberately small. A bigger lift reads more dramatically but makes the drop target ambiguous: the block stops matching the size of the slot it's about to land in, so the user loses the visual cue about where it will actually sit. 1.04 plus a large shadow carries the "raised" reading without that cost. Animated via `AnimatedScale` on the existing `motionFast`/`curveStandard` tokens so both the lift and the settle are visible rather than snapping.

**[Timeline, post-Phase 13a] `shadowLift` is a real Tier 2 token with a different value per palette, not an inline `BoxShadow`.**
This is the app's *second* shadow (the task detail sheet's sticky Continue button is the first), so per design principle 5 — a value used twice becomes a token — it gets promoted before a third use appears rather than after. Threading it through `AmbleTheme` means the constructor, both palettes, `copyWith`, **and** `lerp`; missing either of the last two would make the token silently revert to the other palette's value mid theme-animation, which is a bug that only appears during a transition and is very hard to spot by eye. Covered by tests for exactly that.

The per-palette split was confirmed with the user rather than assumed. Light uses `slate900` at 22% alpha; dark uses **pure black at 55%**. This isn't a tweak for taste — depth on a near-black surface has to come from something *darker than the background*, and the light shadow (tinted with the light-mode ink colour) is close to invisible against `ink900`. Both use the same geometry (`space9` blur, `space4` y-offset), which is deliberately larger than the Continue button's `spacingMd`/`spacingXs` so "lifted" is clearly distinguishable from the app's existing resting elevation — asserted in a test rather than left to drift.

**[Phase 13a-pastel] Light-mode palette shifted toward a softer, pastel character — Tier 1 only, dark mode untouched.**
Requested directly: lower chroma, higher lightness, gentler than the existing saturated coral/sage/slate/berry set, in the character of soft peach/mint/blue consumer-app palettes. Scoped as a checkpoint session — re-derive Tier 1 primitives, re-verify contrast and category distinguishability, stop for visual review before Phase 13b (dark mode) resumes. Only `color_primitives.dart` changed; `semantic_theme.dart` needed no edits, confirming Tier 2's job (meaning-bound references to Tier 1) held exactly as designed — a palette swap propagates without touching the semantic layer.

**Category accents (the actual pastel target)** — full before/after:

| Token | Old (L, C, H) | New (L, C, H) | Old hex | New hex |
|---|---|---|---|---|
| `clay500` (health) | 0.62, 0.10, 30 | 0.78, 0.090, 35 | `#BB6E62` | `#EBA390` |
| `ochre500` (work) | 0.62, 0.10, 120 | 0.78, 0.095, 160 | `#7E8F46` | `#7FCAA3` |
| `periwinkle500` (personal) | 0.62, 0.10, 250 | 0.78, 0.075, 240 | `#558AC0` | `#8BBEE3` |
| `berry500` (admin) | 0.62, 0.10, 340 | 0.78, 0.090, 345 | `#AE6E99` | `#E2A0C5` |

Hues nudged (not held fixed) specifically to recover perceptual separation that lowering chroma uniformly compresses — see the distinguishability entry below for why. Minimum pairwise hue gap held at 55° (health vs admin), the same floor Phase 3's category-color fix established.

**Sage (brand/accent ramp)** — only the two values with real consumers moved; the rest were left alone rather than adjusted on principle:

| Token | Old (L, C, H) | New (L, C, H) | Consumer | Note |
|---|---|---|---|---|
| `sage50` | 0.967, 0.007, 124 | 0.975, 0.006, 138 | none | nudged for ramp consistency only, zero risk |
| `sage100` | 0.927, 0.014, 129 | **unchanged** | `colorSurfaceSecondary` (light) | already low-chroma; no payoff from softening further |
| `sage300` | 0.787, 0.046, 132 | **unchanged** | `colorAccent`/`colorTaskCompleted` (**dark mode**, Phase 13a) | left alone deliberately — dark mode is explicitly out of scope this session, and `sage300` is a shared primitive, so moving it would have been an unrequested dark-mode side effect |
| `sage500` | 0.517, 0.051, 136 | 0.550, 0.060, 140 | `colorAccent`/`colorTaskCompleted` (light) — buttons, FAB, switches, sliders, selected chips | the ramp's functional anchor; tuned to hold WCAG AA, not picked by eye (see contrast table) |
| `sage700` | 0.404, 0.039, 137 | 0.430, 0.050, 138 | none | nudged for ramp consistency only, zero risk |
| `sage900` | 0.289, 0.024, 135 | **unchanged** | none | no consumer, no reason to touch |

`coral*` (the alert ramp — destructive dialog action, Settings danger item, current-time indicator) was deliberately **not touched**. It's a functional/status color, not a category swatch, and the brief's pastel direction describes the category palette; softening an alert color works against the one thing it has to do (register as urgent at a glance). Not an oversight — a scope boundary.

**Contrast re-measurement, WCAG AA (4.5:1 text, 3:1 graphics) as the bar:**

| Pair | Ratio | Verdict |
|---|---|---|
| `sage500` white-glyph (button/FAB text-on-fill) | 4.76:1 | pass |
| `sage500` vs `sand50` (accent-on-background) | 4.56:1 | pass |
| `slate900`/`slate500` body text vs `sand50` | unchanged from Phase 4 baseline | pass (not touched) |
| Category accent vs `sand50` (pill-on-timeline-canvas) | 1.83–2.00:1 | **not a text-contrast context — the pill is a color field, not text on a background; this ratio was never the relevant check** |
| Category accent, white glyph (icon-on-pill, as originally coded) | 1.81–2.09:1 | **fail** — well under the 3:1 graphics floor at the new L≈0.78 |
| Category accent, `slate900` glyph (icon-on-pill, as fixed) | 8.45–9.18:1 | pass |

The white-glyph failure is the session's one real, required companion change, not scope creep: at pastel lightness a white icon on any of the four category pills drops to ~1.8–2.1:1, which fails outright. `TaskCapsuleBlock` (`lib/features/timeline/task_capsule_block.dart`) now computes `iconColor` per-badge — `colorTextPrimary` (dark) only when the badge is a plain category color and reads as light per `ThemeData.estimateBrightnessForColor`; `colorTaskSkipped`/`colorTaskCompleted` badges keep the original `colorSurfacePrimary` (white), since those stay dark enough for white to hold in both palettes. This was a deliberately minimal, non-token fix: a new Tier 2 token (e.g. `colorTaskCategoryIcon`) was considered and rejected for this session specifically because the work order said Tier 2 should need no changes, and a widget-local per-badge color pick achieves the same correctness without touching the token schema. Revisit as a real token if dark mode (13b) ever needs the same logic — right now dark mode's glyph is untouched and still reads correctly for the reason already on record (Phase 13a: `ink900` badge backgrounds keep the white glyph legible there).

**Category distinguishability re-verified via OKLab deltaE, not assumed safe.** Lowering chroma uniformly (the naive version of "more pastel") compresses perceptual separation even with hues held fixed — confirmed by computing pairwise OKLab distance before and after. A first pastel draft (L=0.78, C=0.075–0.095, hues unchanged at 30/120/250/340) scored *worse* than the old palette on 5 of 6 pairs. Nudging hues to 35/160/240/345 (v3) recovered separation: 5 of 6 pairs meet or beat the old palette's plain OKLab deltaE, and the closest pair under a red-green-deficiency proxy (health vs work, both damped 65% on the `a`-axis) lands at exactly the old palette's worst-pair score (0.060) rather than below it — the CVD-relevant floor Phase 3 originally fixed on is preserved, not just the raw hue-angle gap. Verified with a throwaway Python script mirroring the existing hand-written OKLCH→sRGB conversion (no new Dart dependency, no product code) — not shipped, not referenced by the app.

**Not done this session, flagged for 13b:** dark-mode category/sage values are untouched, so `sage300` (dark accent) and the category ramp now diverge in "character" between light (soft pastel) and dark (still the old saturated set) until 13b addresses it deliberately. This is expected, not a bug — dark mode depends on this palette landing first, per the work order's own sequencing.

**[Phase 13a-pastel-v2] Category palette re-derived a second time against a user-supplied reference ("8 Soft Pastel Colors": Peach/Mint/Sky/Lavender/Lemon/Blush/Aqua/Sand) — supersedes the mid-tone pastel above.**
The first pastel pass (L=0.78, C=0.075–0.095) was a genuinely gentler mid-tone accent, but the user's reference sits at a different point entirely — L≈0.95–0.99, C≈0.02–0.08, a true pale tint closer to `sand50`/`sage100` than any mid-tone swatch. Confirmed via AskUserQuestion rather than guessed which of two structural approaches to take, since neither light nor dark glyph clears real contrast at that lightness on a small pill: chose **pale tint fill + a separate, saturated same-hue icon glyph** (rejected: deriving a second darker set just for the icon pill while keeping the pale set elsewhere; rejected: matching the reference literally and accepting sub-3:1 icon contrast).

**Category → reference mapping**, confirmed via AskUserQuestion: Peach→health, Mint→work, Sky→personal, Lavender→admin (the other four reference swatches — Lemon/Blush/Aqua/Sand — aren't part of Amble's four-category taxonomy and weren't used).

**Two colors per category now, not one** — `AmbleTheme` gained a second map, `categoryIconColors`, alongside the existing `categoryColors` (now holding the pale *tint*, not a mid-tone). Threaded through the constructor, both palettes, `copyWith`, and `lerp`, per the same pattern `shadowLift` established. `TaskCapsuleBlock`'s icon color now reads `categoryIconColors` directly instead of the brightness-estimation heuristic from the first pastel pass — a real per-category design choice now exists, so inferring one is no longer necessary.

**Full before/after** (v1 = first pastel pass above, v2 = this entry; hex values matched to the reference's stated hex through this file's own `oklch()` pipeline, verified within 1–3/255 per channel rather than hand-copied):

| Category | Pill fill (v1 mid-tone) | Pill fill (v2, pale tint) | Icon glyph (v2) |
|---|---|---|---|
| health | `clay500` 0.78, 0.090, 35 → `#EBA390` | `clayTint` 0.975, 0.075, 36 → `#FFE6D4` | `clay500` (repurposed) 0.540, 0.150, 32 → `#B54531` |
| work | `ochre500` 0.78, 0.095, 160 → `#7FCAA3` | `ochreTint` 0.956, 0.060, 153 → `#D3FDDE` | `ochre500` (repurposed) 0.550, 0.110, 153 → `#358452` |
| personal | `periwinkle500` 0.78, 0.075, 240 → `#8BBEE3` | `periwinkleTint` 0.956, 0.055, 218 → `#C8FAFF` | `periwinkle500` (repurposed) 0.550, 0.130, 216 → `#0082A1` |
| admin | `berry500` 0.78, 0.090, 345 → `#E2A0C5` | `berryTint` 0.946, 0.066, 300 → `#F4E4FF` | `berry500` (repurposed) 0.550, 0.140, 300 → `#7F5BB6` |

**The primitive names `clay500`/`ochre500`/`periwinkle500`/`berry500` were repurposed to mean the icon glyph, not the pill fill** — a real naming risk, addressed directly rather than left implicit: dark mode's `categoryColors` map referenced these same four names for its pill fill (Phase 13a's measured 4.9–5.2:1-against-`ink900` mid-tone values), and repurposing them in place would have silently changed dark mode's rendering — an out-of-scope side effect through a name collision, not a value change anyone asked for. Fixed by preserving the original mid-tone numbers under new, dark-exclusive names (`clay500Dark`/`ochre500Dark`/`periwinkle500Dark`/`berry500Dark`, unchanged from Phase 13a) and repointing dark's `categoryColors`/`categoryIconColors` (both, identically — dark mode never had a separate icon treatment) at those. Dark mode's rendering is byte-for-byte unchanged by this session; only the light-mode pill/icon split moved.

**Contrast, per category, icon-glyph vs. its own pale pill (WCAG: 4.5:1 text, 3:1 graphics)**:

| Category | Icon vs. own pill | Verdict |
|---|---|---|
| health | 4.54:1 | pass (clears text-grade AA) |
| work | 4.13:1 | pass (clears text-grade AA) |
| personal | 3.95:1 | pass (clears graphics-grade AA with margin; just under text-grade) |
| admin | 4.27:1 | pass (clears text-grade AA) |

**Distinguishability re-verified again, and a real regression was caught and fixed before shipping.** The reference's Sky (H≈234) and Lavender (H≈292) swatches sit close enough in hue that a first v2 draft (hues held at the reference's raw values) scored a personal-vs-admin icon-level CVD deltaE of 0.051 — *below* the pre-pastel palette's worst-pair floor of 0.060, i.e. a genuine step backward on the exact axis Phase 3 originally fixed. Caught by re-running the same OKLab-deltaE check used in the first pastel pass, not assumed safe because the source was "a nice-looking reference." Fixed by widening personal/admin apart (218°/300° instead of the reference's 234°/292°), which brought the worst pair to 0.076 — clearing the old floor with margin. The icon-level deltaE table (not the pill-level one) is the one that matters here, per this session's own design decision that the saturated icon — not the pale tint — is what actually carries the distinguishing signal; pill-level separation is looser by design (the reference's own swatches are close together at the tint level) and was accepted as a known, deliberate tradeoff rather than re-optimized.

Verified via the same throwaway, unshipped Python script as the first pastel pass, `dart format .`/`flutter analyze`/`flutter test` clean at 118/118 (one golden intentionally regenerated a second time), and real Android emulator screenshots of Timeline (all four categories together), the task-detail category picker, and Settings — all re-captured against this v2 palette.

**[Timeline, drag ghost] Ghost-rendering state moved from `_DraggableTaskBlock` to its parent `_DayTimelineState` — a real gesture bug, not a style preference.**
The first implementation had `_DraggableTaskBlock`'s `build()` return `Positioned.fill(child: Stack(...))` so a single widget could inject two `Positioned` entries (ghost + live block) into the ambient day-timeline Stack. This passed every check available short of a live drag: `flutter analyze` clean, full test suite clean, and a temporary magenta debug-marker widget confirmed the ghost's positioning math was correct. It still shipped a real bug, reported by the user: the first drag attempt on a fresh install would visually "arm" (shadow, scale, and preview time labels all appeared, proving `onVerticalDragStart` fired) but the block would not move, and only a second drag on the same task actually worked.

Root cause: `Positioned.fill` sizes its child to the *entire* ambient Stack's bounds, not to the block's own content — so every dragged task's wrapper had a hit-testable footprint the height of the whole day column while dragging, not just its own badge. The added Stack nesting (one more layer than the pre-ghost structure) combined with that inflated footprint disturbed gesture-arena arbitration on the first pointer-down after launch closely enough that the drag recognizer lost, evidenced by `onVerticalDragStart` firing while `onVerticalDragUpdate` never delivered real deltas.

Fixed by lifting "which task is currently dragging" to a new `_draggingTaskId` field on `_DayTimelineState`, and having the parent's existing `for (final slot in layoutOverlappingTasks(tasks))` loop emit the ghost as a genuine sibling `Positioned` — conditional on `_draggingTaskId == slot.task.id`, computed from the same `_minutesSinceStart`/column-offset math the live block already used — rather than nesting it inside the dragged widget's own Stack. `_DraggableTaskBlock` reports drag start/end via a new `onDraggingChanged` callback and is otherwise back to its exact pre-ghost shape: a single `Positioned` root, same nesting depth as before this feature existed.

Re-verified against the specific failure mode, not just re-derived structurally: force-stopped and relaunched the app, then dragged a task on the very first attempt post-launch — confirmed via a real `adb input draganddrop` mid-drag capture showing both the live preview labels and a visibly separate faded original-position element, with the drop landing correctly and no stuck lift/shadow state surviving either the drop or a subsequent app restart. Worth recording as a methodology note: "passes analyze, tests, and a structural debug check" is not the same bar as "works on a real interaction" — the gap here was gesture-arena behavior, which none of those three exercise.

**[Timeline] Day view widened from a fixed 6am-10pm window to the full 24-hour day — `_startHour`/`_endHour` constants, not a scroll fix.**
Reported directly: tasks scheduled before 6am or after 10pm were unreachable — not a broken scroll gesture, the scrollable content simply never extended that far, since `_startHour = 6`/`_endHour = 22` sized the whole day's rendered height. Widened to `_startHour = 0`/`_endHour = 24`. `_endHour = 24` is deliberately one past the last valid `TimeOfDay` hour (0-23) — `HourMarkers`' label loop now caps at `min(endHour, 23)` so it never constructs an invalid `TimeOfDay(hour: 24, ...)`, while the `SizedBox` height calculation still uses the full `24 - 0` span so the last hour's worth of vertical space (and anything scheduled in it) actually renders. Same convention a real 24-hour clock uses — a "00:00" tick, no separate "24:00" one.

A larger redesign was requested alongside this — moving from a fixed hourly grid to a dynamic, task-driven scale with per-task start/end labels (see a provided reference screenshot) — deliberately deferred to its own session rather than bundled in here. That's a real change to the timeline's visual model, and CONSTITUTION.md already names the capsule timeline as the one component worth dedicated design-review time rather than folding into an unrelated bug fix; confirmed via AskUserQuestion rather than assumed.

Verified on-device past the code fix itself: scrolled to the genuine top of a real day and confirmed 12:00 AM renders with a task at 4:00 AM (previously unreachable) visible; scrolled to the genuine bottom and confirmed it reaches past 11:00 PM to the actual end of the day. `dart format .`/`flutter analyze`/`flutter test` clean, 118/118 — no test file needed changes, since this scenario had no prior coverage either way.

**[Timeline] Fixed hourly grid replaced with task-boundary labels — linear scale kept, two scope decisions confirmed rather than assumed.**
The deferred redesign from the entry above. Two AskUserQuestion decisions set the actual scope: **(1) linear vs. non-linear scale.** The reference image reads as visually compressing empty gaps between tasks; chose to keep the scale strictly linear/proportional (1 real minute = fixed pixels, unchanged) and only swap which labels render — the non-linear alternative would have touched drag-to-reschedule's pixel-to-minute math, the current-time indicator's positioning, and overlap layout, none of which were in scope for a labels-only ask. **(2) gutter content.** `TaskCapsuleBlock` already shows each task's own start time on the card itself, so gutter labels are a distinct, separate annotation — chose task start/end times only, with generic hourly ticks removed entirely (not falling back to them in gaps), matching the reference exactly. Accepted tradeoff: a busy day's empty stretches now show zero time reference. A fully empty day is unaffected — it already renders `_EmptyDayState` instead of the day view at all.

**New pure function `taskBoundaryTimes`** (`task_boundary_markers.dart`) collects every task's start/end `DateTime`, de-duplicates via a `Map<DateTime, TaskBoundary>` keyed on the exact timestamp, sorts chronologically. De-duplication was itself a confirmed decision (AskUserQuestion): a task starting exactly when another ends shares one label rather than rendering the same time twice — matches the reference, avoids visual clutter. This also transparently handles overlapping (side-by-side-column) tasks that happen to share a boundary time, and a zero-duration task (start == end), since de-dup only cares about the `DateTime` value, not which task or column produced it — no separate overlap-aware logic needed.

**`HourMarkers` deleted outright**, not deprecated or left unused — it had exactly one real caller (`timeline_screen.dart`), and every other reference was a doc comment, updated in `current_time_indicator.dart` rather than left dangling on a retired class. `TaskBoundaryMarkers` ports the same collision-avoidance rule the old grid used (a label near the bold current-time label is hidden, threshold from the caption text's own line height) — re-keyed on `DateTime.difference` since boundaries are now full timestamps, not bare hour integers.

6 new unit tests for `taskBoundaryTimes`. Verified on-device: scrolled a real day from a task at 12:05 AM through a genuinely empty stretch to one at 7:15 AM with zero grid lines rendered in between — confirms the "gaps show nothing" tradeoff renders as intended, not just as a spec. `dart format .`/`flutter analyze`/`flutter test` clean, 124/124 (118 + 6 new).

**[Splash/carousel] `hasSeenSplash` lives in the existing `PreferencesRepository` (Phase 13a), not a new box or entity — as explicitly scoped.**
`PreferenceKeys.hasSeenSplash`, a plain `bool`, plus `HasSeenSplash` — a `keepAlive: true` Riverpod notifier mirroring `ThemeModeSetting`'s exact shape (`build()` defaults to `false` when unset, `markSeen()` persists `true`). `main.dart`'s root `MaterialApp` **watches** this provider (not `read`s it once at startup), so `SplashCarouselScreen`'s CTA — which just calls `markSeen()` — causes an immediate rebuild that swaps `home:` to `AmbleHome` with no separate `Navigator.push` and no stub onboarding step in between, matching the work order's explicit "route straight into the existing app" instruction rather than inventing placeholder onboarding.

**Debug-only reset gated on `kDebugMode`, not a `FeatureFlags` entry.** Confirmed via AskUserQuestion rather than assumed (the work order asked for "a way to reset it for testing/screenshots... flag your approach"). `FeatureFlags` gates *product* features that are sometimes on, sometimes off, per build config (`bool.fromEnvironment`) — this is categorically different: a developer convenience that must never exist in any real build, full stop. `kDebugMode` is compile-time `false` in every release/profile build, giving the same dead-code-elimination guarantee as a `FeatureFlags` gate, without implying "off by default, but could be turned on for a real release" the way a `FeatureFlags` entry does. Lives in `SettingsScreen` as a new "Developer" section — reuses the existing `_SettingsPanel` pattern rather than a separate screen or dialog.

**[Splash/carousel] Splash logo is a live-drawn `CustomPainter`, reusing the Phase 9 placeholder app-icon mark — not a new asset, not new branding.**
The work order explicitly asked to flag rather than invent final branding if a real logo doesn't exist. Rather than export a new placeholder PNG (the Phase 9 approach, via a one-off Python/Pillow script), drew the same diagonal line-and-dot motif directly in Flutter (`_BrandMarkPainter`), colored from `theme.colorAccent`/`colorSurfacePrimary` — real Tier 2 tokens, so it's on-brand and theme-aware (light/dark) for free, with no new asset file to keep in sync with the actual launcher icon if that ever changes. Confirmed visually consistent with the real installed launcher icon on-device.

**[Splash/carousel] Carousel copy — four slides, explicitly draft, not final.**
Per the work order's own instruction to draft reasonable placeholder copy and flag it rather than treat it as signed off. Content pulled directly from CONSTITUTION.md's design principles already established across this build (principle 1, "the plan is provisional, not a verdict" → "Plans change. That's the plan."; principle 2, "capture is frictionless" → "Capture first, plan later"), plus the general calm/visual-planning positioning and the local-first/no-account fact (genuinely true of the app, not aspirational marketing). Each slide's icon circle reuses one of the four pastel category tint/icon-color token pairs (`TaskCategoryToken.personal/work/health/admin`) purely for visual variety across slides — no relationship to the user's actual task categories, and the mapping (which slide gets which category's colors) is arbitrary, not meaningful.

**[Splash/carousel] `PageView`, no new dependency — confirmed rather than silently assumed.**
The work order flagged this as likely unneeded and asked to flag if a package seemed warranted. It wasn't: Flutter's built-in `PageView` plus a `PageController` covers swipe navigation, `onPageChanged` for the dot indicator, and `nextPage()` for the "Next" button's own advance — nothing the carousel needed required a third-party carousel package.

**[Splash/carousel] Two real, pre-existing dark-mode bugs found and fixed, not introduced by this session — scope expanded deliberately, confirmed via AskUserQuestion each time rather than fixed silently.**

**(1) Category icon glyphs invisible in dark mode.** `AmbleTheme.dark`'s `categoryIconColors` mirrored `categoryColors` (introduced together during the pastel-palette session), so an icon rendered in the exact color of its own pill background. Traced to that session's `TaskCapsuleBlock` rewrite, which made `iconColor` read `categoryIconColors` unconditionally in both palettes — the old code path (reading `colorSurfacePrimary`, white only by coincidence in light mode) no longer existed to fall back on. This was present on every real Timeline task pill in dark mode already; the splash carousel's much larger category circles just made it impossible to miss rather than being its cause. Fixed dark mode's `categoryIconColors` to pure white for all four categories — the actual pre-pastel-session value — verified at 3.56-3.83:1 contrast against each dark category color (clears the 3:1 WCAG graphics floor), matching Phase 13a's own originally-measured 3.6-3.8:1 figure almost exactly.

**(2) The bottom NavigationBar stayed white in dark mode.** `backgroundColor: theme.colorSurfacePrimary` has been correctly wired since Phase 5, but Material 3's `NavigationBar` applies its own `surfaceTintColor` overlay by default (derived from `ColorScheme.fromSeed`) that paints *over* an explicit `backgroundColor` rather than being superseded by it — a genuine Material 3 gotcha, not a logic error in this codebase's own code. Fixed with `surfaceTintColor: ColorPrimitives.transparent`. Never caught in any earlier session because no prior dark-mode verification screenshot happened to have the nav bar itself in frame — Phase 13a's checkpoint screenshots covered the Settings toggle and one other screen, and this session confirms neither included the nav chrome. Worth recording as a verification gap, not just a code gap: dark mode has been "verified" multiple times without ever actually checking this specific, highly visible piece of chrome.

Both fixes were verified on a genuinely clean rebuild (`flutter clean`, fresh `pub get`, fresh `build_runner`, fresh install) after an initial `flutter build apk --debug` over stale build state appeared not to fix the nav bar — which was a stale-artifact false negative, not a failed fix; worth remembering that a hot `--debug` rebuild doesn't always invalidate every cached asset.

**[Splash/carousel, correction] `AmbleTheme.dark.colorSurfacePrimary` was `ColorPrimitives.white` — a real bug in the session's own edit, not the "stale build" the surfaceTintColor fix looked like it resolved.**
User-reported after the previous entry shipped: Timeline, Settings, and the splash screen's page *background* (not just the nav bar) stayed fixed white in dark mode. Root cause was a straight copy-paste slip made while restructuring the dark palette block during the `categoryIconColors` fix a few entries up — the surrounding comment correctly describes `colorSurfacePrimary` as "near-black `ink900`," but the actual field assignment two entries above it still read `ColorPrimitives.white`, unnoticed because nothing in that specific edit's diff review caught the mismatch between comment and value.

This single wrong value explains the earlier "nav bar stayed white" investigation more completely than the `surfaceTintColor` fix alone did: `colorSurfacePrimary` backs the nav bar *and* every `Container(color: theme.colorSurfacePrimary)` root across Settings, Inbox, the splash screen, `AppSheet`, `AppButton`, and the task detail sheet — a dozen consumers, not one. The `surfaceTintColor` fix was still real and still necessary (Material 3's tint overlay is a genuine, separate gotcha, confirmed by testing it in isolation before this correction), but it was fixing a symptom on top of a much larger root cause that a first, insufficiently skeptical rebuild-and-screenshot pass didn't surface — the earlier verification happened to look at a moment/build where the two bugs' visual effects were hard to tell apart.

Fixed: `colorSurfacePrimary: ColorPrimitives.ink900` (matching what the adjacent comment always said it should be). Re-verified on a fresh emulator boot + fresh install + dark mode explicitly re-selected (not carried over from stale app state): Timeline, Settings, and the splash screen all now show a genuinely dark background, confirmed via direct pixel sampling in earlier debugging (not just visual impression) before this fix, and a real on-device screenshot after it. `dart format .`/`flutter analyze`/`flutter test` clean, 124/124, unchanged.

Worth recording as its own methodology note, distinct from the two bugs above: a comment that correctly states what a value *should* be is not evidence that the value *is* that — always check the actual assignment, especially right after refactoring the surrounding block, since that's exactly when a comment and its value can silently drift apart.

**[Timeline] Connector line, icon top padding, and dynamic scroll range — three visual/behavioral requests against a reference image, confirmed via AskUserQuestion where genuinely ambiguous.**

**Connector line**: a `borderWidthConnector` Tier 2 token (3.0, distinct from the existing 2px `borderWidthHairline`) plus `colorBorder` draws a continuous gray vertical thread between every consecutive pair of tasks in the day, regardless of the gap between them — confirmed against the alternative (only connecting tasks close together in time) via AskUserQuestion; the reference always connects every pair. New `_TimelineConnectors` widget in `timeline_screen.dart`: sorts tasks by `scheduledAt`, draws one segment per consecutive pair from the earlier task's pill-bottom to the later task's pill-top, skipping pairs where the later task actually starts before the earlier one ends (an overlap-layout case — nothing to connect there, the side-by-side pills already show the clash). When consecutive tasks land in different overlap-layout columns, the segment is drawn at the *later* task's column x rather than attempting a diagonal, since the reference never shows overlapping tasks and a diagonal would read as accidental rather than deliberate.

**Icon top padding**: `TaskCapsuleBlock`'s badge `Container` padding changed from `theme.spacingXs` (4px) to `theme.spacingMd` (16px, `SpacingPrimitives.space5`) — a real existing token, not introduced for this.

**Dynamic scroll range**: replaced the fixed `_startHour`/`_endHour` module constants (0–24, from the previous session's "tasks before 6am/after 10pm unreachable" fix) with a per-day computed range: `[earliest task start − 30min, latest task end + 30min]`. Confirmed via AskUserQuestion that this is a deliberate, understood reversal of the *specific mechanism* (fixed hours) the previous fix used, while preserving its actual *intent* (nothing scheduled should be unreachable) — a task-driven range inherently keeps early/late tasks reachable, since the range is built from their own times rather than a hardcoded window. 30 minutes of padding (also confirmed via AskUserQuestion, against a 60-minute alternative) keeps a task's full pill and time label clear of the scroll boundary at either edge.

This required a real, non-trivial refactor, not just changing two constants: `_startHour: int` was consumed as `(dt.hour - startHour) * 60 + dt.minute` throughout — hour-count arithmetic that only works for a whole-hour, same-day boundary. Since the new range can start at any minute (and, in principle, cross midnight for an unusual day), every "`minutesSinceStart`" computation was changed to `dt.difference(rangeStart).inMinutes` — a genuine `DateTime` diff — across `_DayTimelineState`, `TaskBoundaryMarkers` (now takes `rangeStart: DateTime` instead of `startHour: int`), `_TimelineConnectors`, and `CurrentTimeIndicator` (now takes both `rangeStart` and `rangeEnd`, since a tightly-clamped range means the real current time can easily fall entirely outside it — previously only the "before the top" case was guarded). The auto-scroll-to-now behavior on open now falls back to scrolling to the top of the range when "now" isn't inside it at all, rather than the previous unconditional `now`-relative jump.

Verified on-device, not just by re-reading the diff: confirmed the connector line renders as one continuous thread through several consecutive tasks in both directions from the auto-scrolled starting position, and confirmed hitting a genuine scroll limit at the top of a real day's range (the current-time indicator pinned exactly at the scroll boundary, with the day's earliest task sitting just below it — the expected shape of "clamped to real content plus padding," not an artificially wide window). `dart format .`/`flutter analyze`/`flutter test` clean, 124/124 — one golden (`timeline_capsule_preview.png`) intentionally regenerated for the icon-padding change; no test referenced the old `startHour`/`TaskBoundaryMarkers`/`CurrentTimeIndicator` APIs directly, so nothing else needed updating.

**[Timeline, correction] Icon top padding corrected from 16px to 12px — a new standalone `spacingIconTop` token, not a reuse of `spacingMd`.**
Follow-up correction to the entry above: the requested padding was actually 12px, not 16. 12 doesn't sit on `AmbleTheme`'s existing spacing progression (`spacingXs`=4, `spacingSm`=8, `spacingMd`=16 — nothing at 12), so reusing `spacingSm` or `spacingMd` would have meant picking the nearest available value rather than the one actually requested. Confirmed via AskUserQuestion which way to represent it: added `spacingIconTop` as its own narrow-purpose Tier 2 token (`SpacingPrimitives.space4` = 12, threaded through the constructor, both palettes, `copyWith`, and `lerp`) rather than restructuring the shared scale to insert a new general-purpose rung — mirrors how `borderWidthConnector` was added a few entries up for the same reason (a real value with no existing token, not large enough to justify reshaping the whole scale). Keeps `TaskCapsuleBlock`'s own "consumes only Tier 2, zero hardcoded values" contract intact — reaching into `SpacingPrimitives` (Tier 1) directly from that widget would have violated it. `dart format .`/`flutter analyze`/`flutter test` clean, 124/124 — golden regenerated again for the new padding value, confirmed on-device.

**[Timeline] Completion checkbox moved off the badge to a trailing circle; tapping the pill/title now always opens edit — a real interaction-model reversal, plus a new emoji celebration.**

Reported directly, with a clear complaint: tapping the badge toggled completion while tapping the name opened edit, and that split within one row read as confusing/inconsistent. Fixed by making the whole pill+title area a pure tap-to-edit zone (removed the badge's own `onTap: onToggleComplete` `GestureDetector`) and adding a new, dedicated `CompletionCheckbox` as a trailing element in the row — matching a reference image showing a hollow ring on the far right of each task, filled with a checkmark when done.

**Minimum tap target, confirmed rather than eyeballed**: the visible ring is 24px (`theme.spacingLg` — already `SpacingPrimitives.space7`, no new token needed there), but the actual tap area is padded out to a new `spacingMinTapTarget` (48px) token — Material Design/WCAG 2.5.8's minimum target-size guidance, quoted directly in the request. Not part of the general spacing progression; documented as a fixed accessibility floor, not a design-scale rung, same category as `spacingIconTop`/`borderWidthConnector` from the entries above.

**Ring color is the category's own saturated color** (`categoryIconColors`), not the badge's `iconColor` (which intentionally goes white/near-black for the skipped/completed cases, tuned for contrast against the badge fill specifically) — the checkbox needed to stay recognizably "this task's category" in every status, which `iconColor` doesn't guarantee.

**Removed the old completed-state corner badge** (a small checkmark overlapping the icon pill's corner) — confirmed via AskUserQuestion rather than assumed, since leaving it alongside the new trailing checkbox would have meant two indicators of the same fact. The trailing checkbox is now the single source of truth for completion status.

**Celebration**: on completing (not un-completing — confirmed via AskUserQuestion, since celebrating an undo reads wrong) a Unicode emoji is picked at random from an 8-item list (clap, trophy, gold medal, direct hit, drum, chequered flag, muscle, glowing star) and fades in over the checkbox, holds, then fades out, timed to finish exactly as the checkbox itself shows filled/checked underneath — "when fade out see checkbox ticked," per the request. Built with a single `AnimationController` and a `TweenSequence` (fade-in/hold/fade-out weighted by duration), composed from the existing `motionNormal`/`motionSlow` tokens rather than a new one-off duration for the whole sequence.

**The animated Noto emoji set the request linked to was deliberately not used** — confirmed via AskUserQuestion. That's Google's Lottie-backed animated-emoji library, fetched from their CDN; using it as specified would have meant both a new network dependency and a new playback package (e.g. `lottie`), in a codebase that's stayed dependency-free all session per CLAUDE.md's "no new dependency without flagging first" rule. Plain Unicode emoji rendered as `Text` achieves the same "an emoji celebrates the tap" effect with zero new dependencies and works fully offline.

Verified on-device, not just read back from the diff: confirmed the trailing checkbox toggles independently of the pill (hollow ring → filled+checkmark), confirmed the celebration emoji genuinely renders and fades out to reveal the ticked checkbox underneath (not just that the code compiles), and confirmed tapping the pill/title area now opens the real edit sheet rather than toggling completion. `dart format .`/`flutter analyze`/`flutter test` clean, 124/124 — one golden (`timeline_capsule_preview.png`) regenerated for the removed corner badge and new trailing checkbox.

**[Timeline, follow-up] Swapped static Unicode emoji for the real animated Noto Lottie set — user explicitly overrode the earlier no-new-dependency decision.**

Directly authorized: "Use noto > override dependence allowed." Added `lottie: ^3.5.1` (current stable, published July 2026 — checked pub.dev before pinning) as a real new dependency, confirmed via AskUserQuestion which of two delivery paths to use: bundle the 8 needed Lottie JSON files locally vs. fetch from Google's CDN at runtime. Chose bundling — works offline, no runtime network dependency, immune to the CDN reorganizing later.

Downloaded the 8 files matching the user's own codepoint list (`u1f44f_1f3fc` clap, `u1f3c6` trophy, `u1f947` gold medal, `u1f3af` direct hit, `u1f941` drum, `u1f3c1` chequered flag, `u1f4aa_1f3fc` muscle, `u1f31f` glowing star) from `https://fonts.gstatic.com/s/e/notoemoji/latest/{codepoint}/lottie.json` — Google's own documented asset CDN pattern, verified each returns 200 before committing, and spot-checked one file's JSON structure (`v`/`fr`/`layers` keys, 1024×1024, valid Bodymovin/Lottie schema) rather than trusting the HTTP status alone. Committed under `assets/celebrations/`, registered in `pubspec.yaml`.

`CompletionCheckbox` swapped its `Text(emoji)` for `Lottie.asset(..., repeat: false)`. The existing fade-in/hold/fade-out `AnimationController` is unchanged and still controls *opacity* only — the Lottie animation plays its own internal timeline independently once mounted, so the celebration is now doubly animated (the emoji itself moves, and it also fades in and out over the checkbox), not just a fade on a static glyph.

Verified on-device: forced a completion and confirmed a real animated graphic renders over the checkbox (a genuine gold trophy Lottie, not a fallback/placeholder box), confirming the asset path, `pubspec.yaml` registration, and `Lottie.asset` wiring are all correct end to end. `dart format .`/`flutter analyze`/`flutter test` clean, 124/124, unchanged from the previous entry (no test touches the celebration's asset type).

**[Post-Phase 9] "Prevent overlapping tasks" preference — new default-true toggle, confirmed reversal of the side-by-side-overlap default for one flag state, not a reopening of that decision.**
Confirmed directly by the user: a new Settings preference (`PreferenceKeys.preventOverlappingTasks`), default **true**, that blocks (reject-and-snap-back, no auto-adjust, no cascade) creating/scheduling/dragging a task into a slot overlapping an existing scheduled task. The earlier "overlaps allowed, shown side by side" decision (see the post-Phase 9 "Drag shows live start/end times" entry above) still stands as the behavior when the switch is off — this adds an opt-out layer, it doesn't replace the default rendering path (`layoutOverlappingTasks` is untouched and still runs whenever an overlap is allowed to exist, regardless of the preference's value).

**Layering choice: checked in the UI layer before calling `TaskList`'s mutators, not pushed into `TaskList` itself.** Per CONSTITUTION.md's one-directional layering and the reasoning that this is a UI-level validation concern (a UI opinion about what schedule is acceptable to accept), not a data-integrity one (Hive itself has no opinion on overlaps) — pushing it into `TaskList` would mean the provider layer needs to know about a preference, which none of its other mutators do. Verified this doesn't create a gap: `importTasks` and `_materializeSeries`/`generateRecurrenceInstances` are the only other callers of the write paths, and both are deliberately excluded — a bulk/backup restore or a recurring series materializing 8 weeks out must never silently reject entries (no UI exists there to show an inline error, and silently dropping imported/generated data would be a much bigger, undiscussed behavior change).

**Completed tasks still count as blocking.** Flagged per the work order: the preference exists to give a realistic view of what's occupying the day, and a completed task occupied its slot exactly as much as a pending one — only the candidate's own id (`excludeTaskId`) is ever excluded from the comparison.

**`overlapsExistingTask` (`lib/shared/services/overlap_checker.dart`) mirrors `task_overlap_layout.dart`'s interval semantics rather than importing/reusing them.** The two live in different layers (`shared/services/` vs. a `features/timeline/` rendering concern) and serve different questions (does a slot's occupant list need N columns vs. does exactly one candidate slot conflict with anything) — mirroring the same half-open-interval check (`startA < endB && startB < endA`) was simpler than extracting a shared low-level primitive for one boolean comparison used from three call sites.

**Drag-to-reschedule's rejection has no inline error text, by design** — confirmed with the user: the snap-back itself (identical to the existing `minutesDelta == 0` no-op branch) is the rejection signal, since there's no natural place for inline text mid-gesture. `_DraggableTaskBlock` became a `ConsumerStatefulWidget` (previously plain `StatefulWidget`) so `onDragEnd` can read the preference and the current task list before deciding whether to call `onReschedule`.

**[Cascade replanning] Push replaces reject-and-snap-back for the drag path only — a deliberate, confirmed reversal of the immediately-preceding decision, and a deliberate reversal of SCOPE.md's "explicitly out of MVP" line for cascade replanning specifically.** Confirmed directly by the user, as its own phase: `docs/SCOPE.md` now describes the implemented behavior in place of the old deferral line. The create wizard's Save/Schedule button and the edit-schedule modal's Save button (`task_detail_sheet.dart`) are untouched — both keep the exact reject-with-inline-error behavior from the entry above, since neither has a drag gesture to compute a push direction from, and a typed/picked time that overlaps should still just be rejected. Only `_DraggableTaskBlock.onDragEnd`'s rejection branch (`timeline_screen.dart`) changed.

**The push algorithm, derived by hand from two worked examples before any code was written** (per the work order's explicit instruction not to skip to UI wiring with an unverified algorithm) — full derivation and worked-example unit tests in `lib/shared/services/cascade_reschedule.dart` / `test/shared/services/cascade_reschedule_test.dart`. Direction: compare the distance from the dragged task's *new start* to the overlapped task's own start vs. its own end — whichever is nearer decides the push direction. Nearer-to-end → push the existing task **earlier**, its end landing exactly on the dragged task's new start (own duration preserved). Nearer-to-start → push the existing task **later**, but its new start lands on the dragged task's new *end*, not its start — because pushing later means the existing task now has to clear the mover's *entire* span, not just where it begins. This asymmetry is real, not an inconsistency: pushing earlier only needs to clear the mover's start (nothing after that start belongs to the mover from the earlier task's new position); pushing later has to clear the mover's end (the later task is now sitting immediately after the whole mover). Verified against both of the user's worked numbers: existing 11:00–12:00, drop at 11:45 (nearer end) → existing becomes 10:45–11:45 (the user's own restated "10:48" does not survive re-derivation from the stated rule and was treated as an arithmetic slip, per the work order's explicit warning not to trust a possibly-typo'd restatement — same category as the "12:115" the work order itself flagged as sic); existing 11:00–12:00, drop at 10:15 (nearer start) → existing becomes 11:15–12:15 (matches the user's own number once "12:115" is read as the stated typo for 12:15).

**Cascade chains via a simple BFS-style queue with a visited-id set, not recursion.** Each pushed task's new slot is checked against every remaining same-day task for a fresh overlap, which — if found — gets pushed the same way and queued to have its own new slot checked in turn. The visited set is both the cycle guard and the "never push the same task twice in one cascade" rule; chain length is additionally capped at the day's participating-task count, so a pathological input can't loop indefinitely even if the visited-set logic ever had a gap. Day-boundary guard (any move landing before 00:00 or after 24:00 of the day the dragged task started on) is checked per-move as the cascade builds, returning `null` the instant any move fails it — the caller then snaps the whole drag back untouched, exactly like the old reject behavior, so nothing partially applies.

**Persistence: a new `TaskList.rescheduleTaskWithCascade(List<TaskMove>)` provider method, not a UI-layer loop over the existing `rescheduleTask`.** Judgment call, flagged per the work order's explicit "make a call and justify it" instruction. Chose the provider-layer loop, following `_materializeSeries`'s precedent (loops and writes several tasks through `TaskRepository`, one `_refresh()` at the end) rather than `_DraggableTaskBlock` looping calls to the existing single-task `rescheduleTask`: a UI-layer loop would mean the widget makes N separate awaited provider calls, each triggering the notifier's own `_refresh()` and rebuilding the Timeline mid-loop — visually, every pushed task would appear to move one at a time rather than atomically, which contradicts the "applied atomically... pushed tasks just appear at their new positions after the drop completes" requirement. `rescheduleTaskWithCascade` looks each `TaskMove`'s task up fresh by id (mirroring `rescheduleTask`'s exact per-task semantics — `originalScheduledAt` set only once, `status` → `rescheduled`) and calls `_refresh()` once, after the whole cascade is written.

**`TaskMove` is a small paired class (`taskId` + `newScheduledAt`), not the `Task` object itself.** The cascade algorithm is pure and widget-free (`shared/services/`), so it never holds a live `Task` reference that could go stale between computation and the eventual write — the UI layer re-reads `ref.read(taskListProvider)` fresh right before computing the cascade, and `rescheduleTaskWithCascade` re-resolves each task by id from the repository immediately before writing it, the same pattern every other mutator in `TaskList` already uses.

**[Timeline] Task-boundary labels reverted to a fixed hourly grid, anchored to clock hours rather than to the dynamic `rangeStart`.**
Requested directly, reversing the entry above (`hourlyGridTimes` in `task_boundary_markers.dart` replaces `taskBoundaryTimes`). The dynamic per-day `_visibleRange` from a later session (earliest task − 30min to latest task + 30min) means `rangeStart` is almost never on a whole hour, so a naive "every `intervalHours` from `rangeStart`" grid would produce odd-looking ticks like 6:47, 7:47. Confirmed via AskUserQuestion: ticks snap to the next whole clock hour at or above `rangeStart`, then step by `intervalHours` (default 1) — reads as a normal clock, matches the "11:00 12:00" example given directly. `intervalHours` is a real parameter (not a hardcoded 60), so the mentioned "every 2 hours" follow-up needs no further code change, just a different call-site value — not yet exposed as a user-facing setting, since that wasn't asked for.

**[Timeline] Left-side hour gutter made a Settings toggle (`ShowHourLabelsSetting`), off collapses the gutter width to 0.**
Requested directly: "Add config switch off day hours (on the left side of scren) configurable in settings". Confirmed via AskUserQuestion which of two readings of "off" was meant: collapsing the gutter to reclaim its width for tasks (chosen) vs. leaving the width reserved but blank. New `PreferenceKeys.showHourLabels` (bool, absent defaults to `true` — a fresh install looks unchanged), `ShowHourLabelsSetting` provider mirroring `PreventOverlappingTasksSetting`'s exact shape (`keepAlive: true`, same reasoning). New "Timeline" section in Settings (its own section, not folded into "Scheduling" — that one governs overlap behavior, a different concern from gutter display) with one `AppSwitch`, matching the existing panel pattern exactly.

**[Timeline] Completed-task badge switched from `colorTaskCompleted` (sage green) to `colorTextSecondary` (the same grey as completed title text); `CompletionCheckbox` gained a `useMutedCompletedColor` variant used on the Timeline.**
Requested directly: the badge should read the same grey as its own title text when completed, and the checkbox's checked-state ring should match. Two AskUserQuestion confirmations settled the shape: (1) the badge reads `colorTextSecondary` directly at its call site rather than repointing `colorTaskCompleted`'s own value — always exactly in sync by construction, no risk of the two drifting if either is edited independently later. `colorTaskCompleted` is now unused but left defined (flagged in-code, not silently deleted — removing a Tier 2 token isn't this change's call). (2) The checkbox variant is a new `useMutedCompletedColor: bool` param (default `false`) on the existing `CompletionCheckbox`, not a separate widget — this reverses a previously-confirmed decision ("ring is always the category color, regardless of status," recorded in the removed comment) specifically for the completed case, so the reversal is scoped to one flag rather than duplicating the whole component. Only the checked-state FILL changes; the unchecked border stays the category color (`ringColor`) unconditionally, and every other status (skipped, rescheduled, pending) is unaffected. Timeline's `TaskCapsuleBlock` is currently the checkbox's only caller and passes `useMutedCompletedColor: true`; the default keeps any future caller on the original always-category-colored ring unless it opts in.
Contrast checked, not assumed: the new grey badge's white glyph clears a similar WCAG ratio to the existing skipped-badge/white-glyph pairing already shipped (dark mode: ~2.48:1 new vs. ~2.00:1 existing) — both sit below the 3:1 AA graphics bar, but this isn't a new or worse tradeoff than what's already accepted for the skipped state.

**[Timeline] Repeats settings on an already-recurring task made fully editable — change days or disable, with a non-destructive future-instance policy.**
Requested directly, the follow-up to the read-only staging above. Four AskUserQuestion rounds settled the real data-model decisions before writing code (only the template task carries the rule; every other instance just references it):
1. **Which row edits.** Any instance's Repeats panel edits the WHOLE series, resolved to the shared template via a new top-level `findSeriesTemplate(instance, allTasks)` — not "only editable from the template," since a user has no way to tell which instance that is.
2. **Future-instance policy on a rule change or disable.** Delete untouched future instances only (still `pending`, never individually rescheduled — `originalScheduledAt == null` — and scheduled today or later); anything the user completed, skipped, or moved is a real action on that occurrence and is never deleted just because the series rule changed underneath it. Same policy for both a days-change and a disable.
3. **"Untouched" defined precisely** as above, confirmed exactly as stated rather than assumed.
4. **Disable semantics.** Clears BOTH `recurrenceId` and `recurrenceRule` on the template — it becomes an ordinary task, same as one that never repeated (switch re-enables as editable, repeat icon disappears) — not a "was recurring" husk that keeps `recurrenceId` for history. Past/touched instances keep their own `recurrenceId` untouched; only the template detaches.

New `TaskList.updateTaskWithChangedRecurrence`/`disableTaskRecurrence` (`task_providers.dart`), alongside the existing `updateTaskWithNewRecurrence` — three distinct methods, not one with branching optional params, same reasoning as before (every other `updateTask` caller must never accidentally touch recurrence). Both new methods: save the edited task's own field changes, resolve the series template, prune untouched future instances, then either write the new rule + re-materialize (change) or clear the template's recurrence fields (disable).

`_RecurrencePanel`'s `enabled` parameter (added for the read-only staging) removed again — the analyzer flagged it as genuinely dead once every caller relies on the default, and keeping an unused parameter "as a future seam" doesn't survive this repo's own no-dead-code rule.

**[Timeline] A drop onto a busy zone is never rejected — the cascade falls back to the opposite push direction instead.**
Requested directly, after observing drops snapping back in condensed zones: "should always allow to drop should not revert, can't think of case that should not be allowed to drop to a busy already taken zone, it'd just move other items up down or some up some down." Previously each conflicting task picked ONE push direction (earlier or later, by proximity to the mover's edges) and the whole cascade aborted if that direction ran past 00:00/24:00 — so a dense evening cluster rejected the drag even when shifting those tasks earlier would have fit fine. Now each push tries its preferred direction first, then the opposite one, and only fails if neither fits. The proximity rule that picks the *preferred* direction is unchanged, so the two hand-derived worked examples (and the 3-task chain) still produce byte-identical results — verified before committing to the change.

Rejection is now reachable only when the day genuinely cannot hold the tasks: the dragged task itself landing outside 00:00–24:00, or a day packed near its full 24 hours (rare, given the 2h-per-task cap). The all-or-nothing contract is unchanged — a failed cascade still applies nothing and snaps back.

Two test assertions that encoded the old behaviour were updated rather than left passing: the "day-boundary guard aborts" test now asserts the fallback succeeds and pushes earlier, and the cycle-guard test no longer accepts `isNull` as a valid outcome for a mid-morning cluster with hours free on both sides — that permissive assertion would have hidden exactly the over-eager rejection later reported.

**[Task detail] Removing a recurring task asks "this occurrence" vs "all occurrences"; "all" means this one plus everything from today forward.**
Requested directly. Previously Remove deleted a single instance with no confirmation at all, even for a series. Two scope questions confirmed via AskUserQuestion rather than assumed:

1. **Past instances are kept** on "all occurrences" — the series stops from today forward, history stays. Same reasoning as `disableTaskRecurrence`: a completed past occurrence is a record of work that happened, and "remove this repeating task" reads as "stop it from here on," not "erase what I did."
2. **Touched future instances are NOT spared**, unlike `_deleteUntouchedFutureInstances` (which protects completed/skipped/rescheduled occurrences from a *rule change*). That protection exists so an incidental rule edit can't silently destroy deliberate work; an explicit delete is the opposite intent, and leaving a rescheduled future occurrence behind would read as the delete having failed.

New `TaskList.deleteTaskSeries(instance)` handles the "all" branch. A template that falls in the past survives as history but is stripped of `recurrenceId`/`recurrenceRule`, or it would keep generating new instances on the next rolling-window top-up.

**UI is an `AppSheet`, not `AppAlertDialog`**: this is a three-way choice (this / all / dismiss) and the dialog primitive returns `bool?` — two actions plus dismiss. The sheet is also the primitive the action menu itself uses, so the two reads as one flow. Reuses the existing `_ActionRow` and `colorTaskAlert`; no new widgets or tokens.

**[Timeline] Hiding hour labels now also collapses the gaps and sizes pills proportionally to duration.**
Requested directly: with no hour scale on screen, the gaps between tasks reference nothing — they just push a sparse day into long stretches of empty scrolling. Three decisions confirmed via AskUserQuestion:

1. **One switch, not two.** Collapsing rides on the existing "Show hour labels" toggle rather than getting its own. They're logically one decision (the gaps only mean something because the hour scale gives them meaning), and separate switches would allow the incoherent "labels on, gaps collapsed" combination.
2. **Fully proportional pill heights**, over stepped buckets or a dampened curve — a 1h task reads as literally twice a 30m one.
3. **Drag-to-reschedule is disabled in collapsed mode.** Vertical position there is stacking order, not time, so there's no honest pixel→minute conversion. Rejected both alternatives (repurposing drag as reordering — a different feature that silently rewrites times; and converting via the collapsed scale — arbitrary, since the gaps crossed mean nothing). "Edit time and duration" still works, and dragging returns when labels are back on.

The scale is **derived, not chosen**: `_collapsedPixelsPerMinute = pillWidth / 30`, so 30m lands exactly on the badge-size floor and every longer duration scales from there. Also suppressed in collapsed mode: the connector thread and the current-time indicator (both encode elapsed time), and the open-scrolled-to-now behaviour.

**[Task detail] Create-flow redesign: new "General" category, uncapped duration, and one redesign shared by both create and edit-schedule.**
From a wireframe mockup + voice walkthrough, confirmed via AskUserQuestion rather than assumed:

1. **A genuine 5th category, not a reorder.** `TaskCategory.general` is a real new Hive-persisted value (`@HiveField(4)`, appended after `admin` — the existing four indices are never renumbered), grey, first in the picker and the default for new tasks (`_category = task?.category ?? TaskCategory.general`, was `personal`). Display order is `general, health, work, personal, admin` via a new `TaskCategoryPickerOrder.orderedForPicker` extension, kept deliberately separate from the enum's own declaration order so Hive-index stability and picker-display order can't accidentally couple.
2. **Duration has no upper cap.** Explicitly confirmed ("No cap — any positive duration") when asked whether to raise or remove the old slider's ceiling. `_NumericTimeField`'s hour box for Duration passes `firstMax: null` — uncapped — while Start/End time hour boxes stay capped at 23/59 as before.
3. **Typed numeric boxes replace the scroll-wheel/slider for both Start time and Duration.** The mockup showed typed entry; confirmed directly rather than inferred from the image alone.
4. **One redesign, two call sites.** The new preview card + numeric time entry apply to both the create wizard and the standalone "Edit time and duration" modal, since both already share `_ScheduleStepScaffold` — confirmed "Both" rather than scoping to create-only, so the two flows don't visually diverge.

**[Design system] An explicit 0/1/2 surface scale, a shared field shell, and one masked component for both Time and Duration.**
Requested as "we need to refine our forms and design system", referencing Revolut's inline-label behaviour. Four AskUserQuestion rounds settled it before any code:

1. **Floating label follows the platform convention**: the label rests full-size and centred while the field is empty AND unfocused, and floats small to the top as soon as the field is focused OR holds a value. Chosen over a permanently-small static label — a filled field should never hide what it is, and the motion is the point of the request. `AppFieldShell` takes `isFocused` and `isFloating` as SEPARATE flags, because they genuinely differ: a filled-but-blurred field floats its label without taking the focused fill.
2. **Field fill is a new token (`colorSurfaceField`), not a repointed `colorSurfaceSecondary`.** The existing secondary surface is sage-tinted and has other call sites, so reusing it would have dragged unrelated screens into this change. The surface scale is now explicitly 0 (base) / 1 (pane) / 2 (field), with a separate active tone for focus.
3. **Neutral greys for surfaces ONLY.** Category tints, the sage accent, and the text ramps are untouched. The warm `sand`/`sage` chrome was specified away for surfaces; extending that to text and borders would have been a whole-app visual shift well beyond the two frames under discussion.
4. **Emoji category glyphs, but the app's real five categories.** The mockup's "Shopping" was placeholder art, not a data-model request — no Hive change. `emoji` is a NEW getter alongside the existing `icon`, not a replacement: `icon` stays for the places a monochrome vector that takes a tint from its context is still the right primitive.

**One component serves Time and Duration**, per the explicit "e.g. time and duration same comp". They differ only in `firstMax` (23 vs. uncapped) and `firstDigits` (2 vs. 3). Those are deliberately two knobs rather than one: the cap governs the *value*, the digit count governs the *mask*, and an uncapped field still needs a finite width for the caret arithmetic to work — collapsing them would truncate a long duration or break the caret.

**The masked field is a single `TextField`, replacing the two-box `_NumericTimeField`.** With two boxes the separator is dead scenery between independent inputs and the requested caret behaviour is impossible; as one masked string, crossing the separator is just ordinary caret movement. This also retired the auto-advance-focus mechanism (and the `onChanged`-vs-controller-listener bug it carried) entirely — there is no second field to advance to any more.

**`AppPane` draws a hairline border.** The 0→1 step is only ~3% lightness (#F7F7F7 → #FFFFFF), which is the intended quiet scale but too close to define an edge on its own — verified by rendering, where panes read as absent and their fields looked like they floated on the page.

**[Design system] Radius tokens named by size; accent swap scoped to the accent role; exit dialog gets a cancel.**

1. **Radius rungs are `sm/md/lg/xl`, not component names.** The previous `radiusControl`/`radiusCard`/`radiusSheet` named the *consumer* rather than the value, which meant two controls that should look identical could be pointed at two different tokens that happened to hold the same number — and would then silently diverge the moment one was retuned. `radiusModal` (40) is deliberately NOT a rung on that scale: it's a one-off shape for one surface, and putting it on the shared scale would invite it onto ordinary cards.
2. **The brand blue applies to the accent ROLE only** — buttons, FAB, selected states, day pills. `colorTaskCompleted` keeps sage, confirmed directly: green reads as "done" independently of branding, and repointing it would have made completed tasks indistinguishable from selected ones.
3. **The exit confirmation offers three choices, with cancel as the default.** Confirmed directly over a two-action version. `AppAlertDialog.show` returns `bool?` and cannot express three outcomes, so `showThreeWay` is a SEPARATE method returning an `AppAlertDialogChoice` enum rather than an optional third parameter on the existing one — overloading it would have made every existing call site's `true`/`false` ambiguous. Dismissal resolves to `cancel` rather than null, so the safe outcome and the default outcome are the same and no accidental dismissal can lose work.
4. **The pane's edge is a shadow, not a border or a darker ground.** Reverses last session's decision to deepen the background to #EFEFEF: that solved the invisible-pane problem by moving away from the specified #F7F7F7. `shadowPane` carries the separation instead, which keeps the spec AND says "raised" rather than "outlined".

**[Task detail] An unset schedule is a real state; the wheel is an alternative, not a mode.**

1. **Time and duration start unset, and Confirm is gated on both.** Confirmed directly over the alternative (show empty fields but silently fall back on save). A field that displays `hh : mm` and then saves 14:00 would be lying about what it holds; gating Confirm keeps the empty state honest and matches the existing "Continue needs a name" pattern. The DATE stays non-nullable — "Today" is a genuine default the user can see, not a hidden fallback.
2. **The duration wheel offers 0–23 hours**, while typing stays uncapped. A 100-row wheel makes ordinary values (1–2 hours) a long scroll to reach, and a task longer than a day is rare enough that typing is the right path for it. The two entry methods deliberately have different ranges rather than the wheel constraining the field.
3. **Mask capacity and display width are permanently separate concepts** (`_maxHourDigits` vs. the rendered width). This is the second time they were conflated — the first fix corrected the widget but left the formatter padding to capacity, so `000 : 30` survived. Capacity now exists only inside the formatter.
4. **Caret resets to the hour on focus, not on every frame.** An unconditional post-frame reset also fires after each keystroke's frame and drags the caret back to the start mid-typing; it is gated on the focus TRANSITION and skipped if the caret has already moved.

**[Task detail] The 2-step wizard collapses to one screen with per-field modals; only the initial create walks the user through them in sequence.**

1. **Step 1 is retired from the live flow, kept as an unreferenced backup.** Confirmed directly ("keep step 1 as backup") rather than deleted outright — `_DetailsStepScaffold`/`_EditDetailsForm`/`showEditDetailsSheet` are untouched, just no longer reachable from the create wizard's own routing. Recoverable without touching git history if this needs to come back.
2. **Date keeps the native OS picker; it does NOT get a fourth custom modal.** Confirmed directly. Only Name/Category, Start time, and Duration are the three per-field modals — the mockup's numbering was these three, not four.
3. **The duration wheel steps by 5 minutes; the start-time wheel stays every-minute.** Confirmed directly. The two fields are NOT symmetric: a start time is commonly set to an exact minute (matching a meeting, an appointment), while a duration is rarely picked to the exact minute by wheel — the typed field already covers that case for whoever wants it.
4. **Auto-advance (Name → Start time → Duration on Done) runs ONLY for a genuinely new task**, never for editing an already-scheduled one or the Inbox "give it a schedule" case. Confirmed directly. The eligibility is a one-shot flag, set once in `initState` (`task == null`) and cleared the first time the guided sequence is interrupted (a modal dismissed without confirming) or completed (Duration, the last step, resolves either way).
5. **`Task.notificationsEnabled` is a real persisted field, not a UI-only placeholder.** Confirmed directly over shipping the switch as decorative. Defaults to `true`, matching `NotificationService`'s prior unconditional-scheduling behaviour exactly, so no existing task's notification behaviour changes on upgrade.
6. **Notifications gets its own bare pane; Repeats stays nested inside Date's pane.** Direct instruction: "this section has no header, just pane with label and switch inside" — the two toggles are NOT symmetric in the mockup despite both being switches, because Repeats is conceptually part of "which day(s)" (Date's own question) while Notifications is an unrelated, standalone setting.

**A `showModalBottomSheet` route does not unmount the screen behind it.** Discovered debugging this session's tests, not a design decision but a real constraint the test helpers now account for: the main screen's widgets (its own `TextField`s included) stay in the tree, so any finder that doesn't scope to the modal's own widget subtree risks silently targeting the wrong screen. Documented in ERROR_LOG.md as a reusable gotcha for any future modal-over-modal test.

**[Task detail] Create flow becomes two stages in one sheet; Duration and Category leave the guided-modal-chain design entirely.**

1. **Supersedes the prior entry's item 6**: Repeat is no longer nested inside Date's pane — it moved to its own standalone bare pane (matching Notifications) earlier this session, before the stage restructure below. Both Repeat and Notifications are now symmetric: two standalone toggles, neither borrowing another field's pane.
2. **The guided auto-advance chain (Name → Start time → Duration on Done) is retired.** Superseded by a mockup-driven redesign: stage 1 (Name only, inline on the sheet, no modal) confirms into stage 2 (the full form) — one transition, not a three-modal chain. Confirmed directly from a 3-screenshot mockup. The empty-name-abandons-the-flow behavior (prior session) carries over unchanged, just re-anchored to stage 1's own Done instead of the old auto-opened modal's Done.
3. **Duration no longer opens its own modal from the create flow.** The presets + wheel that used to live inside `TaskDurationModal` now render inline in the schedule pane, directly under a read-only "Duration" link row. Confirmed directly ("Opens nothing — presets/wheel are already inline"). `TaskDurationModal` itself is NOT deleted — it's still the real UI for the separate "Edit time and duration" flow (`_EditScheduleForm`, reached from an existing task's action sheet), which is unaffected by this change.
4. **Category gets its own small modal, split from Name.** `TaskNameCategoryModal` bundled Name+Category because they used to open together; now that Name lives in stage 1 alone, a new `TaskCategoryModal` (category chips only, same visual chip component) is what the schedule pane's "Category" row opens. `TaskNameCategoryModal` itself is NOT deleted — still used by `_EditDetailsForm`.
5. **Time becomes read-only text on the schedule pane, not an inline typed field.** Confirmed directly. All Time entry/editing happens in `TaskStartTimeModal`; the row just reflects the resolved range (or a placeholder dash) and opens that modal on tap — unlike Duration and Category, whose rows are styled as blue links, Time's value renders in the ordinary text colour per the mockup.
6. **Stage 2's panes run a one-time staggered entrance, not a persistent animation.** Each pane fades/slides in with a 40ms delay per position, triggered once in `initState` — re-running it on every rebuild (e.g. a duration change re-rendering that one pane) would be visually wrong, so it's scoped to first build only.

**[Timeline] Overlap clustering: 2–3 mutually-overlapping tasks render as one aggregate block, not individual pills.**

1. **A run of 4+ chain-overlapping tasks is excluded from clustering entirely — not clipped to the first 3.** Confirmed directly via AskUserQuestion rather than guessed: `detectOverlapClusters` treats the whole contiguous overlapping run as one group, and only emits an `OverlapCluster` if that group's size is 2 or 3; a run of 4+ falls through untouched and all its tasks render as ordinary individual capsules (naive spatial overlap) for that run. No "4th item" visual treatment exists anywhere in the codebase.
2. **Clustering is ON by default.** `PreferenceKeys.disableOverlapClustering` is absent/null → `false`, matching the `ShowHourLabelsSetting` precedent of "missing key means the modern default, not the legacy one."
3. **The dragged task is excluded from cluster detection for the duration of the drag**, regardless of what it overlaps underneath — `detectOverlapClusters` is called on `tasks.where((t) => t.id != _draggingTaskId)`. This is what keeps the actively-dragged task rendering as its own full capsule while everything else on the day updates around it.
4. **The crossfade on cluster formation/dissolution is a bare `AnimatedSwitcher`**, keyed by the cluster's member-task-ids joined with `-`. A change in cluster MEMBERSHIP (not just position) changes the key, so Flutter treats it as a different widget and animates the transition; no custom animation was built, per the work order's explicit "do not build an elaborate animation."
5. **The cluster boundary label uses `min(start)`/`max(end)` across the cluster's own tasks, not any one member's own time.** Rendered by a separate widget (`OverlapClusterBoundaryLabels`) styled like the existing hour-marker/`TaskBoundaryMarkers` labels, so the cluster's actual span reads consistently with how a single task's boundary already reads elsewhere on the timeline.
6. **REVISED (direct visual feedback, following on-device review of the first build): the stacked-icon badge and "Overlapping tasks" label are gone.** The original design's aggregate block (icon badge + label + flat list, all in one card) was replaced with two separate pieces: the cluster's member tasks now render as their REAL `TaskCapsuleBlock` pills, at genuine spatial height/column/position (reusing `layoutOverlappingTasks` — the same function the naive-overlap/clustering-disabled mode already uses, not a reimplementation), with icon/title/time/checkbox content suppressed via a new `TaskCapsuleBlock.contentHidden` flag — leaving just the category-colored shape. The flat title+time list (unchanged in its own row styling) moved out of that card into its own block, positioned beside the pill group and top-anchored at the cluster's start. The two pieces are deliberately NOT positionally tied beyond that shared top anchor — the list's row order (chronological, earliest first) is the only correspondence to which pill is which, confirmed directly as a decision rather than attempting per-row-to-pill vertical alignment. `OverlapClusterBlock` (the list) and the new `OverlapClusterPills` (the blanked pills) are both still keyed by cluster membership and wrapped in their own `AnimatedSwitcher`, so the crossfade behavior from item 4 above is unchanged — it now just crossfades two widgets per cluster instead of one.
7. **`OverlapClusterPills`' inner `Stack` needs an explicit `SizedBox`,** sized from `layoutOverlappingTasks`' own column/height math (max column count × pill width, tallest pill's bottom edge) — a real bug hit during this revision: a `Stack` containing only `Positioned` children takes unbounded constraints from a loosely-positioned ancestor `Positioned` (no `right`/explicit width), which crashed with "A Stack requires bounded constraints from its parent." Sizing the `Stack`'s own parent explicitly, rather than relying on the ancestor to constrain it, fixed this.
8. **SUPERSEDES items 6–7's `OverlapClusterPills` widget: cluster member pills are now draggable, and lanes are fixed rather than packed.** Confirmed directly, immediately after reviewing the item-6 build: a clustered task must stay draggable like every other task, so the separate non-interactive `OverlapClusterPills` layer (a static `Stack` of blanked `TaskCapsuleBlock`s) is gone. Clustered tasks now render through the SAME `_DraggableTaskBlock` loop every other task uses, each carrying a `contentHidden` flag (title/icon/time suppressed, checkbox and drag both stay live) that a resting cluster member sets and the drag handler forces off the instant that task becomes the one being dragged — a dragged pill always shows full content, matching the pre-existing "dragged task renders normally" rule, now also covering a task that started out clustered. `TaskCapsuleBlock.contentHidden`'s meaning changed to match: it no longer blanks the checkbox, only icon/title/time (see item 9 below — this checkbox behavior was itself revised again immediately after). Lane assignment also changed from `layoutOverlappingTasks`' packed-column algorithm (which reuses a column once its previous occupant finishes, and does not guarantee one lane per task) to a fixed one-lane-per-task scheme (`_withClusterLanes`) — lane index always equals chronological position within the cluster, confirmed directly for every cluster size (not just 3-task clusters), so "leftmost pill, topmost list row" is a real positional guarantee rather than an incidental one.
9. **SUPERSEDES item 8's pill-side checkbox: the checkbox moved into each list row instead, and the pill is a pure colored shape again.** Confirmed directly, immediately after reviewing item 8's build — having a checkbox sit right next to each blanked pill read as if the pill were its own separate task card, rather than a plain positional marker for its corresponding list row. `TaskCapsuleBlock.contentHidden` reverted to suppressing the checkbox too, alongside icon/title/time — a resting cluster member's pill is once again ONLY the category-colored shape, still draggable and tappable. Each `_ClusterTaskRow` in `OverlapClusterBlock` now carries its own trailing `CompletionCheckbox`, right-aligned within the list panel — matching where an ordinary capsule's checkbox sits within its own row, just scoped to the list's own width instead of the full timeline width. `_DraggableTaskBlock`'s resting-blanked-row width is back to just the pill's own width (not pill+checkbox as in item 8), and the cluster list's `left` offset only needs to clear the pill lanes again, not a trailing checkbox column.
10. **`contentHidden` must never change `TaskCapsuleBlock`'s widget SHAPE, only what's visually shown within it — a real bug from items 8–9, reported directly as "double drag needed."** `contentHidden` was implemented as an early-return branch (a bare `GestureDetector`+`AnimatedContainer`, structurally different from the full `TweenAnimationBuilder`→`Container`/`ClipRRect`/`BackdropFilter`→`IntrinsicHeight`/`Row` tree the non-hidden path builds). Since a resting cluster member's `contentHidden` flips false the INSTANT a drag on it starts (see item 3, drag-exclusion), that swap tore down and rebuilt the very `GestureDetector` tracking the gesture mid-press — so the first touch only ever lifted the pill (the rebuild happened) but never tracked the finger, and dragging only worked after a release-and-re-press. This is the EXACT bug class already documented and fixed once for `isLifted`/the frosted wrapper (see this widget's own doc comment above the `TweenAnimationBuilder` return) — reintroduced by not applying the same rule to a second conditional. Fixed the same way: `contentHidden` now renders the identical tree shape at every value, fading icon/title/time/checkbox to `opacity: 0` (and `IgnorePointer`-wrapping the checkbox) rather than omitting them. A second-order bug surfaced by this same fix: the title/time text's own intrinsic height doesn't shrink just because it's invisible, and `IntrinsicHeight` (wrapping the whole pill/text/checkbox Row) sizes the row to its TALLEST child — a short cluster pill (badge-height floor) is often shorter than two lines of body text, so a naive opacity-only fix produced a real `RenderFlex` overflow ("OVERFLOWED BY 56px"). Fixed by wrapping the text column in `ClipRect` + `ConstrainedBox(maxHeight: contentHidden ? pillHeight : infinity)`, capping its layout height (not just its paint) to the pill's own height whenever hidden. `timeline_capsule_preview.png` (golden) regenerated for the resulting sub-pixel `ClipRect` boundary diff (0.01%, 64px, confirmed benign anti-aliasing, same pattern as an earlier session's regeneration) — not a real visual regression for the non-clustered path this golden covers.

11. **The cluster's flat list has no card/rounded-background wrapper.** Confirmed directly ("names should not have that rounded bg") — `OverlapClusterBlock`'s outer `Container` (surface fill, `radiusTaskPill` corners, `shadowPane`) is gone; rows sit directly on the timeline as plain text, matching the "no per-row card" rule the individual rows already followed. Only vertical padding remains, so rows don't sit flush against the pills.
12. **A dragged cluster member's ghost renders at its RESTING lane, not an ordinary `layoutOverlappingTasks` column.** Real bug, reported directly: "ghost state is not treated as solid object and causes items to go in 2 lanes and transparent one." The dragged task is excluded from `detectOverlapClusters` (item 3), so it falls back to the ordinary packed-column algorithm for its own slot — but the REMAINING cluster members recompute into the fixed one-lane-per-task scheme (item 8), and the two schemes don't agree on column indices. The ghost (left behind at the original position, 20% opacity, purely visual) was reading its column from the ordinary scheme, landing it in a lane that visually collided with a remaining member's new fixed lane. Fixed with a second `restingClusters` computation (identical to `clusters` but WITHOUT the drag exclusion) used only to look up the ghost's lane via `_ghostSlotFor` — the ghost now always renders at the same lane it held as a resting member, consistent with its neighbours.
13. **The cluster's flat list holds its full structure through a drag — the dragged member's own ROW fades, the list does not shrink mid-drag.** Confirmed directly: the list used to crossfade to a shorter version the instant a drag started (since the `clusters` it read from already excludes the dragged task), which read as the row vanishing rather than the task being lifted — inconsistent with how a normal capsule's own name/time/checkbox merely FADE while lifted, not disappear. Fixed by rendering the list from `restingClusters` (full membership, computed without drag exclusion) instead of `clusters`, with a new `OverlapClusterBlock.fadedTaskId` (→ `_ClusterTaskRow.isFaded`) fading just that one row via `AnimatedOpacity` — same treatment `TaskCapsuleBlock.isLifted` already gives its own parts. The list's own `AnimatedSwitcher` key is now derived from `restingClusters`' membership, which stays fixed for the whole drag and only changes once the drop actually commits a new schedule — so the crossfade still fires for a genuine membership change (drop creates/dissolves a cluster), just not for the drag itself.
14. **Day strip: background fill now marks the SELECTED day (any day); a small accent-colored dot marks the CURRENT day.** Supersedes the earlier mockup-driven design ("grey bg is for the current day," DECISIONS.md's Phase-N day-strip entry) — confirmed directly. `_DayChip`'s `background` is now driven by `isSelected` instead of `isToday`; the outline previously used to distinguish "selected but not today" is removed, since background alone now signals selection for every day. `isToday` instead renders a small filled circle (`theme.colorAccent`) below the day number, in a fixed-height slot so a neighbouring non-today chip's text doesn't shift depending on whether the dot is present — both states (today, selected) remain independently visible at once, same goal as the design it replaces, just inverted which state gets which treatment.

**[Quick Capture natural-language parsing] Pure Dart, on-device, no external APIs — confidence threshold and highlighting approach flagged for review, per the work order.**

1. **Confidence rule (the auto-create gate): a parse is confident if and only if an explicit date/time anchor was matched** — a relative-day word (`today`, `tomorrow`, `next <weekday>`) or a clock time (`at 10:30`, `3pm`). A duration or recurrence phrase found with NO such anchor does NOT make the parse confident, even though something was extracted — flagged explicitly per the work order's own "every morning" example. Rationale: auto-create with no confirmation step is only safe when the resulting `scheduledAt` is the thing the user unambiguously typed, not an inference. When not confident, `durationMinutes`/`recurrenceRule` are both `null` in the result even if phrases matched — nothing partially leaks into a result the caller might treat as meaningful — and the ENTIRE raw input becomes the title untouched, exactly matching today's existing Quick Capture fallback.
2. **A relative-day word with no explicit clock time defaults the time-of-day to 09:00.** Flagged as an explicit placeholder choice, not a guess at intent beyond "this day" — duration defaults to `quickCaptureDefaultDurationMinutes` (30, matching the create-flow default) under the same reasoning.
3. **Live highlighting is implemented via a `TextEditingController` subclass overriding `buildTextSpan`,** not a stacked-overlay `RichText`-over-invisible-`TextField` approach. Flagged per the work order: this keeps cursor placement, selection, IME composition, and deletion entirely native, since there's only ever one real text-rendering object rather than two that must stay pixel-identical to each other. Re-parses on every `buildTextSpan` call — acceptable for Quick Capture's short, single-line input, not a pattern to reuse for a large multi-line document.
4. **Highlight colors: three EXISTING Tier 2 tokens, not new ones** — `colorAccent` (date/time), the WORK category's `categoryIconColors` entry (duration), `colorTaskAlert` (recurrence). Flagged mapping, since none of the three was originally named for this purpose, chosen for being saturated/distinguishable and not colliding with any category pill shown elsewhere in the same view.
5. **New adaptive component: `AppUndoToast`.** No existing snackbar/toast-style component existed. Deliberately NOT a Cupertino/Material platform branch — neither platform's native transient-message UI is being reached for directly, and a plain rounded card reads as "the app's own" chrome on both platforms, same reasoning `AppTextField` already applies to genuinely platform-neutral widgets. Self-manages its own `OverlayEntry` lifecycle (inserted on `show`, removed on expiry or on Undo). Uses `Overlay.of(context, rootOverlay: true)` — the app's outermost Overlay, not the nearest enclosing one — since this toast is shown immediately after a provider write that rebuilds the calling screen, and the root Overlay is the more robust target for anything meant to outlive that specific screen.
6. **Undo removes the whole series for a confident parse that also matched recurrence**, not just the one instance — `createTask` materializes a whole series immediately when a `RecurrenceRule` is supplied, so `AppUndoToast`'s undo calls `deleteTaskSeries` (not `deleteTask`) when the created task `isRecurring`.

**[Quick Capture parser] Follow-up: undo bug fix, expanded vocabulary (bare times/durations, weekday groups/ranges, category detection).**

1. **Real bug fixed: Undo never actually removed the created task.** `onUndo`'s closure re-read `ref.read(taskListProvider.notifier)` at the point it was CONSTRUCTED — but that's after `Navigator.of(context).pop()` had already disposed the sheet's own `State`, and `ref` on a disposed `ConsumerState` cannot be used. Fixed by closing over the `notifier` reference captured at the very top of `_submit()`, well before any disposal — a plain Riverpod `Notifier` instance's lifetime is tied to the provider container, not to the particular widget that read it, so holding onto it across the sheet's own teardown is safe.
2. **Bare 24h time (no am/pm), e.g. "11:00", is now a valid date/time anchor** — same confidence grant as an am/pm form. When both an am/pm form and a bare-24h reading could apply to the same digits, the am/pm form is preferred (tried first in the shared clock-time pattern).
3. **Bare duration, no leading "for", e.g. "2h", "90m", "1h5m"/"1h 5m"** (space optional) now extracts a duration on its own — same confidence rule as before: still not confident with no anchor present, matching the existing "for 30 mins" form's own behavior exactly. The explicit "for ..." form is tried first and wins whenever both could match the same digits.
4. **Recurrence vocabulary expanded**: `everyday` (daily); `weekdays`/`every weekday` (weekly, Mon-Fri, no "every" required for the bare form); `weekend`/`weekends`/`every weekend` (weekly, Sat-Sun, same bare-form rule); a weekday RANGE — `mon-fri`, `mon to fri`, `monday to friday` (abbreviated or full names, hyphen/en-dash/"to" as the separator) — expands inclusive-range to a weekly rule (confirmed directly: no "every" prefix required for the range form, matching how "weekdays" alone already doesn't need one); `every <weekday>` now also accepts the 3-letter abbreviation (`every fri`), not just the full name.
5. **Category detection**: a category name (`work`, `personal`, `health`, `admin` — `general` excluded, since it's already the default for "nothing matched") appearing anywhere in the text sets the created task's category and is removed from the title, confirmed directly. Unlike duration/recurrence, a detected category is surfaced regardless of [confidence] — it carries no scheduling ambiguity, so there's no reason to withhold it from a non-confident (plain-capture) result the way duration/recurrence are withheld. Highlighted with a 4th token color: the PERSONAL category's own icon color (deliberately different from duration's WORK-category color, so the two never coincide).

**[Task Detail] Real bug fixed: editing an already-scheduled task's Repeats panel silently did nothing on Save — the live flow never had the recurrence-editing logic its own retired sibling form already had.**

1. **`_TaskDetailFlowState` (the real, live edit flow — `showTaskDetailSheet`) now seeds `_repeats`/`_selectedDays`/`_wasRecurring` from `widget.task` on open**, and its `_save()` runs the same three-way branch `_EditScheduleFormState` already had (start / change / disable a series), instead of unconditionally calling plain `updateTask`. Ported directly from `_EditScheduleFormState` — the two forms had duplicated, not shared, this logic, and only one of them ever got the fix when recurrence-editing was first built. See ERROR_LOG.md for the full root-cause account, including how a fully-passing 6-test suite against the OTHER (unreferenced) form masked this for as long as it did.
2. **`edit_schedule_repeats_test.dart` retargeted at `showTaskDetailSheet`, not `showEditScheduleSheet`** — the real live entry point, not the retired backup form. All six tests needed `tester.ensureVisible` added before each tap: the live schedule stage is a genuine `SingleChildScrollView` (the retired form's fields all fit in the default test viewport without scrolling), so a bare `tap()` could miss its target as "off-screen" on the live form even though the same tap worked fine against the old one.
3. **`showEditScheduleSheet`/`_EditScheduleForm` themselves are left untouched, still present as a backup** — deleting genuinely-dead code, or further consolidating the two forms into one shared implementation, is a separate decision from this bug fix and wasn't asked for here. Flagged as worth doing eventually (the duplication is exactly what let this bug happen), but out of scope for a targeted fix.

**[Loading spinner] Save/create buttons show a spinner while the write is in flight, to stop double-taps from double-creating/editing a task.**

1. **Scope, confirmed directly via AskUserQuestion: Task Detail (create + edit) and Quick Capture's Add button only.** The retired `_EditScheduleForm`/`_EditDetailsForm` (dev-scaffold-only, unreferenced from real UI — see the recurrence-editing bug above) deliberately did NOT get this treatment; no reason to invest in dead code.
2. **`AppButton` gained an `isLoading` flag, not a separate loading-button widget.** Keeps the adaptive Cupertino/Material branching in one place per CONSTITUTION.md design principle 4, and every existing call site that doesn't pass it is unaffected (defaults `false`).
3. **Loading is visually distinct from plain-disabled, not the same washed-out grey.** A disabled button reads as "you can't do this"; a loading button just did exactly what was asked and is busy following through — so the fill/foreground keep their normal enabled colors, and only the label swaps for a small spinner (`CupertinoActivityIndicator` on iOS/macOS, `CircularProgressIndicator(strokeWidth: 2)` elsewhere), sized off the label's own `fontSize` so the button's height doesn't jump when it swaps in.
4. **`isLoading` is only the visual half — the actual re-entrancy guard is a `bool _isSaving`/`_isSubmitting` flag on the calling `State`, checked synchronously at the top of the async handler.** A second tap can land before the button visually re-renders as disabled, so the flag (not the widget's disabled-ness) is what actually stops a second write. Wrapped in `try/finally` so a thrown error still resets the flag — the button never gets stuck spinning forever with no way to retry.
5. **Widget tests that tap a button which can trigger this state can no longer call `pumpAndSettle()` immediately after the tap** — a `CircularProgressIndicator`/`CupertinoActivityIndicator` animates every frame for as long as `isLoading` is true, so `pumpAndSettle()` (which waits for zero newly-scheduled frames) hangs until it times out. Both affected test files' shared `_tapAndSettle` helper now does a single bounded `pump()` immediately after the tap, then the existing real-I/O delay, then a final `pumpAndSettle()` once the save has actually finished. Full mechanism in ERROR_LOG.md.

**[Voice dictation] Tap-to-talk mic button on Quick Capture, using on-device speech-to-text.**

1. **New dependency added, flagged and confirmed first per the standing rule: `speech_to_text: ^7.4.0`.** Verified publisher, 1.62k likes/150 pub points/505k weekly downloads, published ~3 months prior to adding — chosen because it wraps `SFSpeechRecognizer` (iOS) and Android's `SpeechRecognizer` behind one Dart API, fitting the single-codebase principle, rather than writing two platform channels by hand. Android `minSdk` 24 and `compileSdk`/`targetSdk` (Flutter defaults, 35+) both clear the package's stated floors (SDK 21 min, `compileSdkVersion` 31+).
2. **Not new parsing logic — the exact same pipeline as typed input.** Dictated text is written into the same `_titleController` `TextEditingController` Quick Capture already uses, then goes through the identical `parseQuickCapture` → `taskListProvider.createTask`/`captureTask` path on Add. No separate voice-specific code path exists past the transcript landing in the field.
3. **Highlighting only applies to the FINAL transcript, not progressively during dictation** — confirmed directly rather than assumed. `speech_to_text`'s `onResult` callback fires repeatedly with interim (non-final) results that can still change words mid-recognition; only `result.finalResult == true` writes into `_titleController.text`, so the live token-highlighting `_QuickCaptureTextEditingController` already does per keystroke never has to fight with a still-changing partial transcript.
4. **Mic/speech-recognition permission requested lazily, on first tap of the mic button** — same pattern as `NotificationService.requestPermissionIfNeeded` (Phase 6): no prompt at app launch, first real use only. Denial leaves the mic button inert (the `_toggleListening` call simply returns without entering the listening state); typing still works unconditionally, so the form never dead-ends on a denied permission.
5. **New adaptive component: `AppMicButton`**, not a repurposed `AppIconButton` — a mic button needs a second visual state ("actively listening") that a plain icon button's binary enabled/disabled doesn't model. Reuses `AmbleTheme.colorTaskAlert` (already used for Quick Capture's own recurrence-token highlight) as the "listening" fill color, rather than introducing a new token, per design principle 5 (a value used twice becomes a token — here, reusing an existing one rather than minting a near-duplicate).
6. **Silence timeout (`pauseFor`) explicitly set to 5 seconds, confirmed directly** — the package's default (unset `pauseFor`) falls back to a short, platform-chosen pause (a couple of seconds on both iOS and Android), which was cutting dictation off mid-sentence on a natural pause (e.g. "Call client... tomorrow at 10:30"). Set via `SpeechListenOptions(pauseFor: Duration(seconds: 5))` on the `listen()` call.

**[Timeline] Day view's rendered height floored at the viewport height, not left to shrink with task count.**

`_DayTimeline`'s scroll range (`_visibleRange`) is deliberately dynamic — earliest task minus 30min to latest task plus 30min, a prior confirmed decision (see the entry above on connector lines/dynamic scroll range) — so a day with few tasks produces a short `contentHeight`. Reported directly: with one task, the timeline surface looked "cut off," extending only as far as the task content did rather than filling the screen. Confirmed via AskUserQuestion: floor the rendered height at the viewport's available height (not revert to a fixed calendar-day range) — `_DayTimelineState.build`'s `SingleChildScrollView` is now wrapped in a `LayoutBuilder`, and `dayHeight` is `math.max(contentHeight, constraints.maxHeight - theme.spacingMd * 2)` (the padding subtraction accounts for the scroll view's own vertical padding, which is inside the measured viewport but outside the `SizedBox` being sized). The dynamic range itself (and its effect on scroll bounds, hour markers, "now" positioning) is unchanged — this only stops the rendered content from being SHORTER than the screen, never taller than what the dynamic range would already produce for a busier day.

**[Dev tooling] New `core/dev_config.dart` — runtime-toggleable, in-memory dev scratch config, distinct from both `FeatureFlags` and real `PreferencesRepository` settings.**

Requested directly: "some dev config place for stuff" to try out layout variants (initially: two task-capsule text layouts — stacked vs. inline — plus hiding the status-icon row and the duration suffix) without a rebuild. Three genuinely different existing mechanisms were available and each was the wrong fit on its own: `FeatureFlags` (`bool.fromEnvironment`) is compile-time and gates real *product* features that ship sometimes-on/sometimes-off per build; `PreferencesRepository`/Hive is real, persisted, user-facing settings (`PreferenceKeys`); neither fits "flip a value live while comparing options, never ship to a real build."

`DevConfig` is a new small file of plain `@Riverpod(keepAlive: true)` notifiers (`DevTimelineTaskTextLayout`, `DevTimelineTaskIconsVisible`, `DevTimelineTaskDurationVisible`) with in-memory-only state — no `PreferencesRepository` write, so it resets on every app restart, deliberately. Same `kDebugMode` gating precedent as the Settings screen's existing "Developer" section (see the earlier entry on the debug-only splash reset): every read site is a plain field default that equals current shipped behavior (`stacked`/true/true), and the ONLY place these providers' `set()` is ever called is inside `SettingsScreen`'s `if (kDebugMode)` block — so a release build's behavior is byte-identical to before this file existed, and the whole Developer panel (including the new controls) dead-code-eliminates the same way the splash-reset button already does.

Wired into `TaskCapsuleBlock` as three new constructor params (`textLayout`, `iconsVisible`, `durationVisible`, all defaulting to current behavior) read by `_DraggableTaskBlockState.build` in `timeline_screen.dart` (already a `ConsumerStatefulWidget`, so no new prop-threading chain was needed beyond that one read site). The `inline` layout uses a single `Text.rich` with two `TextSpan`s (time+duration, then title) rather than two separate `Text` widgets, so it shares one line and one `overflow: TextOverflow.ellipsis` — the two spans can't independently truncate the way two stacked `Text` widgets do.

Explicitly not a `TaskCapsuleBlock`-only mechanism — `DevConfig` is meant to be the one place future "try N options live" dev toggles go, not just this one.

**[Timeline] Task pill's badge now shows the category's emoji, not the monochrome `.icon` glyph — reverses an earlier explicit decision, confirmed via AskUserQuestion first.**

The category picker chips (create/edit flow) have shown `TaskCategory.emoji` since an earlier session, deliberately kept separate from `.icon` — that session's own decision recorded `.icon` as "retained for the places a monochrome vector that takes a tint from its context is still the right primitive," naming the pill badge specifically as that case (`iconColor` tints it for contrast against the pale category fill). Requested directly to reverse this: the pill badge should match what the picker already shows during creation.

Since an emoji carries its own fixed color (unlike a vector icon), the `iconColor` local in `TaskCapsuleBlock` — computed purely to tint the old `Icon` for contrast against the pale category fill in the skipped/completed cases — became genuinely dead once the badge switched to `Text(task.category.emoji, ...)`; removed rather than left unused, along with the doc comment explaining its old purpose. `CompletionCheckbox`'s ring color (a separate use, reads `theme.categoryIconColors` directly, not the removed local) is unaffected.

One golden regenerated: `test/features/timeline/timeline_capsule_preview.png` (0.25% pixel diff — exactly the badge glyph swap, nothing else moved). Per the visual-verification guardrail, inspected the regenerated PNG directly: emoji render as placeholder squares in the golden-test harness's font (a known Flutter widget-test limitation — the test font has no emoji glyph coverage, same substitution the OLD `Icon` glyphs already showed in this same golden), not a real rendering defect; layout/position/color are what the golden actually verifies and those are correct. On a real device (real system font), the emoji renders normally.

**[Category] `TaskCategory` (fixed 5-value enum) converted to a persisted, user-extensible `Category` entity — full build, including a new "Add category" modal.**

Confirmed as full scope (not a UI-only mockup, not a design-only pass) rather than assumed: adding a real "Add new category" flow only makes sense once categories are user-creatable data, not a closed Dart enum, so this was sized and scoped as new architecture before any code — same "own model, own box, own repository" shape as `TrackedBehavior`. Full model/repository/provider shape, the deprecation approach for the old `Task.category` field, the seed/backfill migration, and the create-only v1 scope are all recorded in `docs/CONSTITUTION.md`'s "Category" section — this entry covers the decisions CONSTITUTION.md doesn't, plus what got fixed after the initial build.

1. **`categoryId` (new, `@HiveField(15)`) supersedes `Task.category` (old enum, `@HiveField(8)`) — the old field is deprecated in place, never removed/renumbered/repurposed.** Non-negotiable per this codebase's own established Hive field-index-stability rule (see the `notificationsEnabled`-addition precedent this entry cites, and ERROR_LOG.md): renumbering or reusing a retired `@HiveField` slot silently corrupts/misreads already-installed users' persisted data. `Task.create`/`TaskList.createTask` require `categoryId` and no longer accept the old `category:` enum parameter at all — the deprecated field is disk-compat-only from here on.
2. **Seed/backfill migration uses fixed, hardcoded UUIDs for the 5 built-ins (`BuiltInCategoryIds`), not randomly generated ones** — required for idempotency (re-seeding must not duplicate rows) and so the backfill step has a stable, known target to resolve each legacy `TaskCategory` value onto. Gated by a new `PreferenceKeys.categoriesSeeded` flag, runs once at launch from `main.dart`, mirroring `TaskList.materializeDueRecurrences()`'s own "launch-only, idempotent" precedent.
3. **12-color palette: single saturated swatch per color, not the built-in categories' pale-tint + saturated-icon-color pair.** Confirmed via AskUserQuestion — a user-picked palette of 12 is closer to "pick a hue" than the app's own small, meaning-bound 4-category taxonomy the tint/icon split was originally solved for; doubling that treatment to 12 colors (24 hand-tuned values, light+dark) was deliberately avoided as disproportionate design work for this scope. New Tier 1 primitive `ColorPrimitives.categoryPalette12` — 12 OKLCH swatches, same `L=0.62, C=0.15` held constant, hue stepped 30° apart around the full wheel (15°, 45°, 75°, 105°, 135°, 165°, 195°, 225°, 255°, 285°, 315°, 345° — red through pink/rose), mechanically distributed rather than hand-tuned per swatch, matching the original 4-category hue-spacing reasoning (Phase 3) generalized to 12 evenly-spaced steps. Exposed as `AmbleTheme.categorySwatches`; `Category.colorToken` is a plain `int` index into it (not a Hive-adapted enum), matching how the codebase already indexes into fixed color lists elsewhere (e.g. `OverlapClusterBlock`'s column index).
4. **v1 CRUD scope: create + list only, no edit or delete** — confirmed via AskUserQuestion over also building an edit (rename/recolor) modal. `CategoryRepository` has no `deleteCategory` method at all; deleting a category referenced by existing tasks (orphaned `categoryId`) is a real, deliberately unresolved question, same posture CONSTITUTION.md already takes on `TrackedBehavior` deletion.
5. **"+ Add new" entry point on BOTH live category pickers** (`TaskCategoryModal`, the primary create-flow picker, and `TaskNameCategoryModal`, the older combined Name+Category modal still used by the edit flow's `_EditDetailsForm`) — confirmed via AskUserQuestion over primary-picker-only, so a user editing an existing task can add a new category too, not just when creating one.
6. **New "Add category" modal is a bespoke header, not a promoted `_StepScaffold`.** `task_detail_sheet.dart`'s `_StepScaffold` (the shared chrome its own multi-step wizard reuses) is private to that file; rather than promote it to a shared/exported widget for one external caller, the new modal builds its own simpler header — it's a single flat 3-section form (name / color / emoji), not a multi-step wizard, so the step-flow chrome would have pulled in more machinery than needed and risked that 2900+-line file's existing callers for no real benefit.
7. **Export/import gains full `Category` support** — the backup JSON envelope now carries a `categories` array (`Category.toJson`/`fromJson`, hand-written like `Task`'s own, not codegen) alongside `tasks`, and `BackupService.parseImportFile` returns a new `ParsedImportFile{tasks, categories}` wrapper instead of a bare `List<Task>`. Deliberately NOT following `TrackedBehavior`'s existing precedent of having no export support at all — an un-exported custom category would leave a restored device's tasks with dangling `categoryId`s, a real regression `TrackedBehavior`'s gap doesn't currently cause (nothing depends on it being exported yet).

**Post-build fixes** (found and fixed in the same session, after the two implementing background agents hit the session's API rate limit mid-work and had to be resumed manually):

8. **Widget tests that mount anything reading `categoryListProvider`** (the Task Detail sheet, either category picker) **need a real, seeded `Category` Hive box in their setup — a plain provider override alone doesn't stand in for one**, unlike `taskRepositoryProvider`, because `categoryRepositoryProvider` itself calls `Hive.box<Category>(categoryBoxName)` internally. New shared test helper `test/support/seeded_category_box.dart` (`openSeededCategoryBox`) opens a uniquely-named box and seeds the same 5 built-ins, deliberately bypassing the real seed function's `PreferenceKeys.categoriesSeeded` gate and `Task` backfill loop (launch-once production concerns unrelated to what these tests check). Wired into every widget-test file that renders `_TaskDetailFlowState`/`TaskCategoryModal`/etc.: `edit_schedule_repeats_test.dart`, `create_flow_initial_modal_test.dart`, `exit_confirmation_test.dart`, `task_action_sheet_remove_test.dart`.
9. **`AddCategoryModal`'s own widget test hit the same "tap Save then `pumpAndSettle()` hangs on the spinner" class of bug this session already hit and fixed twice for `task_detail_sheet.dart`'s Save button — but the FIRST fix attempt reproduced a subtly different, also-broken variant.** The working pattern (`edit_schedule_repeats_test.dart`'s `_tapAndSettle`) wraps `tap`, the real-I/O delay, AND the final `pumpAndSettle()` all inside ONE `runAsync` block. A first fix pass here split them — `tap`/`pump` outside `runAsync`, only the delay inside, `pumpAndSettle()` outside again — which raced across real-zone/test-zone boundaries and hung indefinitely (confirmed by two separate ~2+ minute stalls, not by reasoning about it). Fixed by matching the established working shape exactly: everything in one `runAsync` block. **Lesson, worth restating**: this exact bounded-pump pattern has a specific required shape (one `runAsync`, tap+pump+delay+settle all inside it) — a plausible-looking variant that moves pieces in/out of the `runAsync` boundary is not equivalent and can silently hang rather than fail fast.
10. **`seedBuiltInsAndBackfillIfNeeded` had all 5 built-in categories default to `colorToken: 0`** (health/work/personal/admin all identical to general) — a real bug, not a placeholder left intentionally; caught while reviewing the seed function for an unrelated test fix, not by a failing test (nothing asserted on the specific built-in color values). Fixed to distinct indices 0–4 (general=0, health=1, work=2, personal=3, admin=4).

**Correction to this entry**: at the time this was written, the `Zone` model/repository/provider/service files present alongside the Category work were assumed to be unrequested scope creep from the Category-building agent, and flagged as such. That assumption was wrong — a separate, explicit work order for exactly this Zone data-layer build (model + repository + providers + zone-overlap check + feature flag, "additive, inert data layer" only, no UI, mirroring `TrackedBehavior`'s Phase 10 treatment) had been issued directly; the Zone code was correctly-scoped, deliberate work, not an agent going off-script. See the dedicated Zone entry below for the real account. Left here rather than deleted, so the correction itself is on record.

**[Zone] Data-layer build — model, repository, providers, zone-overlap check, feature flag. No UI. Same "additive, inert" treatment `TrackedBehavior` got in Phase 10.**

Explicit work order, scoped narrowly on purpose: build only what CONSTITUTION.md's Zone design section already specified, non-recurring only, with capacity-exceeded behavior and zone recurrence explicitly NOT resolved this pass (both real, still-open forks named in that same design section).

1. **Time-of-day representation: `startMinutes`/`endMinutes` as plain `int`s (minutes since midnight, 0–1439/1–1440), not `TimeOfDay`.** Flagged rather than assumed — `TimeOfDay` has no Hive adapter, and the codebase already treats "hours × 60 + minutes" as its boundary representation for time-of-day elsewhere (`AppSegmentedTimeField`). `durationMinutes` is a derived getter (`endMinutes - startMinutes`), never stored, so it can never drift from the two values that define it, per CONSTITUTION.md.
2. **`Task.zoneId` appended at `@HiveField(16)`** — the next free slot after `categoryId` (15, from the same session's Category work) and `notificationsEnabled` (14). Confirmed the full pre-existing test suite passes unmodified after this addition before proceeding (same risk-management posture as every prior `Task` field addition — Phase 10's `behaviorId`/`actualAmount`, Phase 11's `recurrenceId`/`recurrenceRule`, this session's own `categoryId`).
3. **`ZoneRepository`/`HiveZoneRepository` mirror `TrackedBehaviorRepository`'s exact shape** (`getAll`/`getById`/`save`/`delete`), new Hive box `zones` opened in `main.dart` alongside the existing boxes.
4. **`ZoneList` Riverpod notifier is `keepAlive: true`, unread by anything** — same Phase 1 `autoDispose`-caused-a-real-bug precedent as every other app-level CRUD notifier in this codebase. Nothing currently watches it; built now so the data layer is complete and testable ahead of any UI, same posture `TrackedBehaviorList` had before its own UI existed.
5. **`zonesOverlap` is a genuinely separate function from `overlapsExistingTask` (`overlap_checker.dart`), not a reused/generalized version of it** — confirmed in scope per CONSTITUTION.md ("Zones cannot overlap each other... needs its own check"). `Zone` and `Task` don't share a supertype, and the two functions reason over different shapes (minutes-since-midnight with no date, vs. a specific `DateTime` + duration on a specific day) — genuinely different domains, not the same logic wearing two names. Half-open interval semantics, matching `overlapsExistingTask`: a zone ending exactly when another starts does not count as overlapping.
6. **`calculateZoneCapacity` is built (sum of assigned tasks' durations vs. zone duration) but deliberately unconsumed** — a pure, read-only calculation only, per the explicit non-goal. No UI or validation logic calls it. What happens when it reports over-capacity (block/warn/silent overflow) stays exactly as undecided as CONSTITUTION.md already recorded it.
7. **Feature flag `FeatureFlags.zoneEnabled`, same `bool.fromEnvironment('zone')` mechanism as `trackedBehaviorEnabled`** — checked for consistency with the existing pattern rather than introducing a second gating mechanism. Default off; nothing reads it yet, since no UI exists to gate.
8. **Zone recurrence and zone drag/cascade were not built, not guessed at — confirmed absent, not just omitted silently.** Grepped the new Zone files for any recurrence/series concept: none exists. `Zone` has no `recurrenceId`/`recurrenceRule`-equivalent field, and no drag/cascade code touches `Zone` at all.

**Verification, beyond the standard analyze/format/test gate**: ran the full pre-existing suite specifically to confirm the `Task.zoneId` addition (the highest-risk step, same reasoning as every prior `Task` model change) broke nothing, before writing any Zone-specific tests. Added `test/shared/repositories/hive_zone_repository_test.dart` (CRUD + constructor-invariant coverage, mirroring `hive_tracked_behavior_repository_test.dart`'s shape) and `test/shared/services/zone_overlap_checker_test.dart` (overlapping / nested / identical / adjacent-not-overlapping / clearly-separate / order-independence cases) and `test/shared/services/zone_capacity_test.dart` (sum-of-assigned, zone-only-task contributes nothing, over-capacity, empty-zone cases) — none of these existed when the data layer itself was first built; added in a later session while resuming/verifying this work.

App confirmed unchanged from a user's perspective: no UI reads any Zone provider, `FeatureFlags.zoneEnabled` defaults off, and the full existing test suite (which exercises every real screen) passes unmodified.

**[Zone] First UI: Settings → Zones → list → add/edit. `FeatureFlags.zoneEnabled` now actually gates something.**

Requested directly: a Zones section in Settings navigating to a dedicated list page (not an inline summary), with add/edit modeled on the real task-creation modal.

1. **`task_detail_sheet.dart`'s private `_StepScaffold`/`_HeaderCircleButton` promoted to a new shared, public widget: `lib/core/widgets/app_step_scaffold.dart` (`StepScaffold`/`HeaderCircleButton`).** This is the actual chrome "matching the add-task modal UI" requires — colored header banner, close button, primary button pinned to the bottom with a loading state — not a look-alike rebuild. All 3 internal `_StepScaffold(...)` call sites and the 1 standalone `_HeaderCircleButton` usage inside `task_detail_sheet.dart` were repointed at the public versions with zero behavior change (confirmed via the full test suite passing unmodified, including an isolated rerun of `edit_schedule_repeats_test.dart` — the file most directly exercising `_TaskDetailFlowState._save`, the highest-risk consumer of this chrome). This reverses the reasoning behind `AddCategoryModal`'s own choice NOT to promote `_StepScaffold` (a bespoke header, because that modal is a small bottom sheet, not near-full-screen) — Zone's add/edit was confirmed to need the real near-full-screen presentation, so promoting was the correct call here specifically.
2. **Zone add/edit presentation: near-full-screen slide-up (matching real task creation), not a bottom sheet (matching `AddCategoryModal`'s precedent).** Confirmed via AskUserQuestion — the two existing precedents in this codebase genuinely diverge on this exact question, so it wasn't assumed either way. `showZoneFormScreen` pushes via the same `PageRouteBuilder` shape (`opaque: false`, scrim barrier, slide-up transition) as `task_detail_sheet.dart`'s own `_pushDetailRoute`.
3. **Zone list is a real pushed page (`MaterialPageRoute`), not a sheet** — explicitly requested ("go to page where you see zones list"), and no existing pushed-page precedent existed anywhere else in this single-bottom-nav-tabs app (every other screen is a bottom-nav tab or a sheet/near-full-screen modal) — `MaterialPageRoute` under the root `MaterialApp` was the simplest correct choice, not a new pattern invented for this.
4. **Zone-to-zone overlap is checked client-side in the form, before Save, using the already-built `zonesOverlap`** — the zone being edited is excluded from the comparison set (editing a zone's own time against itself would trivially "overlap"). Rejection surfaces via `StepScaffold`'s existing `errorMessage` slot (the same inline-message mechanism `task_detail_sheet.dart`'s own "Prevent overlapping tasks" rejection uses), naming the specific conflicting zone by title. This was already confirmed in scope by the Zone data-layer work order ("Zones cannot overlap each other... needs its own check") — this session is the first UI to actually enforce it.
5. **Settings entry mirrors `FeatureFlags.trackedBehaviorEnabled`'s gating shape** (`if (FeatureFlags.zoneEnabled) ...[ ]`, const-eliminated when off) **but not its inline-form-button shape** — Zones gets a summary line + a "Manage zones" button that navigates to the list page, per the direct request that this be a real page to browse/manage a list rather than a single focused create action.
6. **`AppSegmentedTimeField`'s masked digit-entry mechanics are NOT re-tested here** — the new widget test drives zone creation through `zoneListProvider.notifier.createZone(...)` directly rather than simulating keystrokes into the time fields, since that field already has its own dedicated widget tests (`app_segmented_time_field_test.dart`) and re-deriving its input mechanics in a second test file would be duplicate, brittle coverage of something already covered elsewhere.

Widget test added: `test/features/zones/zone_form_screen_test.dart` (Save disabled until name+valid-time-range are set, a created zone reaches `zoneListProvider`, editing pre-fills the name/title).

**[Zone] Ungated — `FeatureFlags.zoneEnabled` now defaults `true`, ships in every build including release.**

Requested directly, immediately after the UI landed and was verified. Changed from `bool.fromEnvironment('zone')` (defaults `false`, only reachable via `--dart-define=zone=true`) to `bool.fromEnvironment('zone', defaultValue: true)` — still overridable off for testing (`--dart-define=zone=false`) but on by default now, same shape `trackedBehaviorEnabled` would need if it were ever ungated the same way. `docs/SCOPE.md`'s Zone entry rewritten from "design-only spike, not started" to reflect current reality: implemented and live, with the three still-open forks (capacity enforcement, per-occurrence recurrence, drag/cascade) carried forward as still-open, plus a note that no UI yet exists for *assigning* a task to a zone (only creating/editing zones themselves). Notification scheduling is now horizon-capped (14 days) and never awaited on a write path — forced by Android's 500-concurrent-alarm limit.**
Full incident writeup in `docs/ERROR_LOG.md`. Three choices worth recording here, all confirmed with the user before building (the alternative offered was fixing only the blocking, leaving notifications broken):

**The horizon is 14 days, deliberately shorter than the 8-week materialization window.** Tasks are still persisted 8 weeks ahead — the Timeline needs to render them — but only the near two weeks hold an actual OS alarm, topped up on each launch. This is what bounds the alarm count: previously a single daily recurring series registered ~56 alarms, and a handful of series exceeded Android's hard cap of 500, after which *every* `zonedSchedule` threw. 14 days is far enough that a normally-used app tops the window up long before a task falls due, and short enough to keep the count well under the cap even with many series. The trade-off, accepted explicitly: a task scheduled 3+ weeks out has no OS alarm registered at the moment it's created — it gets one on a later launch. Not a behavior change any user can observe unless the app is never opened for weeks, in which case the alarm was going to be unreliable regardless.

**`refreshScheduled` cancels ALL notifications before re-registering.** Considered tracking which ids fell out of the horizon and cancelling those individually; rejected as strictly worse — it needs bookkeeping that can drift, and it can't help an install that has *already* hit the cap (the stale 500 are still registered, so nothing new can schedule). `cancelAll()` then re-register is idempotent, self-healing, and needs no persisted state about what was scheduled last time. Cost is a burst of platform calls at launch, which is why it's not awaited before `runApp`.

**Notification sync is fire-and-forget on all ten `TaskList` write paths, not awaited.** The task is already persisted before the sync runs, and `_syncNotificationSafely` already catches and logs its own failures — so awaiting it only ever meant "make the user wait for something we've decided is allowed to fail." This is a generalization of the earlier post-Phase-9 fix (which made a notification failure not *abort* the save); this makes it not *delay* the save either. The principle: a side effect the code treats as optional must not sit on a user-visible critical path.

## [2026-09-02] Zone recurrence + notify

**Modal-sizing/header-alignment fix (Task 1 of this session): already in place, not redone.** `AppSheetSize` (`small`/`half`/`nearFull`) on `AppSheet.show`, `add_category_modal.dart` → `nearFull`, `task_category_modal.dart` → `half`, and the left-aligned `Expanded(Text(...))` + trailing `TextButton('+ Add new')` header fix on both `task_category_modal.dart` and `task_name_category_modal.dart`, were all already landed in the working tree before this session started. Verified by reading all four files directly and confirming `flutter analyze` is clean on them — no code changes were needed for this task.

**Zone recurrence: simple only, confirmed directly by the user.** `Zone` now embeds the same `RecurrenceRule` value object `Task` uses (`frequency`/`interval`/`daysOfWeek`), reused as-is — one rule, same start/end time on every occurrence. This is explicitly NOT the harder "per-occurrence-adjustable times, materialized instances" version CONSTITUTION.md's Zone section flags as a genuine open fork (a Monday zone at 07:00-08:00 and a Wednesday zone at 07:30-08:15 in one series) — that stays deferred. CONSTITUTION.md's Zone section is updated to name which version is now implemented so a future session doesn't conflate the two. `@HiveField(5)` on `Zone`, nullable, null meaning non-recurring (unaffected default) — confirmed safe by running the full 335-test pre-existing suite immediately after regenerating `zone.g.dart`, before writing anything else, per this repo's established risk discipline for every prior field addition.

**Zone notify: an alert fires at the zone's start time, confirmed directly.** Same "Notifications" `AppSwitch` shape `task_detail_sheet.dart` uses for `Task.notificationsEnabled`. `Zone.notificationsEnabled` at `@HiveField(6)`, defaulting `true` (opt-out, not opt-in) — matches a newly created `Task`'s own default, so a newly created zone behaves the same way.

**`NotificationService.scheduleForZone`/`cancelForZone`: one-shot next-occurrence, not a true recurring OS alarm.** A zone has no absolute `DateTime` the way a task does — recurring TASKS work because each materialized instance is a real persisted row that gets its own individual `scheduleForTask` call, and Zone deliberately has no materialization step this pass (see the recurrence decision above). Building genuine recurring OS-level scheduling (`DateTimeComponents.dayOfWeekAndTime`) for a Zone was judged not-confidently-low-risk for this session, so the simpler version shipped instead: `scheduleForZone` resolves the rule (or the bare `startMinutes` for a non-recurring zone) to a single concrete `DateTime` — the soonest upcoming occurrence, today if it still qualifies, otherwise the next matching day within a 7-day lookahead — and schedules ONE `zonedSchedule` call for it, not a repeating alarm. This goes stale after that one occurrence fires; the mitigation is that the Zone form's `_save()` re-resolves and re-schedules on every save, the same way a user re-opening and re-saving a zone would refresh it. **Known gap, flagged rather than silently accepted**: unlike `Task`, there is no launch-time `refreshScheduled`-equivalent wired into `main.dart` for zones — a zone's one-shot alert is only ever (re)registered when its form is saved, never topped up automatically at app launch the way `TaskList.refreshScheduledNotifications()` does for tasks. This was deliberately left out of scope (the work order named model/service/UI/tests, not `main.dart`), but means a zone notification, once it fires or is lost (app reinstall, alarm cleared), stays gone until the zone is next edited and saved. Worth a dedicated follow-up if zone notifications turn out to matter in practice.

**Non-recurring zone whose time has already passed today: no-ops, does not roll to tomorrow.** Mirrors exactly how `scheduleForTask` already treats any past `scheduledAt` — no-op, not "reschedule for the next sensible time." Re-deriving "next occurrence for a non-recurring thing" is exactly the kind of recurrence-adjacent scope creep CONSTITUTION.md's still-deferred per-occurrence-times fork warns against building at this depth, so the simpler, already-established convention wins.

**Zone notification id: separate hash input (`"zone:" + id`), not the bare id.** Task and Zone ids are both client-generated UUIDs hashed into the same 31-bit space via `taskId.hashCode & 0x7fffffff` — the existing `_notificationId` doc comment already accepts a theoretical Task-vs-Task collision as negligible given UUID v4's collision space. Rather than accept that same risk a second time for a Task-vs-Zone collision (which is avoidable at near-zero cost), `_zoneNotificationId` prefixes the hash input so a Task and a Zone can never land on the same platform notification id even if their raw UUIDs happened to hash equal.

**`_RecurrencePanel` (task_detail_sheet.dart's 7-day-chip Repeat UI): duplicated locally in `zone_form_screen.dart` as `_ZoneRecurrencePanel`, not promoted to a shared `core/widgets/` component.** Judgment call, flagged rather than made silently. Considered promoting the whole panel the way `_StepScaffold` → `StepScaffold` was promoted earlier this session (real precedent for "extract when a second real caller needs it"), but `_RecurrencePanel` is a small (~35-line), stateless, StatelessWidget-plus-two-pure-functions unit with no complex internal state — genuinely small enough that duplicating it costs less than the churn of carving a second public widget out of an already 2900+-line file (new export surface, a second set of call sites to keep in sync, doc-comment cross-references to maintain both directions). The two pure helper functions (`_recurrenceRuleFromSelectedDays`/`_selectedDaysFromRecurrenceRule`) are duplicated verbatim (same rule: all 7 days = daily, smaller selection = weekly with that `daysOfWeek` set) since they're each ~10 lines and file-private in the original, so importing them wasn't even an option without promoting. Revisit if a third caller ever needs the same panel — two duplicates is an accepted trade, three would cross into "should have been promoted."

**Wheel time picker for Zone: `AppSegmentedTimeField` stays primary, `AppWheelTimePicker` added as the alternate entry via a trailing `AppFieldActionButton` — verified against `task_detail_sheet.dart`'s real pattern, not assumed.** Read `task_detail_sheet.dart`'s own `_openStartTimeModal`/`_openDurationModal` directly before deciding: the real task-creation flow keeps typed entry (`AppSegmentedTimeField`) as primary and opens a combined modal (there, `TaskStartTimeModal` — a typed-field-plus-wheel hybrid) via a trailing clock-icon `AppFieldActionButton`. Zone's form previously had `AppSegmentedTimeField` with no alternate at all. Rather than build a second `TaskStartTimeModal`-style hybrid (out of proportion for this task), Zone's Start/End fields each gained a trailing `AppFieldActionButton` opening `AppWheelTimePicker.show` directly — the bare titled wheel-picker sheet already built for exactly this "alternate browsing entry" purpose, per its own doc comment. This is wheel-as-alternate, not wheel-only or wheel-first as primary, matching the verified real pattern rather than the initial "time is set with wheels" phrasing, which described the alternate-entry AFFORDANCE, not a wheel-only redesign.

**`ZoneList.createZone` now returns `Future<Zone>` instead of `Future<void>`.** Needed so the form's save path can pass the created zone (with its freshly minted id) into `scheduleForZone` without a second read. Backward compatible: both existing call sites (`zone_form_screen.dart`, `zone_form_screen_test.dart`) only ever `await`ed the call without using a return value, so widening the return type breaks nothing.

**Follow-up, same session: closed the launch-time-refresh gap flagged above.** New `ZoneList.refreshScheduledNotifications()` (mirrors `TaskList.refreshScheduledNotifications`'s shape exactly — loop every zone, skip if `!notificationsEnabled`, call `scheduleForZone`), wired into `main.dart` right after the existing task-notification refresh, same fire-and-forget (`unawaited`, not awaited before `runApp`) discipline. Confirmed with the user directly before building rather than silently leaving the gap or silently fixing it — this was flagged as an explicit open question, not assumed either way. `flutter test` (345/345) and `flutter analyze` (2 pre-existing, unrelated warnings only) reconfirmed clean after this addition.

**[Zone] Rendered as a background block on the Spatial Task View (Timeline) — display-only, per CONSTITUTION.md's "Zone is metadata on a Task, not a container that owns it."**

Explicit work order, scoped narrowly: this is the Spatial Task View's own decorative treatment of Zone data, NOT the future Spatial Zone View (where Zone genuinely acts as a layout container) — that stays separate, out-of-scope future work. A task's spatial positioning/sizing logic is untouched; every persisted zone renders for its time range unconditionally (no filtering on whether any task references it via `zoneId`), since that kind of filtering would itself be Zone-view/container behavior.

1. **New Tier 1/2 color tokens: `ColorPrimitives.zoneBackground`/`zoneBackgroundDark` → `AmbleTheme.colorZoneBackground`.** Proposed and confirmed with the user directly before building, per the work order's own instruction not to treat this as small enough to decide silently. `oklch(0.97, 0.02, 240)` light / `oklch(0.28, 0.02, 240)` dark — a cool, very-low-chroma blue-grey. Hue 240 was chosen deliberately apart from every existing signal on the Timeline: the 4 built-in category hues (clay 32, ochre 153, periwinkle 216, berry 300) and `colorFreeWindow`'s zero-chroma neutral sand tone — a zone block can't be mistaken for a category tint or a free-window gap indicator. Chroma (0.02) is deliberately far below the pale category tints' own 0.055-0.075, so a zone block reads as sitting visually "behind"/"beneath" everything else rather than competing for attention. Confirmed via AskUserQuestion over a zero-chroma neutral alternative (which would have read closer to `colorFreeWindow`'s own family, undesirable given the two answer different questions — Zone = "this time range has meaning," free window = "nothing is scheduled here" — and must not visually conflate). Full `copyWith`/`lerp`/light/dark boilerplate updated in `semantic_theme.dart`, matching every other Tier 2 color token's exact shape.
2. **`ZoneBackgroundBlock` (new, `zone_background_block.dart`) positioned using the SAME time-to-pixel math the Timeline already uses for tasks** (`rangeStart`/`pixelsPerMinute`, threaded down as plain fields the same way `categoryById`/`showHourLabels` already are — `_DayTimeline`/`_DayTimelineState` stay non-Riverpod-aware). Inserted into the Stack's child list right after `TaskBoundaryMarkers` and before `_TimelineConnectors`/`FreeWindowBlock`/task capsules — Stack paints in list order, so this guarantees zone blocks render strictly BEHIND every task, with zero risk of a z-order regression on tasks (verified: no task-rendering code was touched at all). `IgnorePointer`-wrapped — purely decorative, never intercepts a tap meant for a task pill, unlike `FreeWindowBlock`, which is deliberately tappable for a different reason (opens create-task).
3. **Offset constant: `zoneBackgroundOffset = 12.0`, matches `theme.spacingIconTop` exactly — no new spacing token needed.** The existing spacing scale already has this exact value (`SpacingPrimitives.space4`) under a semantic name; flagged and confirmed this was an exact match before treating it as "good enough," not assumed. Deliberately its OWN named constant rather than reading `theme.spacingIconTop` directly at each use site, so (a) the top and left offsets are visibly the same value by construction, not two independent reads that could drift, and (b) a future change to `spacingIconTop` for its original (icon-badge-top) purpose doesn't silently also move every zone block on the Timeline. Applied as: `top`/`left` shift by `-zoneBackgroundOffset` from the strict time/column position, `width`/`height` grow by `+zoneBackgroundOffset` — the bottom-right corner stays at the strict position while the top-left moves earlier/further left, so the extra space reads as padding before the zone's real start rather than the whole block shifting uniformly (confirmed against the requested visual: "starts visually slightly earlier/further left than its literal position").
4. **Feature-flag gated the same way every other Zone UI surface is** (`FeatureFlags.zoneEnabled`, already defaulting `true` from the prior ungating session) — `TimelineScreen` reads `zoneListProvider` only when the flag is on, an empty list otherwise, so the flag-off path is a zero-cost no-op rather than a second code path to maintain.
5. **New widget test** (`zone_background_block_test.dart`): verifies the exact offset math (`top`/`left` = strict position − 12, `width`/`height` = strict size + 12), confirms `zoneBackgroundOffset` is literally 12.0, and confirms the block is `IgnorePointer`-wrapped. No golden/screenshot test added — this is a rendering-only, non-interactive block whose visual correctness was verified directly on a real iOS simulator (see PROGRESS_LOG.md for the screenshot comparison), matching how `FreeWindowBlock` itself also has no dedicated widget test, only its pure positioning-math function does.
6. **New dev scaffold** `zone_background_preview_main.dart` (seeds real tasks + categories + zones, matching `timeline_screen_main.dart`'s existing pattern but updated for the current `categoryId`-based model) — used to capture the on/off comparison screenshots, not part of the shipped app.

**Follow-up, same session: guaranteed a visual gap between two back-to-back zone blocks.** Reported directly: "there should always be a gap between zones even if one starts the time the other ends." Purely cosmetic per the user's own framing (confirmed via AskUserQuestion) — `zonesOverlap`'s half-open-interval save-time validation is UNCHANGED, two zones can still be *saved* exactly back-to-back; this only stops their *rendered blocks* from touching or overlapping.

Turned out to be a genuine (if latent) rendering bug, not just a missing nicety: with `zoneBackgroundOffset` extending each block's top/left `-12px` earlier than its strict start, two zones scheduled exactly back-to-back rendered as ACTIVELY OVERLAPPING blocks, not merely touching ones — the next zone's block reached backward past the previous zone's own strict end. Worked out algebraically before writing code (a block's bottom edge trimmed by only `gap` cannot produce positive clearance against a neighbor whose top edge extends `offset` in the opposite direction — the two adjustments fight from opposite sides of the same boundary): the correct fix recedes the TRAILING edges (bottom/right) by `offset + gap` rather than just `gap`, so `width`/`height` become `strict − zoneBackgroundGap` (not `strict + zoneBackgroundOffset − zoneBackgroundGap` as a first, wrong attempt produced). This guarantees exactly `zoneBackgroundGap` of clearance against ANY neighbor's own worst-case (offset-extended) leading edge, without either block needing to know the other exists — verified both by hand-checked arithmetic (`python3` scratch check, not shipped) and a new dedicated widget test asserting the exact back-to-back case.

New constant `zoneBackgroundGap = 4.0`, confirmed as an exact match to the existing `theme.spacingXs` (`SpacingPrimitives.space2`) token — same "check for an exact match before minting a new token" discipline `zoneBackgroundOffset` already established. Confirmed via AskUserQuestion (4px, not a different value) before implementing.

Updated `zone_background_block_test.dart`: the old "width/height grow by +offset" test was replaced (the old formula no longer describes the code), and a new test directly asserts the back-to-back scenario — two zones with `earlier.endMinutes == later.startMinutes` render with exactly `zoneBackgroundGap` of clearance between `bottomOf(earlier)` and `topOf(later)`.

## [2026-09-02] Spatial Zone View — Zone as a real layout container

**[Zone] Second Timeline display mode added, alongside (not replacing) the existing Spatial Task View.** Per the per-view-job design principle: rather than teaching the existing Task view a second rendering mode for Zone-as-container, a wholly separate `ZoneDayTimeline`/`ZoneContainerBlock` widget tree was built, switched in at `TimelineScreen`'s own top level by a new Settings toggle (`ZoneViewEnabledSetting`, `PreferenceKeys.zoneViewEnabled`, default `false`, same persisted-`bool` shape as `ShowHourLabelsSetting`). `_DayTimeline`/`ZoneBackgroundBlock` (the prior session's decorative-only Task view rendering) are completely untouched — verified both by code review (no edits to either file beyond the Settings-screen/TimelineScreen switch site) and a real on-device screenshot with the toggle off, compared against the prior session's own Task view screenshot.

1. **Containment precedence, confirmed directly, two-part:** (a) explicit `task.zoneId` wins outright, even when the task's own `scheduledAt` falls outside that zone's time range — no mismatch warning shown, the assignment is trusted at face value; (b) only when `zoneId` is null does a computed fallback apply (`scheduledAt` falling inside a zone's `startMinutes`/`endMinutes` window) — and that fallback is READ-ONLY for display: nothing in `resolveZoneContainment` (`shared/services/zone_containment.dart`) ever writes `zoneId` onto a `Task`, per CONSTITUTION.md's "Zone is metadata on a Task, not a container that owns it." A task matching neither path is "unzoned."
2. **Unzoned-task placement, confirmed directly:** NOT a separate flat "Unzoned" section, NOT hidden — an unzoned task renders exactly where the Task view would put it, at its own real spatial time position on the SAME outer time axis the zone containers are stacked on, interleaved among them. Implemented by `ZoneDayTimeline` positioning `result.unzonedTasks` with ordinary `TaskCapsuleBlock`s (`Positioned` by `scheduledAt`), alongside `result.containments`' `ZoneContainerBlock`s, in one `Stack`.
3. **Non-recurring zone applies to every day viewed** — confirmed directly as a genuine open question this session raised itself (not in the original work order). A `Zone` with `recurrenceRule == null` has no date field of its own, so `resolveZoneContainment`'s `_zoneAppliesOnDay` treats it as always-applicable, same as if it carried an implicit "every day" rule. A recurring zone still only applies on days its rule matches — mirrors `NotificationService`'s own private `_ruleMatchesDay` day-match logic exactly (duplicated, not imported, since the original is file-private — same "small pure helper, fine to duplicate" precedent as `_ZoneRecurrencePanel`).
4. **`resolveZoneContainment` built as a pure, provider-free function**, not logic embedded in a widget `build` — same "pure grouping function, widget layer consumes it" shape `detectOverlapClusters`/`OverlapCluster` already established, confirmed directly as the right architecture before writing any widget code. Fully unit-tested in isolation (`test/shared/services/zone_containment_test.dart`, 8 cases: explicit match, computed fallback, neither/unzoned, the zoneId-vs-time mismatch, a half-open boundary edge case, non-recurring-applies-every-day, recurring-only-matching-weekdays, and containment chronological ordering).
5. **`ZoneContainerBlock`'s inner task-row list mirrors `OverlapClusterBlock`'s `_ClusterTaskRow` precedent, not `TaskCapsuleBlock`'s own pill** — confirmed directly, twice: first via the original work order's own reasoning (flat list inside a spatial shell, not a second capsule treatment), then reinforced by a direct follow-up correction after an initial draft leaned toward the capsule's field order. Every row in `zone_container_block.dart`'s `_ZoneTaskRow` is the SAME fixed height (`zoneContainerRowHeight = 44.0`) regardless of the task's own duration — no proportional sizing, unlike `TaskCapsuleBlock`'s duration-scaled pill height. Field order is **start time → duration → category emoji glyph → title**, the reverse of the capsule's own icon-first/time-below layout — confirmed directly as the one deviation from the `OverlapClusterBlock` precedent (which has no duration column and no glyph at all), since the reference screenshot for this view showed all four fields on one row in that specific order.
6. **Container header** shows title + total duration together on the left (e.g. "Morning ritual (1h 30m)"), the zone's own start–end time range right-aligned on the same row — matches the reviewed reference screenshot's layout exactly. An empty zone (no member tasks at all, either path) still renders its container, per the confirmed spec — `ZoneContainmentResult.containments` includes one entry per applicable zone unconditionally, not filtered to non-empty ones.
7. **Container height: `max(strict time-span height, intrinsic content height)`, not a hand-computed minimum-height constant.** Judgment call, flagged rather than silently decided: rather than pick an arbitrary floor value that would need to stay in sync with the header's own padding/font-size and the row height constant by hand, `ZoneDayTimeline`'s `_PositionedContainer` wraps `ZoneContainerBlock` in `ConstrainedBox(minHeight: strictHeight)` + `IntrinsicHeight` — Flutter's own layout measures the real content height (header + padding + N fixed-height rows) and the container grows past its strict time-span height automatically whenever a short zone has more tasks than its natural duration would otherwise fit, with zero risk of clipping and no separate constant to maintain.
8. **`ZoneDayTimeline` is deliberately much simpler than `_DayTimeline`**: no drag-to-reschedule, no overlap clustering, no placement line, no "scroll to now" animation — all explicitly out of scope per CONSTITUTION.md's Zone section (drag/cascade stays deferred) and the work order's own non-goals. It reuses `TaskCapsuleBlock` as-is for unzoned tasks (same `pixelsPerMinute`, same tap/complete callbacks) but is otherwise a plain `StatelessWidget` over a `SingleChildScrollView` + `Stack` — read-focused; editing still goes through the ordinary task action sheet via `onTaskTap`.
9. **Settings toggle placed inside the existing `FeatureFlags.zoneEnabled`-gated "Zones" panel**, directly under "Manage zones" — not a new top-level Settings section — since it's a Zone-feature-specific display preference, not a general Timeline setting. With the flag off, the whole panel (including this toggle) is const-eliminated, so an ordinary build's Settings screen is completely unaffected, matching the flag's own "gates UI entry points only" contract.
10. **Two new dev scaffolds**, neither part of the shipped app: `zone_view_preview_main.dart` (seeds a mix of explicit-zoneId, computed-fallback, zoneId/time-mismatch, and genuinely-unzoned tasks across two zones, with the toggle pre-set on) and `zone_toggle_preview_main.dart` (opens straight to `SettingsScreen` against whatever data is already persisted, used to screenshot the toggle itself without needing simulator tap/scroll automation, which wasn't available in this environment — confirmed directly with the user rather than silently working around the gap).

## [2026-09-02] Timeline polish pass

**`_visibleRange` (`_DayTimeline` and `ZoneDayTimeline`) is now always a full 24h calendar day, superseding the earlier dynamic (task-derived) range decisions above.** Reported directly: a day with no tasks, or with tasks clustered in a narrow window, couldn't be scrolled past that window at all — not just "looked cut off" (the earlier, already-fixed viewport-floor issue), but a genuine scroll-range bug. Confirmed via AskUserQuestion: full 24h always, not a wider-but-still-task-derived padding. An empty day's "nothing scheduled" message now floats (`Stack` + `IgnorePointer`) over the still-scrollable timeline rather than replacing it, so an empty day is still fully scrollable/tappable (free-window/create-task affordances included).

**`CompletionCheckbox.ringColor` is optional and defaults to a fixed `theme.colorTextSecondary`, reversing the earlier "ring is always the category color" decision (recorded above) for the unchecked/default case too, not just the completed case.** Reported directly: the checkbox visually changing color per selected category read as wrong — a checkbox is a status control, not a category indicator. All three call sites (`TaskCapsuleBlock`, `ZoneContainerBlock`, `OverlapClusterBlock`'s cluster rows) now omit `ringColor` entirely rather than passing a category-derived color; `useMutedCompletedColor`'s own completed-state behavior is unaffected.

**Duration formatting (`"1h 40m"` vs the buggy `"100m"`) promoted to one shared `formatDurationLabel` (`features/timeline/duration_label.dart`), replacing three independently-duplicated local formatters.** `FreeWindowBlock` and `TaskCapsuleBlock`'s own local copies only handled the whole-hours and under-an-hour cases; `ZoneContainerBlock`'s own copy already had the correct three-way hour/minute split and was promoted as-is. Confirmed via AskUserQuestion: fix all three copies in one pass, not just the one reported.

**`DevShowFreeWindowPrompt` (`core/dev_config.dart`) gates whether `FreeWindowBlock` renders at all, as a dev-only in-memory scratch toggle — not a persisted Settings preference.** Confirmed via AskUserQuestion. Threaded down to `_DayTimeline` as a plain `showFreeWindowPrompt` field (mirrors `showHourLabels`'s own threading, since `_DayTimelineState` is a plain `State`, not `ConsumerState`), read once by `TimelineScreen` and combined with the existing `showHourLabels` gate on the `for (window in findFreeWindows(...))` loop.

**Tapping a task on the Timeline (Zone view and Task view) now opens Edit directly (`showTaskDetailSheet`) instead of the action sheet (Edit/Duplicate/Remove).** Requested directly. The action sheet (`task_action_sheet.dart`) stays in the codebase, unused, in case it's wanted again — not deleted.

**Edit-task screen gained a delete icon button to the left of Save, Edit-flow only (not Create), via generic `onSecondaryAction`/`secondaryActionIcon` params added to the shared `StepScaffold`** rather than a second scaffold or a task-specific one-off — keeps the create/edit view genuinely shared, per the work order's own constraint ("only if it doesn't create another screen"). Delete logic (`removeTask`/`askRemoveScope`/`RemoveScope`) promoted out of `task_action_sheet.dart` into a new shared `task_detail/task_remove.dart` so the action sheet's own Remove row and the new Edit-screen button share one recurring-scope-aware flow rather than duplicating it. Confirmed via AskUserQuestion: no confirmation dialog on the Edit-screen button — matches the action sheet's existing immediate-delete behavior exactly.

**Zone view's unzoned (outer-axis) task capsules now scale pill height by duration (`durationIndicatedBySize: true`), matching the Task view — only in-container rows and a row-being-dragged-out-of-a-container stay fixed-size.** Reported directly: previously EVERY outer-axis capsule in Zone view was forced to a fixed badge-size pill regardless of duration, reasoning (recorded above, at the time) that it should match `ZoneContainerBlock`'s own fixed-row-height convention — but an unzoned task isn't in a container at all, it sits on the same real time axis the Task view uses, so it should read the same way. The floating drag visual now conditionally matches: `durationIndicatedBySize: rowTask == null` — a row dragged OUT of a container still renders fixed-size (matching where it came from), while an outer-axis capsule being dragged keeps scaling by duration (matching its own resting size), so neither case changes size out from under the task the instant a drag starts.

**Zone view was missing `CurrentTimeIndicator` (the "now" line) entirely, and its outer horizontal padding used the wrong token (`spacingMd` instead of `spacingScreenPadding`), causing a visible horizontal jump when switching Task view ↔ Zone view.** Both reported directly. `CurrentTimeIndicator` added to `ZoneDayTimeline`'s Stack with the exact same `rangeStart`/`rangeEnd`/`pixelsPerMinute`/`gutterWidth` shape the Task view already passes it, so "now" lands on the identical pixel row in both modes. Padding fixed to `theme.spacingScreenPadding` (matching `_DayTimeline` exactly — `spacingMd` and `spacingScreenPadding` are genuinely different token values, not aliases).

**View-cycle button added to `DayStrip`, cycling Zone view → List view → Task view → Zone view** (skipping Zone when `FeatureFlags.zoneEnabled` is off), each mode with its own icon. Confirmed via AskUserQuestion: List view = `showHourLabels` off (with Zone view off), Task view = `showHourLabels` on (with Zone view off) — the two persisted settings together fully determine the three-way mode, no new persisted "mode" field needed. Re-centering the scroll position on the current hour (`_scrollToCurrentHourCentered`) now fires on every view-mode switch and day change, not just first mount — confirmed via AskUserQuestion as the intended trigger set (all three: mode switch, day change, hour-labels toggle).

## [2026-09-03] Automatic morning summary to Slack — background task, isolate/Hive gotcha, iOS reliability

Built as a work order, not an ad-hoc request — full spec (Settings UI, background scheduling, isolate-Hive re-init, summary generation, HTTP delivery, reliability copy, tests, real-device verification) given up front. Read CLAUDE.md/CONSTITUTION.md/ARCHITECTURE.md/DECISIONS.md/PROGRESS_LOG.md/ERROR_LOG.md first, per the order's own instruction.

**This feature is not in SCOPE.md and is the first to send data off-device — flagged and confirmed before writing any code, not built silently.** SCOPE.md's existing "today's plan summary" nice-to-have is explicitly "in-app only, not an OS widget," and CONSTITUTION.md's locked stack says "no cloud sync." Raised via AskUserQuestion; confirmed to proceed and add a new SCOPE.md entry documenting the feature as part of this same session (done — see SCOPE.md's Nice-to-have section) rather than treating the work order alone as sufficient authorization to expand scope.

**Two new dependencies, both pre-flagged in the work order itself and confirmed necessary before adding**: `workmanager` (`^0.10.9`, Android WorkManager + iOS BGTaskScheduler) and `http` (already a transitive dependency, promoted to direct — the standard first-party Dart HTTP client for the webhook POST). No existing dependency covers either.

**Settings additions follow the exact `PreferencesRepository`/`PreferenceKeys` pattern Phase 13a established**, not a new mechanism: `slackWebhookUrl`/`slackSummaryEnabled`/`slackDisplayName`/`slackIconEmoji`, four new `@Riverpod(keepAlive: true)` setting providers mirroring `ShowHourLabelsSetting`'s exact shape (bool) and a new nullable-`String` variant (trim-and-remove-if-blank on `set`) for the three text settings. `slackSummaryEnabled` is a genuinely separate flag from "is a webhook URL saved" — confirmed directly: pasting a URL must not silently turn on background sending.

**`buildSlackPayload`'s `username`/`icon_emoji` fields are OMITTED from the JSON entirely when blank, not sent as empty strings** — requested directly, since an empty string would override the webhook's own configured default with nothing, which is worse than not sending the field. Implemented as a plain `Map<String, dynamic>` built up conditionally, not a fixed-shape model class, since Slack's own payload shape is the only contract here (no local persistence of the payload itself).

**The real isolate/Hive gotcha, handled explicitly and tested, not assumed to work because it compiled**: `Workmanager`'s background callback (`backgroundTaskDispatcher`, `@pragma('vm:entry-point')`) runs in a fresh Dart isolate on Android and an equivalent fresh execution context on iOS — neither has gone through `main()`, so none of `Hive.initFlutter()`/`registerAdapters()`/open boxes carry over. `_sendMorningSummary()` redoes exactly the subset of `main.dart`'s own init sequence this task needs (`tasks` and `preferences` boxes only — not `categories`/`zones`/`trackedBehaviors`, which this task never reads), then goes through `HiveTaskRepository`/`HivePreferencesRepository` exactly like the main isolate does, never a raw `Hive.box<T>()` read outside a repository. Confirmed the gap is real (not just "it compiles, should be fine") by first reading `Hive.box<Task>(taskBoxName)` directly in an early draft without the re-init and hitting `HiveError: Box not found` — this is now the shape the shipped code deliberately avoids.

**Target hour is a named constant (`morningSummaryTargetHour = 7`), not an inline literal**, per the work order's own instruction. `_durationUntilNextTargetHour` (the actual scheduling math, separately unit-tested via a `@visibleForTesting` wrapper) computes the delay until the next occurrence of that hour — today's if it hasn't passed, tomorrow's otherwise, exactly-at-the-hour treated as already passed (rolls to tomorrow, not an immediate re-fire).

**Android**: `registerPeriodicTask` with a 24h `frequency` and a computed `initialDelay`, `ExistingPeriodicWorkPolicy.replace` (re-registering with the current setting value on every launch, same reasoning as `TaskList.refreshScheduledNotifications`'s own re-sync-on-launch pattern — a stale registration should never be trusted). New `AndroidManifest.xml` permission: `INTERNET` (needed for the real HTTP POST; `http`/`workmanager` are pure-Dart/self-merging plugins that don't declare this themselves, unlike `flutter_local_notifications`' Phase 6/7 manifest additions). No custom Android `Application` class needed — `workmanager_android 0.10.8` creates its own `FlutterEngine(applicationContext)` directly inside `BackgroundWorker`, confirmed by reading its Kotlin source rather than assumed from older setup docs that predate this shape.

**iOS**: `Info.plist` gained `UIBackgroundModes: [fetch]` (only `fetch` — this feature uses no `BGProcessingTask`, so `processing` isn't declared, per "don't design for hypothetical future requirement") and `BGTaskSchedulerPermittedIdentifiers: [com.amble.morningSummary]`, the exact task identifier registered in Dart (`morningSummaryTaskName`) — iOS validates BGTaskScheduler registrations against this list and silently refuses anything not present. **No `AppDelegate.swift` change was needed**, confirmed by reading `workmanager_apple`'s own `WorkmanagerPlugin.swift` directly (not assumed from the Phase 6/7 `flutter_local_notifications` precedent, which DID need an `AppDelegate.swift` addition): `registerPeriodicTask` self-registers its BGTaskScheduler launch handler dynamically at runtime, unlike the notification plugin's own setup.

**iOS build blocked on this machine — a genuine environment/SDK gap, not a code problem, reported rather than silently worked around.** `workmanager_apple 0.9.6+` (pulled in by `workmanager ^0.10.9`, the version this feature needs for its modern BGTaskScheduler-based `registerPeriodicTask` API) unconditionally references `BGContinuedProcessingTask` — an iOS 26 API — in Swift source that ships regardless of deployment target. This machine's Xcode 15.4 (iOS 17.5 SDK, same version noted as "functional, upgrade to 16+ deferred as non-blocking" in the Phase 9 entry above) doesn't have that type in its `BackgroundTasks.framework` headers at all, so the build fails to COMPILE, not just to run. Investigated a `dependency_overrides` pin to `workmanager_apple: 0.9.5` (the last version before that source file was added, confirmed via its own CHANGELOG.md) — reverted after `flutter pub get` proved it's not viable: `workmanager_apple <0.9.6` requires `workmanager_platform_interface ^0.9.3`, incompatible with `workmanager 0.10.9`'s `^0.10.4` requirement. There is no version combination that keeps the modern `workmanager` Dart API this feature needs while avoiding the plugin's Swift compile error on this SDK — genuinely blocked until Xcode is upgraded past whatever version first ships `BGContinuedProcessingTask`'s declaration (believed to be Xcode 16+, unconfirmed on this machine). Android is fully unaffected and independently verified (see PROGRESS_LOG.md).

**Real device/environment testing status, reported plainly per the work order's own instruction not to treat emulator/simulator behavior as equivalent to hardware**: no physical Android or iOS device was available this session (`flutter devices` listed only iOS Simulator and macOS desktop at session start). Android verified on the `Amble_Test_API34` emulator from an earlier session (API 34) — real debug APK install, cold launch (confirming `Workmanager().initialize()` doesn't crash startup), and Settings screenshots of every UI element the work order named. On-device TAP interaction on that same emulator turned out to be unreliable in this environment — `adb shell input tap` stopped registering on ANY button partway through verification, confirmed as an environment/tooling issue (not a bug in this feature) by reproducing the same non-response on the pre-existing, already-shipped "Export backup" button. Switched to a deterministic Flutter widget test (`slack_summary_settings_test.dart`) instead, which is the project's own established stronger-evidence path anyway (see the Phase 6 entry on `flutter test` catching what a screenshot alone couldn't): it drives the real widget tree against real Hive-backed repositories and proves both the empty-URL validation message and a real end-to-end HTTP round-trip (via `flutter_test`'s own documented always-400 network stub) surface correctly in the UI. iOS could not be run at all this session (see the build-blocker entry above), so iOS delivery timing/reliability is **entirely unverified on this machine** — flagged as a genuine open gap, not claimed as working from code review alone. The work order's own instruction to be honest about iOS's real observed unreliability (may not fire on time, may not fire some days) is accepted as the expected steady-state behavior once it CAN be verified on real hardware; this session could not produce a first-hand observation of it.

`dart format .`: clean. `flutter analyze`: clean (same 2 pre-existing unrelated warnings only). `flutter test`: 391/392 passing — the one failure (`zone_form_screen_test.dart`, a `LateInitializationError` from `flutter_local_notifications`' platform interface) is pre-existing, in a file untouched this session, confirmed unrelated before reporting done. 22 new tests added across `slack_summary_service_test.dart` (15: summary text generation including empty/sorted/Inbox-excluded cases, payload field omission, webhook validation/HTTP success/failure/network-error handling), `background_tasks_test.dart` (4: the target-hour scheduling math), and `slack_summary_settings_test.dart` (3: field/toggle/button rendering, empty-URL validation, real end-to-end send).

## [2026-09-03] iOS `workmanager_apple` build blocker: `dependency_overrides` pin investigated and confirmed not viable, not silently attempted

Follow-up to the entry above. Tried pinning `workmanager_apple: 0.9.5` (last version before `BGContinuedProcessingTask`) via `dependency_overrides` to unblock iOS Simulator without an Xcode upgrade. `flutter pub get` failed version solving: `workmanager_apple <0.9.6` requires `workmanager_platform_interface ^0.9.3`, but `workmanager ^0.10.9` (needed for this feature's `registerPeriodicTask`/BGTaskScheduler API) requires `^0.10.4` — genuinely incompatible, not a constraint that could be loosened. Reverted the override immediately rather than leaving a broken `pubspec.yaml` in place. Confirmed via AskUserQuestion not to source-patch the plugin locally (vendoring a modified copy via a `path:` override) either — the user chose to leave iOS blocked and upgrade Xcode instead, judged the more durable fix over a local patch that could silently drift from upstream. `pubspec.yaml`/`pubspec.lock` are back to their pre-investigation state (`workmanager: ^0.10.9`, no override), confirmed via `flutter analyze`/`flutter test` staying clean after the revert.

## [2026-09-03] Dev Settings: bulk data-clearing tools

Requested directly, alongside the iOS investigation above: buttons to clear all tasks, clear only the "bad state" orphaned-recurring tasks (the ones that throw `Bad state: No element` — see the earlier `deleteTask`/`findSeriesTemplate` orphan-bug entries), clear all zones, and clear everything.

**Placed in Settings' existing Developer section, not a new one** — same `kDebugMode` gate every other control there already uses, no new gating mechanism introduced. **Every button confirms via a new `_confirmAndClear` helper (`AppAlertDialog`) before running**, judged necessary even for dev-only tooling: these are irreversible bulk deletes, and CLAUDE.md's own guidance on destructive actions applies regardless of whether the screen is debug-only — a mis-tap shouldn't be able to silently wipe a developer's real local data with no chance to back out.

**"Clear bad-state tasks" reuses the existing orphan-detection shape rather than inventing a new one**: a new pure function, `findOrphanedRecurringTaskIds(List<Task>)`, placed next to `findSeriesTemplate` in `task_providers.dart` since it answers the mirror-image question ("which tasks have NO template for their series," vs. `findSeriesTemplate`'s "find THE template for this task's series") — same file, same "pure function over a list the caller already has" shape. `TaskList.clearBadStateTasks()` deletes exactly those ids and returns the count, so the button can report what it actually did rather than a generic "done."

**`clearAllTasks`/`clearAllZones` loop the existing single-item `deleteTask`/`delete` per id rather than adding a bulk-delete method to either repository interface** — no repository currently exposes one, and adding `deleteAll()` to `TaskRepository`/`ZoneRepository` for a dev-only caller would be new production surface for a debug-only need. Deliberately match each single-delete method's own existing behavior exactly, not a "better" version: `clearAllZones` does NOT cancel per-zone notifications, because `ZoneList.deleteZone` itself doesn't either (a real, separate, pre-existing gap — flagged, not silently fixed as a drive-by inside this unrelated change).

## [2026-09-04] Zone containment: TIME wins over an explicit `zoneId` when both are set

**Reverses** the original Spatial Zone View rule (`zoneId` authoritative, winning even over a conflicting `scheduledAt`). Confirmed directly via AskUserQuestion after the alternative options (clear `zoneId` on time change; keep the rule but show a mismatch cue) were put side by side.

**Reported symptom**: a task reading 5:15–17:15 in Task view rendered inside the 09:00–13:00 container in Zone view.

**Root cause**: `zoneId` is only ever SET, never cleared. `rescheduleTaskWithZone` (a Zone-view drop) assigns it; `rescheduleTask` (Task view's own drag) deliberately never touches it, and the task detail sheet never references `zoneId` at all. So any later time change stranded a stale assignment that then outranked the task's real time, and the two views disagreed about the same task.

**Resolution now**: if `scheduledAt` is set, the task places by time — into whichever zone's window contains it, or into no zone if it falls outside every one, regardless of `zoneId`. Only a zone-only task (no `scheduledAt`) places by `zoneId`, which is the case the "a task can be assigned to a zone with no scheduledAt at all" design note exists for. Still strictly read-only: nothing writes or clears `zoneId`, so a stale assignment simply stops affecting placement and stays on the row.

**Why this over clearing `zoneId` on time change**: that would need every current and future write path that touches `scheduledAt` to also maintain the assignment — exactly the kind of invariant that decays silently (this bug is what its absence already looks like). Resolving by time at read time cannot drift.

## [2026-09-04] Overlap clustering: the 3-task cap is removed — EVERY overlapping run clusters

**Reverses** item 1 of the 2026-08 "[Timeline] Overlap clustering" entry above, which capped a cluster at `overlapClusterCap = 3` and let a run of 4+ fall through to plain side-by-side capsules. That cap was itself confirmed directly at the time, as a narrow choice for a case the visual design hadn't specified.

**Reported symptom**: "now we have 2 styles of overlap — some tasks overlap as overlapping but not cluster mode, some overlap as cluster mode; all should overlap as cluster mode." The cap is exactly what produced the two styles: a 2–3 run got the aggregate treatment, a 4+ run silently got the old naive one, on the same day.

`overlapClusterCap` is deleted (not just raised) — a bound of any size reintroduces the same split at a different depth. `detectOverlapClusters` now emits a cluster for every contiguous run of 2 or more, however deep. Nothing downstream needed changing: `_withClusterLanes` already assigns one lane per member for any cluster size, and the cluster list renders a row per member.

Three tests that encoded the cap were rewritten rather than deleted — a 4-task run now asserts one 4-task cluster, a 7-task run asserts one 7-task cluster (proving it is never clipped to the first few), and the before/after test asserts the unrelated tasks are excluded from the cluster rather than the whole run being excluded from clustering.

## [2026-09-05] Task size becomes a global sm/md/lg setting, not a fixed per-view value

Requested directly, following the earlier session's pill-size unification: "let's establish this size as sm ... let's bring md that is slight larger ... previous before this change was lg ... with that also task name and time font size changes." Confirmed via follow-up: this is ONE global setting affecting Task view, List view, AND Zone view together ("it's not per view, it's a setting, that when set affects all"), with List view tracking the setting exactly (no relative "one step smaller" any more) and final values sm=20/12, md=24/14, lg=28/16.

**Architecture**: `AmbleTheme` gains three fixed reference rungs (`sizeTaskBadgeSm/Md/Lg`, `textTaskTitleSm/Md/Lg`) plus keeps `sizeTaskBadge`/`textTaskTitle` as the single ACTIVE fields every real call site already reads. A new `TaskSize` enum (its own Hive-adapter type, typeId 11, mirroring `AppThemeMode`'s own pattern) is persisted via `TaskSizeSetting`. `main.dart`'s `AmbleApp` watches that provider and resolves the active fields onto both light and dark palettes via `AmbleTheme.copyWith` BEFORE either reaches `MaterialApp`'s `extensions` — so `TaskCapsuleBlock`, `ZoneContainerBlock`, and `OverlapClusterBlock` need zero changes; they already read `theme.sizeTaskBadge`/`theme.textTaskTitle` and simply receive whatever the setting resolved to. This is the same "resolve once, consume everywhere" shape the codebase already uses for light/dark itself.

**`textTaskTitleCompact` removed.** List view previously read a separate, always-one-step-smaller style regardless of what Task view's own size was. Confirmed directly that List view should now track the exact same global setting with no relative step, so the separate token was deleted rather than kept alongside the new scale (keeping it would have meant TWO ways to be "small," inviting drift). `TaskCapsuleBlock.compactText` and `OverlapClusterBlock.compactText` still exist and still force the INLINE one-line layout in List mode — that part of their contract is unchanged; only the now-removed font-size branch is gone.

**Default is `TaskSize.md`** (24px/14px) — Task view's own prior fixed size before this session's task-size work began, not the `sm` (20px) value the immediately-preceding session had just unified everything onto. That earlier unification is effectively superseded: `sm` is still available (and is what List view happened to look like historically), but it's no longer the default for a fresh install.

New primitive `SpacingPrimitives.space7Point5` (28.0) added for lg's badge size — the existing spacing scale jumps 24→32 with nothing between, and 28 was an explicit, confirmed number rather than derivable from the existing scale.

**Settings UI**: a new "Task size" panel (Small/Medium/Large `AppSelectableChip`s, matching the existing duration-preset chip pattern) placed near the other Timeline display settings, per direct placement confirmation.
